{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "toggle",
  "type": "registry:ui",
  "title": "BTToggle",
  "description": "Pressable pill toggle atom. Caller controls `pressed` state; component fires onChange(!pressed) on click. Supports default and outline variants, default and small sizes, optional leftIcon/rightIcon props.",
  "framework": "react",
  "category": "atoms",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/BTech-Design-System?node-id=2855-1224",
  "figmaNodeId": "2855-1224",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "BTToggle.css",
      "target": "@ui/BTToggle.css",
      "type": "registry:ui",
      "content": "/* BTToggle styles — Figma 2855-1224.\n   All colors via @btech/tokens CSS vars — no hex fallbacks. */\n\n.bt-toggle {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-2xs);\n  padding: var(--space-sm);\n  border-radius: var(--radius-sm);\n  border: 1px solid transparent;\n  font-family: var(--typography-font-family-sans);\n  font-size: var(--typography-font-size-sm);\n  font-weight: 500;\n  line-height: var(--typography-line-height-xs);\n  letter-spacing: 0;\n  white-space: nowrap;\n  cursor: pointer;\n  user-select: none;\n  outline: none;\n  transition: background 0.15s, border-color 0.15s;\n}\n\n.bt-toggle:focus-visible {\n  outline: 2px solid var(--color-brand-primary);\n  outline-offset: 2px;\n}\n\n/* ── Small ── */\n.bt-toggle--small {\n  padding: var(--space-xs);\n  font-size: var(--typography-font-size-xs);\n}\n\n/* ── Default variant — inactive ── */\n.bt-toggle--default {\n  background: var(--bg-subtle);\n  color: var(--text-primary);\n}\n.bt-toggle--default:hover:not(:disabled) {\n  background: var(--bg-subtler);\n}\n\n/* ── Default variant — active ── */\n.bt-toggle--default.bt-toggle--active {\n  background: var(--color-brand-primary);\n  color: var(--text-inverse);\n}\n.bt-toggle--default.bt-toggle--active:hover:not(:disabled) {\n  background: var(--color-brand-primary-bold);\n}\n\n/* ── Outline variant — inactive ── */\n.bt-toggle--outline {\n  background: transparent;\n  color: var(--text-secondary);\n  border-color: var(--border-primary);\n}\n.bt-toggle--outline:hover:not(:disabled) {\n  background: var(--bg-subtle);\n}\n\n/* ── Outline variant — active ── */\n.bt-toggle--outline.bt-toggle--active {\n  background: var(--bg-subtle);\n  color: var(--text-secondary);\n  border-color: transparent;\n}\n.bt-toggle--outline.bt-toggle--active:hover:not(:disabled) {\n  background: var(--bg-subtler);\n}\n\n/* ── Disabled ── */\n.bt-toggle:disabled {\n  opacity: 0.5;\n  cursor: not-allowed;\n}\n\n/* ── Icon wrappers ── */\n.bt-toggle__icon {\n  display: inline-flex;\n  align-items: center;\n  flex-shrink: 0;\n}\n.bt-toggle__icon img,\n.bt-toggle__icon svg {\n  width: 16px;\n  height: 16px;\n}\n"
    },
    {
      "path": "BTToggle.tsx",
      "target": "@ui/BTToggle.tsx",
      "type": "registry:ui",
      "content": "/**\n * BTToggle — pressable pill toggle atom.\n * Figma source: node 2855-1224.\n * Caller controls `pressed` state; component fires `onChange(!pressed)` on click.\n *\n * @example\n * ```tsx\n * // Default inactive\n * <BTToggle label=\"Option\" onChange={onToggle} />\n *\n * // Controlled active\n * <BTToggle label=\"Bold\" pressed={isBold} onChange={v => setIsBold(v)} />\n *\n * // Outline with icon\n * <BTToggle\n *   label=\"Bold\"\n *   variant=\"outline\"\n *   pressed={isBold}\n *   leftIcon={<BoldIcon />}\n *   onChange={v => setIsBold(v)}\n * />\n *\n * // Disabled\n * <BTToggle label=\"Option\" disabled />\n * ```\n */\nimport './BTToggle.css';\nimport type { BTToggleProps } from './BTToggle.types';\n\nexport function BTToggle({\n  label,\n  pressed   = false,\n  variant   = 'default',\n  size      = 'default',\n  disabled  = false,\n  onChange,\n  leftIcon,\n  rightIcon,\n  className,\n}: BTToggleProps) {\n  const cls = [\n    'bt-toggle',\n    `bt-toggle--${variant}`,\n    pressed ? 'bt-toggle--active' : '',\n    size === 'small' ? 'bt-toggle--small' : '',\n    className ?? '',\n  ]\n    .filter(Boolean)\n    .join(' ');\n\n  function handleClick() {\n    if (!disabled) onChange?.(!pressed);\n  }\n\n  return (\n    <button\n      className={cls}\n      disabled={disabled}\n      type=\"button\"\n      aria-pressed={pressed}\n      data-testid=\"bt-toggle\"\n      onClick={handleClick}\n    >\n      {leftIcon  && <span className=\"bt-toggle__icon bt-toggle__icon--left\">{leftIcon}</span>}\n      <span className=\"bt-toggle__label\">{label}</span>\n      {rightIcon && <span className=\"bt-toggle__icon bt-toggle__icon--right\">{rightIcon}</span>}\n    </button>\n  );\n}\n\nBTToggle.displayName = 'BTToggle';\n"
    },
    {
      "path": "BTToggle.types.ts",
      "target": "@ui/BTToggle.types.ts",
      "type": "registry:ui",
      "content": "export type BTToggleVariant = 'default' | 'outline';\nexport type BTToggleSize    = 'default' | 'small';\n\nexport interface BTToggleProps {\n  /** Text shown inside the toggle. */\n  label: string;\n  /** Controlled pressed/active state. Default: false. */\n  pressed?: boolean;\n  /** Visual style. Default: 'default'. */\n  variant?: BTToggleVariant;\n  /** Padding scale. Default: 'default'. */\n  size?: BTToggleSize;\n  /** Disables interaction and applies opacity:0.5. Default: false. */\n  disabled?: boolean;\n  /** Fired when user clicks — receives the new pressed value. */\n  onChange?: (pressed: boolean) => void;\n  /** Icon rendered before the label (React only). */\n  leftIcon?: React.ReactNode;\n  /** Icon rendered after the label (React only). */\n  rightIcon?: React.ReactNode;\n  /** Extra class name (React only). */\n  className?: string;\n}\n"
    }
  ]
}