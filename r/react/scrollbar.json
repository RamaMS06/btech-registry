{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "scrollbar",
  "type": "registry:ui",
  "title": "BTScrollbar",
  "description": "An overflow wrapper div that renders a thin, token-coloured scrollbar. Thumb: bg-subtlest (neutral-300), Track: bg-subtle (neutral-50), 4px wide.",
  "framework": "react",
  "category": "atoms",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/Btech---Design-System?node-id=2037-4",
  "figmaNodeId": "2037:4",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "BTScrollbar.css",
      "target": "@ui/scrollbar/BTScrollbar.css",
      "type": "registry:ui",
      "content": "/* BTScrollbar — custom scrollbar wrapper atom (Figma 2037:4)\r\n *\r\n * Applies btech-token scrollbar styling to an overflow container.\r\n * Thumb: bg-subtlest (neutral-300), Track: bg-subtle (neutral-50).\r\n * Width/height: 4px. Border-radius: radius-xs (4px).\r\n *\r\n * Usage:\r\n *   Wrap any overflowing content in <BTScrollbar direction=\"y\">.\r\n *   The wrapper is display:block by default; set an explicit height on\r\n *   the parent or the scrollbar itself for the scrollbar to appear.\r\n */\r\n\r\n.bt-scrollbar {\r\n  /* layout defaults — caller controls size */\r\n  display: block;\r\n  overflow: hidden;\r\n}\r\n\r\n/* ── Direction variants ──────────────────────────────────────────────────── */\r\n\r\n.bt-scrollbar--x {\r\n  overflow-x: auto;\r\n  overflow-y: hidden;\r\n}\r\n\r\n.bt-scrollbar--y {\r\n  overflow-x: hidden;\r\n  overflow-y: auto;\r\n}\r\n\r\n.bt-scrollbar--both {\r\n  overflow: auto;\r\n}\r\n\r\n/* ── Firefox (scrollbar-width / scrollbar-color) ─────────────────────────── */\r\n\r\n.bt-scrollbar--x,\r\n.bt-scrollbar--y,\r\n.bt-scrollbar--both {\r\n  scrollbar-width: thin;\r\n  scrollbar-color: var(--bg-subtlest) var(--bg-subtle);\r\n}\r\n\r\n/* ── Webkit (Chrome / Safari / Edge) ─────────────────────────────────────── */\r\n\r\n/* Vertical scrollbar */\r\n.bt-scrollbar--y::-webkit-scrollbar,\r\n.bt-scrollbar--both::-webkit-scrollbar {\r\n  width: 4px;\r\n}\r\n\r\n/* Horizontal scrollbar */\r\n.bt-scrollbar--x::-webkit-scrollbar,\r\n.bt-scrollbar--both::-webkit-scrollbar {\r\n  height: 4px;\r\n}\r\n\r\n.bt-scrollbar--x::-webkit-scrollbar-track,\r\n.bt-scrollbar--y::-webkit-scrollbar-track,\r\n.bt-scrollbar--both::-webkit-scrollbar-track {\r\n  background: var(--bg-subtle);\r\n  border-radius: var(--radius-xs);\r\n}\r\n\r\n.bt-scrollbar--x::-webkit-scrollbar-thumb,\r\n.bt-scrollbar--y::-webkit-scrollbar-thumb,\r\n.bt-scrollbar--both::-webkit-scrollbar-thumb {\r\n  background: var(--bg-subtlest);\r\n  border-radius: var(--radius-xs);\r\n}\r\n\r\n.bt-scrollbar--x::-webkit-scrollbar-thumb:hover,\r\n.bt-scrollbar--y::-webkit-scrollbar-thumb:hover,\r\n.bt-scrollbar--both::-webkit-scrollbar-thumb:hover {\r\n  background: var(--text-disabled);\r\n}\r\n"
    },
    {
      "path": "BTScrollbar.tsx",
      "target": "@ui/scrollbar/BTScrollbar.tsx",
      "type": "registry:ui",
      "content": "/**\r\n * BTScrollbar — overflow wrapper with btech token scrollbar styling (Figma 2037:4).\r\n *\r\n * Wraps any overflowing content and applies a thin, token-coloured scrollbar.\r\n * Set an explicit height (or max-height) on the host element for overflow to\r\n * take effect.\r\n *\r\n * @example\r\n * ```tsx\r\n * // Vertical scroll (default)\r\n * <BTScrollbar style={{ height: 300 }}>\r\n *   {items.map((item, i) => <div key={i}>{item}</div>)}\r\n * </BTScrollbar>\r\n *\r\n * // Horizontal scroll\r\n * <BTScrollbar direction=\"x\" style={{ width: 400 }}>\r\n *   <div style={{ whiteSpace: 'nowrap', width: 800 }}>Wide content…</div>\r\n * </BTScrollbar>\r\n * ```\r\n */\r\nimport React from 'react';\r\nimport './BTScrollbar.css';\r\nimport type { BTScrollbarProps } from './BTScrollbar.types';\r\n\r\nexport const BTScrollbar = React.forwardRef<\r\n  HTMLDivElement,\r\n  BTScrollbarProps & React.HTMLAttributes<HTMLDivElement>\r\n>(function BTScrollbar({ direction = 'y', className, children, ...rest }, ref) {\r\n  const classes = [\r\n    'bt-scrollbar',\r\n    `bt-scrollbar--${direction}`,\r\n    className,\r\n  ]\r\n    .filter(Boolean)\r\n    .join(' ');\r\n\r\n  return (\r\n    <div ref={ref} className={classes} {...rest}>\r\n      {children}\r\n    </div>\r\n  );\r\n});\r\n\r\nBTScrollbar.displayName = 'BTScrollbar';\r\n"
    },
    {
      "path": "BTScrollbar.types.ts",
      "target": "@ui/scrollbar/BTScrollbar.types.ts",
      "type": "registry:ui",
      "content": "/**\r\n * BTScrollbar — shared prop types (Vue + React).\r\n */\r\n\r\n/** Scroll axis shown with custom scrollbar styling. */\r\nexport type BTScrollbarDirection = 'x' | 'y' | 'both';\r\n\r\nexport interface BTScrollbarProps {\r\n  /**\r\n   * Which axis shows the custom scrollbar.\r\n   * @default 'y'\r\n   */\r\n  direction?: BTScrollbarDirection;\r\n  /** Additional CSS class names. */\r\n  className?: string;\r\n  /** Child content rendered inside the scrollable container. */\r\n  children?: React.ReactNode;\r\n}\r\n"
    },
    {
      "path": "index.ts",
      "target": "@ui/scrollbar/index.ts",
      "type": "registry:ui",
      "content": "export { BTScrollbar } from './BTScrollbar';\r\nexport type { BTScrollbarProps, BTScrollbarDirection } from './BTScrollbar.types';\r\n"
    }
  ]
}