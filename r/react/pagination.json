{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "pagination",
  "type": "registry:ui",
  "title": "BTPagination",
  "description": "Data pagination molecule. Three variants: 'number' (first/prev/pages/next/ last), 'display' (rows-per-page selector + prev/next), and 'step' (Step N of M + Prev/Next).",
  "framework": "react",
  "category": "molecules",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/Btech---Design-System?node-id=749-2846",
  "figmaNodeId": "749-2846",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [
    "button",
    "dropdown",
    "dropdown-list",
    "step-number"
  ],
  "files": [
    {
      "path": "BTPagination.css",
      "target": "@ui/pagination/BTPagination.css",
      "type": "registry:ui",
      "content": "/* BTPagination — Figma node 749-2846 */\n\n/* ── Root ──────────────────────────────────────────────────────────────────── */\n\n.bt-pagination {\n  display: flex;\n  align-items: center;\n  width: 100%;\n  font-family: inherit;\n}\n\n.bt-pagination--number {\n  gap: var(--space-lg);\n}\n\n.bt-pagination--display,\n.bt-pagination--step {\n  justify-content: space-between;\n}\n\n/* ── Nav button group ───────────────────────────────────────────────────────── */\n\n.bt-pagination__nav {\n  display: inline-flex;\n  align-items: center;\n  gap: var(--space-sm);\n}\n\n/* ── Pages center section ───────────────────────────────────────────────────── */\n\n.bt-pagination__pages {\n  display: flex;\n  align-items: center;\n  gap: var(--space-sm);\n  flex: 1;\n  justify-content: center;\n}\n\n/* ── Individual page pill ───────────────────────────────────────────────────── */\n\n.bt-pagination__page {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 32px;\n  height: 32px;\n  border: 1px solid var(--border-primary);\n  border-radius: var(--radius-sm);\n  font-size: var(--typography-font-size-xs);\n  font-weight: 500;\n  line-height: var(--typography-line-height-xs);\n  color: var(--text-primary);\n  cursor: pointer;\n  background: transparent;\n  transition: background 100ms ease, border-color 100ms ease;\n  flex-shrink: 0;\n}\n\n.bt-pagination__page:hover:not(.bt-pagination__page--active):not(.bt-pagination__page--ellipsis) {\n  background: var(--bg-subtle);\n}\n\n.bt-pagination__page--active {\n  background: var(--bg-subtle);\n  border-color: var(--color-brand-primary);\n}\n\n.bt-pagination__page--ellipsis {\n  border: none;\n  pointer-events: none;\n  cursor: default;\n  color: var(--text-secondary);\n}\n\n/* ── Display variant ────────────────────────────────────────────────────────── */\n\n.bt-pagination__display-left {\n  display: flex;\n  align-items: center;\n  gap: var(--space-sm);\n}\n\n.bt-pagination__rows-label {\n  font-size: var(--typography-font-size-sm);\n  font-weight: 500;\n  color: var(--text-secondary);\n  white-space: nowrap;\n}\n\n/* Rows-per-page trigger button (wraps the BTDropdown custom trigger) */\n.bt-pagination__rows-trigger {\n  display: inline-flex;\n  align-items: center;\n  gap: var(--space-xs);\n  border: 1px solid var(--border-primary);\n  border-radius: var(--radius-sm);\n  padding: 6px var(--space-sm);\n  font-size: var(--typography-font-size-xs);\n  font-weight: 500;\n  line-height: var(--typography-line-height-xs);\n  color: var(--text-primary);\n  background: transparent;\n  cursor: pointer;\n  font-family: inherit;\n  transition: border-color 100ms ease, background 100ms ease;\n  white-space: nowrap;\n}\n\n.bt-pagination__rows-trigger:hover {\n  background: var(--bg-subtle);\n}\n\n.bt-pagination__rows-trigger--open,\n.bt-pagination__rows-trigger:focus-visible {\n  outline: none;\n  border-color: var(--color-brand-primary);\n}\n\n/* Chevron rotates when open.\n   transform-box: fill-box + transform-origin: center ensures the SVG rotates\n   around its own visual centre, not the SVG coordinate-system origin (0 0). */\n.bt-pagination__rows-chevron-icon {\n  display: block;\n  flex-shrink: 0;\n  transform-box: fill-box;\n  transform-origin: center;\n  transition: transform 150ms ease;\n}\n\n.bt-pagination__rows-chevron-icon--open {\n  transform: rotate(180deg);\n}\n\n/* RTL: render entire pagination as LTR so the visual layout stays default\n   (|< < [Next] page [Previous] > >|). The button text labels are swapped\n   in component code so that the LEFT button reads \"Next\" and the RIGHT\n   reads \"Previous\" — matching RTL reading direction semantics. */\n[dir=\"rtl\"] .bt-pagination,\n[dir=\"rtl\"] .bt-pagination *,\n[dir=\"rtl\"] .bt-pagination__nav,\n[dir=\"rtl\"] .bt-pagination__pages,\n[dir=\"rtl\"] .bt-pagination__display-left {\n  direction: ltr;\n}\n\n/* ── Display variant RTL: swap nav (left) and rows-per-page (right) ─────────── */\n[dir=\"rtl\"] .bt-pagination--display .bt-pagination__nav {\n  order: -1;\n}\n\n/* ── Display variant RTL: mirror rows-per-page inner order (dropdown first) ─── */\n[dir=\"rtl\"] .bt-pagination__display-left {\n  flex-direction: row-reverse;\n}\n\n/* ── Display variant RTL: chevron on left inside rows-per-page trigger ───────── */\n[dir=\"rtl\"] .bt-pagination__rows-trigger {\n  flex-direction: row-reverse;\n}\n\n/* ── Step variant ───────────────────────────────────────────────────────────── */\n\n.bt-pagination__step-label {\n  font-size: var(--typography-font-size-sm);\n  font-weight: 500;\n  color: var(--text-primary);\n}\n\n/* Step nav: 16px gap between Prev and Next buttons */\n.bt-pagination__nav--step {\n  gap: var(--space-lg);\n}\n\n/* ── Icon wrapper ───────────────────────────────────────────────────────────── */\n\n.bt-pagination__icon {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  width: 16px;\n  height: 16px;\n  flex-shrink: 0;\n}\n\n.bt-pagination__icon > svg {\n  width: 16px;\n  height: 16px;\n  display: block;\n}\n"
    },
    {
      "path": "BTPagination.tsx",
      "target": "@ui/pagination/BTPagination.tsx",
      "type": "registry:ui",
      "content": "/**\n * BTPagination — data pagination molecule (React).\n * Figma: node 749-2846.\n *\n * @example number\n * ```tsx\n * <BTPagination currentPage={page} totalPages={20} onPageChange={setPage} />\n * ```\n *\n * @example display\n * ```tsx\n * <BTPagination\n *   variant=\"display\"\n *   currentPage={page}\n *   totalCount={137}\n *   rowsPerPage={rowsPerPage}\n *   rowsPerPageOptions={[10, 20, 50]}\n *   onPageChange={setPage}\n *   onRowsChange={setRowsPerPage}\n * />\n * ```\n *\n * @example step\n * ```tsx\n * <BTPagination\n *   variant=\"step\"\n *   currentStep={step}\n *   totalSteps={5}\n *   onPrev={() => setStep(s => Math.max(1, s - 1))}\n *   onNext={() => setStep(s => Math.min(5, s + 1))}\n * />\n * ```\n */\nimport './BTPagination.css';\nimport { useEffect, useRef, useState } from 'react';\nimport type { BTPaginationProps } from './BTPagination.types';\nimport { BTStepNumber } from '../step-number/BTStepNumber';\nimport { BTDropdown } from '../dropdown/BTDropdown';\nimport { BTButton } from '../button/BTButton';\nimport type { BTDropdownItem } from '../dropdown-list/BTDropdownList.types';\n\n// ── Inline SVG icons ─────────────────────────────────────────────────────────\n\nfunction IconChevronLeft() {\n  return (\n    <svg viewBox=\"0 0 24 24\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" aria-hidden=\"true\">\n      <path d=\"M15.41 7.41L14 6L8 12L14 18L15.41 16.59L10.83 12L15.41 7.41Z\" fill=\"currentColor\"/>\n    </svg>\n  );\n}\n\nfunction IconChevronRight() {\n  return (\n    <svg viewBox=\"0 0 24 24\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" aria-hidden=\"true\">\n      <path d=\"M8.59 16.59L10 18L16 12L10 6L8.59 7.41L13.17 12L8.59 16.59Z\" fill=\"currentColor\"/>\n    </svg>\n  );\n}\n\nfunction IconChevronDown({ open }: { open?: boolean }) {\n  return (\n    <svg\n      viewBox=\"0 0 24 24\"\n      fill=\"none\"\n      xmlns=\"http://www.w3.org/2000/svg\"\n      aria-hidden=\"true\"\n      className={[\n        'bt-pagination__rows-chevron-icon',\n        open && 'bt-pagination__rows-chevron-icon--open',\n      ].filter(Boolean).join(' ')}\n    >\n      <path d=\"M7.41 8.59L12 13.17L16.59 8.59L18 10L12 16L6 10L7.41 8.59Z\" fill=\"currentColor\"/>\n    </svg>\n  );\n}\n\nfunction IconFirstPage() {\n  return (\n    // viewBox pads the 8.27×8 path to match Figma's 25% inset inside a 16px icon container\n    <svg viewBox=\"-4 -4 16 16\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" aria-hidden=\"true\">\n      <path d=\"M0 8V0H1.33333V8H0ZM7.33333 8L3.33333 4L7.33333 0L8.26667 0.933333L5.2 4L8.26667 7.06667L7.33333 8Z\" fill=\"currentColor\"/>\n    </svg>\n  );\n}\n\nfunction IconLastPage() {\n  return (\n    // viewBox pads the 8.27×8 path to match Figma's 25% inset inside a 16px icon container\n    <svg viewBox=\"-4 -4 16 16\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\" aria-hidden=\"true\">\n      <path d=\"M0.933333 8L0 7.06667L3.06667 4L0 0.933333L0.933333 0L4.93333 4L0.933333 8ZM6.93333 8V0H8.26667V8H6.93333Z\" fill=\"currentColor\"/>\n    </svg>\n  );\n}\n\n// ── Windowing algorithm ──────────────────────────────────────────────────────\n\ntype PageItem = number | 'ellipsis';\n\nfunction buildVisiblePages(currentPage: number, totalPages: number): PageItem[] {\n  if (totalPages <= 7) {\n    return Array.from({ length: totalPages }, (_, i) => i + 1);\n  }\n  if (currentPage <= 4) {\n    return [1, 2, 3, 4, 5, 'ellipsis', totalPages];\n  }\n  if (currentPage >= totalPages - 3) {\n    return [\n      1,\n      'ellipsis',\n      totalPages - 4,\n      totalPages - 3,\n      totalPages - 2,\n      totalPages - 1,\n      totalPages,\n    ];\n  }\n  return [\n    1,\n    'ellipsis',\n    currentPage - 1,\n    currentPage,\n    currentPage + 1,\n    'ellipsis',\n    totalPages,\n  ];\n}\n\n// ── Props extension for React callbacks ─────────────────────────────────────\n\nexport interface BTPaginationReactProps extends BTPaginationProps {\n  onPageChange?: (page: number) => void;\n  onRowsChange?: (rows: number) => void;\n  onPrev?: () => void;\n  onNext?: () => void;\n}\n\n// ── Component ────────────────────────────────────────────────────────────────\n\nexport function BTPagination({\n  variant = 'number',\n  currentPage = 1,\n  totalPages = 1,\n  rowsPerPage = 10,\n  rowsPerPageOptions = [10, 20, 50],\n  currentStep = 1,\n  totalSteps = 1,\n  ariaLabel = 'Pagination',\n  onPageChange,\n  onRowsChange,\n  onPrev,\n  onNext,\n}: BTPaginationReactProps) {\n  const isFirstPage = currentPage <= 1;\n  const isLastPage  = currentPage >= totalPages;\n\n  // Detect RTL context — in RTL we swap the \"Previous\"/\"Next\" labels so the\n  // visible button text matches reading direction (Next on left, Previous on\n  // right) while the icons/layout stay LTR.\n  // NOTE: Read direction from the PARENT, not from the nav itself, because the\n  // pagination's own CSS forces `direction: ltr` to keep the visual layout.\n  const navRef = useRef<HTMLElement>(null);\n  const [isRtl, setIsRtl] = useState(false);\n  useEffect(() => {\n    if (typeof window === 'undefined' || !navRef.current?.parentElement) return;\n    setIsRtl(getComputedStyle(navRef.current.parentElement).direction === 'rtl');\n  }, []);\n\n  const labelLeft  = isRtl ? 'Next'     : 'Previous';\n  const labelRight = isRtl ? 'Previous' : 'Next';\n\n  function goTo(page: number) {\n    if (page < 1 || page > totalPages || page === currentPage) return;\n    onPageChange?.(page);\n  }\n\n  const visiblePages = buildVisiblePages(currentPage, totalPages);\n\n  const rowsItems: BTDropdownItem<number>[] = rowsPerPageOptions.map((opt) => ({\n    value: opt,\n    label: String(opt),\n    checked: opt === rowsPerPage,\n  }));\n\n  return (\n    <nav\n      ref={navRef}\n      className={`bt-pagination bt-pagination--${variant}`}\n      role=\"navigation\"\n      aria-label={ariaLabel}\n    >\n      {/* ── NUMBER variant ─────────────────────────────────────────────── */}\n      {variant === 'number' && (\n        <>\n          {/* Left nav */}\n          <div className=\"bt-pagination__nav\">\n            <BTButton variant=\"ghost\" size=\"small\" iconOnly disabled={isFirstPage} aria-label=\"First page\" onClick={() => goTo(1)}>\n              <span className=\"bt-pagination__icon\"><IconFirstPage /></span>\n            </BTButton>\n            <BTButton\n              variant=\"ghost\"\n              size=\"small\"\n              label={labelLeft}\n              disabled={isFirstPage}\n              aria-label=\"Previous page\"\n              onClick={() => goTo(currentPage - 1)}\n              leftIcon={<span className=\"bt-pagination__icon\"><IconChevronLeft /></span>}\n            />\n          </div>\n\n          {/* Center pages — BTStepNumber pills */}\n          <div className=\"bt-pagination__pages\" role=\"list\">\n            {visiblePages.map((page, idx) => (\n              <BTStepNumber\n                key={page === 'ellipsis' ? `ellipsis-${idx}` : page}\n                page={page === 'ellipsis' ? 0 : (page as number)}\n                isActive={page !== 'ellipsis' && page === currentPage}\n                isEllipsis={page === 'ellipsis'}\n                onClick={page !== 'ellipsis' ? () => goTo(page as number) : undefined}\n              />\n            ))}\n          </div>\n\n          {/* Right nav */}\n          <div className=\"bt-pagination__nav\">\n            <BTButton\n              variant=\"ghost\"\n              size=\"small\"\n              label={labelRight}\n              disabled={isLastPage}\n              aria-label=\"Next page\"\n              onClick={() => goTo(currentPage + 1)}\n              rightIcon={<span className=\"bt-pagination__icon\"><IconChevronRight /></span>}\n            />\n            <BTButton variant=\"ghost\" size=\"small\" iconOnly disabled={isLastPage} aria-label=\"Last page\" onClick={() => goTo(totalPages)}>\n              <span className=\"bt-pagination__icon\"><IconLastPage /></span>\n            </BTButton>\n          </div>\n        </>\n      )}\n\n      {/* ── DISPLAY variant ──────────────────────────────────────────────── */}\n      {variant === 'display' && (\n        <>\n          <div className=\"bt-pagination__display-left\">\n            <span className=\"bt-pagination__rows-label\">Rows per page</span>\n            <BTDropdown<number>\n              items={rowsItems}\n              value={rowsPerPage}\n              onSelect={(v) => onRowsChange?.(v)}\n              trigger={(toggle, isOpen) => (\n                <button\n                  className={`bt-pagination__rows-trigger${isOpen ? ' bt-pagination__rows-trigger--open' : ''}`}\n                  aria-label={`Rows per page, currently ${rowsPerPage}`}\n                  aria-expanded={isOpen}\n                  onClick={toggle}\n                >\n                  {rowsPerPage}\n                  <span className=\"bt-pagination__icon\">\n                    <IconChevronDown open={isOpen} />\n                  </span>\n                </button>\n              )}\n            />\n          </div>\n\n          <div className=\"bt-pagination__nav\">\n            <BTButton\n              variant=\"ghost\"\n              size=\"small\"\n              label={labelLeft}\n              disabled={isFirstPage}\n              aria-label=\"Previous page\"\n              onClick={() => goTo(currentPage - 1)}\n              leftIcon={<span className=\"bt-pagination__icon\"><IconChevronLeft /></span>}\n            />\n            <BTButton\n              variant=\"ghost\"\n              size=\"small\"\n              label={labelRight}\n              disabled={isLastPage}\n              aria-label=\"Next page\"\n              onClick={() => goTo(currentPage + 1)}\n              rightIcon={<span className=\"bt-pagination__icon\"><IconChevronRight /></span>}\n            />\n          </div>\n        </>\n      )}\n\n      {/* ── STEP variant ─────────────────────────────────────────────────── */}\n      {variant === 'step' && (\n        <>\n          <span className=\"bt-pagination__step-label\">\n            Step {currentStep} of {totalSteps}\n          </span>\n\n          {/* Prev / Next — secondary-light, 16px gap */}\n          <div className=\"bt-pagination__nav bt-pagination__nav--step\">\n            <BTButton\n              variant=\"secondary-light\"\n              label={isRtl ? 'Next' : 'Prev'}\n              disabled={currentStep <= 1}\n              aria-label=\"Previous step\"\n              onClick={onPrev}\n            />\n            <BTButton\n              variant=\"secondary-light\"\n              label={isRtl ? 'Prev' : 'Next'}\n              disabled={currentStep >= totalSteps}\n              aria-label=\"Next step\"\n              onClick={onNext}\n            />\n          </div>\n        </>\n      )}\n    </nav>\n  );\n}\n\nBTPagination.displayName = 'BTPagination';\n"
    },
    {
      "path": "BTPagination.types.ts",
      "target": "@ui/pagination/BTPagination.types.ts",
      "type": "registry:ui",
      "content": "/**\n * BTPagination — data pagination molecule.\n * Figma: node 749-2846.\n *\n * Three variants:\n * - 'number'  Full numbered paging with first/prev/pages/next/last.\n * - 'display' Rows-per-page selector + prev/next.\n * - 'step'    Step N of M + Prev/Next.\n */\nexport type BTPaginationVariant = 'display' | 'step' | 'number';\n\nexport interface BTPaginationProps {\n  /** Which pagination pattern to render. @default 'number' */\n  variant?: BTPaginationVariant;\n\n  // ── number + display ─────────────────────────────────────────────────────\n  /** Current active page (1-indexed). */\n  currentPage?: number;\n  /** Total number of pages. Required for 'number' variant. */\n  totalPages?: number;\n\n  // ── display ──────────────────────────────────────────────────────────────\n  /** Total item count — used in range label display. */\n  totalCount?: number;\n  /** Items shown per page. @default 10 */\n  rowsPerPage?: number;\n  /** Rows-per-page dropdown options. @default [10, 20, 50] */\n  rowsPerPageOptions?: number[];\n\n  // ── step ─────────────────────────────────────────────────────────────────\n  /** Current step (1-indexed). Required for 'step' variant. */\n  currentStep?: number;\n  /** Total steps. Required for 'step' variant. */\n  totalSteps?: number;\n\n  /** Accessible label for the nav wrapper. @default 'Pagination' */\n  ariaLabel?: string;\n}\n"
    },
    {
      "path": "index.ts",
      "target": "@ui/pagination/index.ts",
      "type": "registry:ui",
      "content": "export { BTPagination } from './BTPagination';\nexport type { BTPaginationReactProps } from './BTPagination';\nexport type { BTPaginationProps, BTPaginationVariant } from './BTPagination.types';\n"
    }
  ]
}