{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "data-table",
  "type": "registry:ui",
  "title": "BTDataTable",
  "description": "Data table organism with toolbar, filter tags, table title, rows, fixed-column variants, horizontal scroll indicator, and display pagination.",
  "framework": "react",
  "category": "organisms",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/Btech---Design-System?node-id=2373-11585",
  "figmaNodeId": "2373-11585",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [
    "badge",
    "button",
    "button-link",
    "checkbox",
    "hint",
    "input",
    "input-dropdown",
    "input-multiple-field",
    "input-search",
    "pagination",
    "radio-button",
    "scrollbar",
    "switch-field",
    "tag",
    "tree"
  ],
  "files": [
    {
      "path": "BTDataTable.css",
      "target": "@ui/data-table/BTDataTable.css",
      "type": "registry:ui",
      "content": ".bt-data-table {\r\n  display: flex;\r\n  flex-direction: column;\r\n  gap: var(--space-sm);\r\n  width: 100%;\r\n  color: var(--text-primary);\r\n  font-family: \"Geist\", system-ui, sans-serif;\r\n  font-size: var(--typography-font-size-sm);\r\n  line-height: var(--typography-line-height-xs);\r\n}\r\n\r\n.bt-data-table__top {\r\n  display: flex;\r\n  align-items: center;\r\n  justify-content: space-between;\r\n  gap: var(--space-md);\r\n}\r\n\r\n.bt-data-table__title {\r\n  color: var(--text-primary);\r\n  font-size: var(--typography-font-size-sm);\r\n  font-weight: 500;\r\n  line-height: var(--typography-line-height-xs);\r\n}\r\n\r\n.bt-data-table__toolbar {\r\n  display: flex;\r\n  align-items: center;\r\n  justify-content: flex-end;\r\n  gap: var(--space-sm);\r\n  min-width: 0;\r\n}\r\n\r\n.bt-data-table__search {\r\n  width: 180px;\r\n  /* Contain the BTInput clear-button slide animation (translateX 6px)\r\n     so it cannot overflow into adjacent toolbar buttons */\r\n  overflow: hidden;\r\n  border-radius: var(--radius-sm);\r\n}\r\n\r\n.bt-data-table__toolbar-icon {\r\n  display: inline-flex;\r\n  width: 16px;\r\n  height: 16px;\r\n  color: currentColor;\r\n}\r\n\r\n.bt-data-table__filters {\r\n  display: flex;\r\n  align-items: center;\r\n  gap: var(--space-xs);\r\n  min-height: 24px;\r\n  min-width: 0; /* allow inner __filters-scroll to shrink + scroll */\r\n}\r\n\r\n/* Horizontally scrollable strip of filter tags.\r\n   flex: 0 1 auto → hug content when it fits, shrink + scroll when it overflows.\r\n   This keeps \"Remove All\" right next to the last tag with the same gap. */\r\n.bt-data-table__filters-scroll {\r\n  display: flex;\r\n  align-items: center;\r\n  gap: var(--space-xs);\r\n  min-width: 0;\r\n  flex: 0 1 auto;\r\n  overflow-x: auto;\r\n  overflow-y: hidden;\r\n  scrollbar-width: thin;\r\n}\r\n\r\n.bt-data-table__filters-scroll::-webkit-scrollbar {\r\n  height: 4px;\r\n}\r\n\r\n.bt-data-table__filters-scroll::-webkit-scrollbar-thumb {\r\n  background: var(--bg-secondary);\r\n  border-radius: 2px;\r\n}\r\n\r\n.bt-data-table__filters-scroll > * {\r\n  flex-shrink: 0;\r\n}\r\n\r\n/* .bt-data-table__filters-action replaced by BTButtonLink */\r\n\r\n.bt-table-title__sortable {\r\n  cursor: pointer;\r\n  user-select: none;\r\n}\r\n\r\n.bt-table-title__sortable:hover {\r\n  background: var(--bg-secondary);\r\n}\r\n\r\n.bt-data-table__viewport {\r\n  width: 100%;\r\n  overflow-x: auto;\r\n  border: 1px solid var(--border-primary);\r\n  border-radius: var(--radius-sm);\r\n  background: var(--bg-primary);\r\n  --fixed-left-shadow-color: transparent;\r\n  --fixed-right-shadow-color: transparent;\r\n  /* Direction-aware shadow cast directions (used by sticky fixed cells).\r\n     LTR: shadow casts toward inline-end = +X; inline-start = -X.\r\n     RTL: physical axes flip -> +X becomes inline-start, -X becomes inline-end. */\r\n  --bt-dt-shadow-cast-inline-end: 4px 0;\r\n  --bt-dt-shadow-cast-inline-start: -4px 0;\r\n}\r\n\r\n[dir=\"rtl\"] .bt-data-table__viewport {\r\n  --bt-dt-shadow-cast-inline-end: -4px 0;\r\n  --bt-dt-shadow-cast-inline-start: 4px 0;\r\n}\r\n\r\n.bt-data-table__viewport--scrolled-left {\r\n  --fixed-left-shadow-color: rgba(0, 0, 0, 0.12);\r\n}\r\n\r\n.bt-data-table__viewport--scrolled-right {\r\n  --fixed-right-shadow-color: rgba(0, 0, 0, 0.12);\r\n}\r\n\r\n.bt-data-table__table {\r\n  width: 100%;\r\n  border-collapse: separate;\r\n  border-spacing: 0;\r\n  table-layout: auto;\r\n}\r\n\r\n.bt-table-title {\r\n  background: var(--bg-secondary);\r\n}\r\n\r\n.bt-data-table--fixed-header .bt-table-title {\r\n  position: sticky;\r\n  top: 0;\r\n  z-index: 4;\r\n}\r\n\r\n.bt-table-title--secondary {\r\n  background: var(--color-brand-primary-subtle);\r\n}\r\n\r\n.bt-table-title--no-fill {\r\n  background: var(--bg-primary);\r\n}\r\n\r\n/* Push background onto <tr> so sticky <th> can inherit it (thead bg doesn't propagate to <tr>).\r\n   --row-bg is also set so sticky cells can use var(--row-bg) instead of fragile `inherit`. */\r\n.bt-table-title tr {\r\n  background: var(--bg-secondary);\r\n  --row-bg: var(--bg-secondary);\r\n}\r\n\r\n.bt-table-title--secondary tr {\r\n  background: var(--color-brand-primary-subtle);\r\n  --row-bg: var(--color-brand-primary-subtle);\r\n}\r\n\r\n.bt-table-title--no-fill tr {\r\n  background: var(--bg-primary);\r\n  --row-bg: var(--bg-primary);\r\n}\r\n\r\n.bt-table-row {\r\n  background: var(--bg-primary);\r\n  --row-bg: var(--bg-primary);\r\n}\r\n\r\n.bt-table-row--no-fill {\r\n  background: transparent;\r\n  --row-bg: transparent;\r\n}\r\n\r\n.bt-table-row:hover {\r\n  background: var(--bg-secondary);\r\n  --row-bg: var(--bg-secondary);\r\n}\r\n\r\n/* ── Tree row children cell — no padding/border so it sits flush ── */\r\n.bt-table-children-row > .bt-table-children-cell {\r\n  padding: 0;\r\n  border-inline-start: 0;\r\n  border-bottom: 0;\r\n}\r\n\r\n/* ── Tree expand animation ── */\r\n@keyframes bt-tree-expand-in {\r\n  from {\r\n    opacity: 0;\r\n    transform: translateY(-6px);\r\n  }\r\n  to {\r\n    opacity: 1;\r\n    transform: translateY(0);\r\n  }\r\n}\r\n.bt-table-children-row {\r\n  animation: bt-tree-expand-in 180ms ease forwards;\r\n}\r\n\r\n.bt-table-row--selected {\r\n  background: var(--color-brand-primary-subtle);\r\n  --row-bg: var(--color-brand-primary-subtle);\r\n}\r\n\r\n.bt-table-row--selected:hover {\r\n  background: var(--color-brand-primary-subtle);\r\n  --row-bg: var(--color-brand-primary-subtle);\r\n}\r\n\r\n.bt-table-item {\r\n  height: 32px;\r\n  padding: 8px;\r\n  border-inline-start: 1px solid var(--border-primary);\r\n  border-bottom: 1px solid var(--border-primary);\r\n  color: var(--text-primary);\r\n  font-size: var(--typography-font-size-sm);\r\n  font-weight: 500;\r\n  line-height: var(--typography-line-height-xs);\r\n  text-align: start;\r\n  vertical-align: middle;\r\n}\r\n\r\n/* Column alignment — CSS variable set inline on <td>/<th>, all inner\r\n   flex containers reference it so every cell type is covered automatically. */\r\n.bt-table-item {\r\n  --col-halign: flex-start;\r\n  text-align: start;\r\n}\r\n\r\n.bt-table-item:first-child {\r\n  border-inline-start: 0;\r\n}\r\n\r\n/* Remove bottom border on last visible row to avoid double line with viewport border.\r\n   Two cases:\r\n   1. Flat table  — last <tr> is a bt-table-row directly\r\n   2. Tree table  — last <tr> is bt-table-children-row; the preceding bt-table-row is the last data row */\r\ntbody tr.bt-table-row:last-child .bt-table-item,\r\ntbody\r\n  tr.bt-table-row:has(+ tr.bt-table-children-row:last-child)\r\n  .bt-table-item {\r\n  border-bottom: 0;\r\n}\r\n\r\n.bt-table-item--header {\r\n  color: var(--text-primary);\r\n  font-size: var(--typography-font-size-xs);\r\n  font-weight: 600;\r\n  text-transform: uppercase;\r\n}\r\n\r\n.bt-table-title--no-fill .bt-table-item--header {\r\n  color: var(--text-secondary);\r\n}\r\n\r\n.bt-table-item--no-fill {\r\n  background: transparent;\r\n}\r\n\r\n.bt-table-item--tree {\r\n  height: auto;\r\n  padding: 0;\r\n  vertical-align: top;\r\n}\r\n\r\n.bt-table-item__content {\r\n  display: flex;\r\n  align-items: center;\r\n  min-width: 0;\r\n  gap: var(--space-xs);\r\n  justify-content: var(--col-halign, flex-start);\r\n}\r\n\r\n.bt-table-item__text {\r\n  min-width: 0;\r\n  overflow: hidden;\r\n  text-overflow: ellipsis;\r\n  white-space: nowrap;\r\n}\r\n\r\n.bt-table-item__header-label {\r\n  flex: 1 1 auto;\r\n  min-width: 0;\r\n  overflow: hidden;\r\n  text-overflow: ellipsis;\r\n  white-space: nowrap;\r\n}\r\n\r\n.bt-table-item__sort {\r\n  display: inline-flex;\r\n  width: 16px;\r\n  height: 16px;\r\n  color: var(--icon-tertiary);\r\n  flex: 0 0 auto;\r\n}\r\n\r\n.bt-table-item__actions {\r\n  display: inline-flex;\r\n  gap: var(--space-2xs);\r\n  align-items: center;\r\n  justify-content: var(--col-halign, flex-start);\r\n  width: 100%;\r\n}\r\n\r\n.bt-table-item__control {\r\n  display: flex;\r\n  width: 100%;\r\n  align-items: center;\r\n  justify-content: var(--col-halign, center);\r\n}\r\n\r\n.bt-table-item__option-group {\r\n  display: flex;\r\n  align-items: center;\r\n  gap: var(--space-md);\r\n  min-width: 0;\r\n  justify-content: var(--col-halign, flex-start);\r\n}\r\n\r\n.bt-table-item__option {\r\n  display: inline-flex;\r\n  align-items: center;\r\n  gap: var(--space-xs);\r\n  min-width: 0;\r\n}\r\n\r\n.bt-table-item__field {\r\n  width: 100%;\r\n  min-width: 112px;\r\n}\r\n\r\n.bt-table-item__tree {\r\n  display: block;\r\n  min-width: 0;\r\n}\r\n\r\n.bt-table-item__tree .bt-tree {\r\n  min-width: 180px;\r\n}\r\n\r\n.bt-table-item__tree .bt-tree-item {\r\n  min-height: 24px;\r\n  padding-block: 2px;\r\n}\r\n\r\n.bt-table-item__tree .bt-tree-node__children {\r\n  display: none;\r\n}\r\n\r\n.bt-table-item__field .bt-input,\r\n.bt-table-item__field .bt-input-dropdown,\r\n.bt-table-item__field .bt-input-multiple-field {\r\n  min-height: 32px;\r\n}\r\n\r\n.bt-table-item__badge {\r\n  display: inline-flex;\r\n  align-items: center;\r\n  justify-content: center;\r\n  border-radius: var(--radius-rd);\r\n  padding: var(--space-xs) var(--space-sm);\r\n  background: var(--color-ext-success-subtler);\r\n  color: var(--color-ext-success-bold);\r\n  font-size: var(--typography-font-size-xs);\r\n  font-weight: 500;\r\n  line-height: var(--typography-line-height-xs);\r\n  white-space: nowrap;\r\n}\r\n\r\n.bt-data-table__fixed-cell {\r\n  position: sticky;\r\n  z-index: 2;\r\n  background: var(--row-bg, var(--bg-primary));\r\n  /* Checkbox column — remove horizontal padding, lock width, always center */\r\n  padding-left: 0;\r\n  padding-right: 0;\r\n  width: 32px;\r\n  min-width: 32px;\r\n  max-width: 32px;\r\n  --col-halign: center;\r\n}\r\n\r\n.bt-data-table__fixed-cell--left {\r\n  inset-inline-start: 0;\r\n  border-inline-end: 2px solid var(--border-primary);\r\n}\r\n\r\n.bt-data-table__fixed-cell--right {\r\n  inset-inline-end: 0;\r\n  border-inline-start: 2px solid var(--border-primary);\r\n}\r\n\r\n.bt-table-title .bt-data-table__fixed-cell {\r\n  z-index: 3;\r\n}\r\n\r\n/* Hide native horizontal scrollbar on viewport — replaced by external BTScrollbar */\r\n.bt-data-table__viewport {\r\n  scrollbar-width: none; /* Firefox */\r\n}\r\n.bt-data-table__viewport::-webkit-scrollbar {\r\n  display: none; /* Chrome / Safari / Edge */\r\n}\r\n\r\n/* External scrollbar spacer */\r\n.bt-data-table__scrollbar-outer {\r\n  margin-top: 8px;\r\n}\r\n\r\n.bt-data-table__pagination {\r\n  display: flex;\r\n  justify-content: stretch;\r\n  width: 100%;\r\n}\r\n\r\n/* ── Tree row depth colors (Figma: TableRow states Level 1–5) ─────────── */\r\n.bt-table-row--depth-1 {\r\n  background: var(--color-l-grey-50);\r\n  --row-bg: var(--color-l-grey-50);\r\n}\r\n.bt-table-row--depth-2 {\r\n  background: var(--color-l-grey-100);\r\n  --row-bg: var(--color-l-grey-100);\r\n}\r\n.bt-table-row--depth-3 {\r\n  background: var(--color-l-grey-150);\r\n  --row-bg: var(--color-l-grey-150);\r\n}\r\n.bt-table-row--depth-4 {\r\n  background: var(--color-l-grey-200);\r\n  --row-bg: var(--color-l-grey-200);\r\n}\r\n.bt-table-row--depth-5 {\r\n  background: var(--color-l-grey-300);\r\n  --row-bg: var(--color-l-grey-300);\r\n}\r\n\r\n/* Selected overrides depth background */\r\n.bt-table-row--depth-1.bt-table-row--selected,\r\n.bt-table-row--depth-2.bt-table-row--selected,\r\n.bt-table-row--depth-3.bt-table-row--selected,\r\n.bt-table-row--depth-4.bt-table-row--selected,\r\n.bt-table-row--depth-5.bt-table-row--selected {\r\n  background: var(--color-brand-primary-subtle);\r\n  --row-bg: var(--color-brand-primary-subtle);\r\n}\r\n\r\n.bt-table-row--depth-1:hover,\r\n.bt-table-row--depth-2:hover,\r\n.bt-table-row--depth-3:hover,\r\n.bt-table-row--depth-4:hover,\r\n.bt-table-row--depth-5:hover {\r\n  background: var(--bg-secondary);\r\n  --row-bg: var(--bg-secondary);\r\n}\r\n\r\n/* ── Expander column cell layout ──────────────────────────────────────── */\r\n.bt-table-item__expander-cell {\r\n  display: flex;\r\n  align-items: center;\r\n  gap: 4px;\r\n  min-width: 0;\r\n}\r\n\r\n/* expander <td> uses internal indent — override inline padding */\r\n.bt-table-item:has(.bt-table-item__expander-cell) {\r\n  padding-inline: 0;\r\n}\r\n\r\n.bt-table-item__expander-indent {\r\n  flex-shrink: 0;\r\n}\r\n\r\n.bt-table-item__expander-btn {\r\n  display: inline-flex;\r\n  align-items: center;\r\n  justify-content: center;\r\n  width: 20px;\r\n  height: 20px;\r\n  flex-shrink: 0;\r\n  border-radius: var(--radius-xs);\r\n  color: var(--icon-secondary);\r\n  background: transparent;\r\n  border: none;\r\n  padding: 0;\r\n  cursor: pointer;\r\n  transition:\r\n    transform 0.15s ease,\r\n    color 0.1s;\r\n}\r\n\r\n.bt-table-item__expander-btn:hover {\r\n  color: var(--icon-primary);\r\n}\r\n\r\n.bt-table-item__expander-btn--expanded {\r\n  transform: rotate(90deg);\r\n}\r\n\r\n\r\n@media (max-width: 720px) {\r\n  .bt-data-table__top {\r\n    align-items: flex-start;\r\n    flex-direction: column;\r\n  }\r\n\r\n  .bt-data-table__toolbar {\r\n    width: 100%;\r\n    justify-content: flex-start;\r\n    flex-wrap: wrap;\r\n  }\r\n\r\n  .bt-data-table__search {\r\n    width: min(100%, 240px);\r\n  }\r\n}\r\n"
    },
    {
      "path": "BTDataTable.tsx",
      "target": "@ui/data-table/BTDataTable.tsx",
      "type": "registry:ui",
      "content": "import './BTDataTable.css';\r\nimport { useCallback, useEffect, useRef, useState } from 'react';\r\nimport { BTButton } from '../button/BTButton';\r\nimport { BTButtonLink } from '../button-link/BTButtonLink';\r\nimport { BTHint } from '../hint/BTHint';\r\nimport { BTInputSearch } from '../input-search/BTInputSearch';\r\nimport { BTPagination } from '../pagination/BTPagination';\r\nimport { BTScrollbar } from '../scrollbar/BTScrollbar';\r\nimport { BTTag } from '../tag/BTTag';\r\nimport { BTTableRow } from './BTTableRow';\r\nimport { BTTableTitle } from './BTTableTitle';\r\nimport type {\r\n  BTDataTableCell,\r\n  BTDataTableColumn,\r\n  BTDataTableProps,\r\n  BTDataTableRow,\r\n  BTTableActionPayload,\r\n} from './BTDataTable.types';\r\n\r\nconst DEFAULT_COLUMNS: BTDataTableColumn[] = Array.from({ length: 10 }, (_, index) => ({\r\n  key: `col${index + 1}`,\r\n  label: 'LABEL',\r\n  sortable: true,\r\n}));\r\n\r\nconst DEFAULT_ROWS: BTDataTableRow[] = Array.from({ length: 10 }, (_, rowIndex) => ({\r\n  id: `row-${rowIndex + 1}`,\r\n  cells: DEFAULT_COLUMNS.reduce<Record<string, BTDataTableCell>>((acc, column) => {\r\n    acc[column.key] = { type: 'text', value: 'Subtext' };\r\n    return acc;\r\n  }, {}),\r\n}));\r\n\r\nfunction titleVariant(variant: string) {\r\n  if (variant === 'secondary') return 'secondary' as const;\r\n  if (variant === 'no-fill') return 'no-fill' as const;\r\n  return 'default' as const;\r\n}\r\n\r\n/** Find a row anywhere in the tree by id. */\r\nfunction findRow(rows: BTDataTableRow[], id: string): BTDataTableRow | null {\r\n  for (const row of rows) {\r\n    if (row.id === id) return row;\r\n    if (row.children?.length) {\r\n      const found = findRow(row.children, id);\r\n      if (found) return found;\r\n    }\r\n  }\r\n  return null;\r\n}\r\n\r\nfunction getAllRowIds(rows: BTDataTableRow[]): string[] {\r\n  const ids: string[] = [];\r\n  for (const row of rows) {\r\n    ids.push(row.id);\r\n    if (row.children?.length) ids.push(...getAllRowIds(row.children));\r\n  }\r\n  return ids;\r\n}\r\n\r\nfunction countAllRows(rows: BTDataTableRow[]): number {\r\n  return rows.reduce((n, r) => n + 1 + countAllRows(r.children ?? []), 0);\r\n}\r\n\r\nfunction rowCheckState(row: BTDataTableRow, sel: string[]): 'checked' | 'indeterminate' | 'unchecked' {\r\n  if (!row.children?.length) return sel.includes(row.id) ? 'checked' : 'unchecked';\r\n  const states = row.children.map((c) => rowCheckState(c, sel));\r\n  if (states.every((s) => s === 'checked')) return 'checked';\r\n  if (states.every((s) => s === 'unchecked')) return 'unchecked';\r\n  return 'indeterminate';\r\n}\r\n\r\nfunction computeHeaderCheckState(rows: BTDataTableRow[], sel: string[]): 'checked' | 'indeterminate' | 'unchecked' {\r\n  if (rows.length === 0) return 'unchecked';\r\n  const states = rows.map((r) => rowCheckState(r, sel));\r\n  if (states.every((s) => s === 'checked')) return 'checked';\r\n  if (states.every((s) => s === 'unchecked')) return 'unchecked';\r\n  return 'indeterminate';\r\n}\r\n\r\n\r\nfunction FilterIcon() {\r\n  return (\r\n    <svg viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\">\r\n      <path d=\"M2 3H14L9.33 8.33V13L6.67 14V8.33L2 3Z\" fill=\"currentColor\" />\r\n    </svg>\r\n  );\r\n}\r\n\r\nexport function BTDataTable({\r\n  title = 'Table Title',\r\n  columns = DEFAULT_COLUMNS,\r\n  rows = DEFAULT_ROWS,\r\n  variant = 'default',\r\n  showToolbar = true,\r\n  showFilters = true,\r\n  showPagination = true,\r\n  showSearch = true,\r\n  showFilter = true,\r\n  searchValue = '',\r\n  rowsPerPage = 10,\r\n  rowsPerPageOptions = [10, 20, 50],\r\n  currentPage = 1,\r\n  totalPages = 5,\r\n  filters = ['Filter Active', 'Filter Active', 'Filter Active', 'Filter Active', 'Filter Active'],\r\n  maxVisibleFilters = 4,\r\n  expandedKeys = {},\r\n  maxHeight,\r\n  fixedHeader = false,\r\n  hasCheckbox = false,\r\n  fixedLeft = 0,\r\n  fixedRight = 0,\r\n  selectedRows = [],\r\n  onSearchValueChange,\r\n  onSearchSubmit,\r\n  onPageChange,\r\n  onRowsChange,\r\n  onAction,\r\n  onCellChange,\r\n  onRemoveFilter,\r\n  onRemoveAllFilters,\r\n  onExpandedKeysChange,\r\n  onSort,\r\n  onSelectedRowsChange,\r\n  toolbarActions,\r\n}: BTDataTableProps) {\r\n  const totalRowCount = countAllRows(rows);\r\n\r\n  const viewportRef  = useRef<HTMLDivElement>(null);\r\n  const scrollbarRef = useRef<HTMLDivElement>(null);\r\n  const syncingFromViewport  = useRef(false);\r\n  const syncingFromScrollbar = useRef(false);\r\n\r\n  const [scrolledLeft,  setScrolledLeft]  = useState(false);\r\n  const [scrolledRight, setScrolledRight] = useState(false);\r\n  const [scrollWidth,   setScrollWidth]   = useState(0);\r\n  const [clientWidth,   setClientWidth]   = useState(0);\r\n\r\n  const updateScrollState = useCallback(() => {\r\n    const el = viewportRef.current;\r\n    if (!el) return;\r\n    // In RTL, scrollLeft is negative in modern browsers when scrolling toward\r\n    // inline-end. Math.abs normalizes for both directions so the shadow tracking\r\n    // works the same way regardless of writing direction.\r\n    const sl = Math.abs(el.scrollLeft);\r\n    setScrolledLeft(sl > 0);\r\n    setScrolledRight(sl < el.scrollWidth - el.clientWidth - 1);\r\n    setScrollWidth(el.scrollWidth);\r\n    setClientWidth(el.clientWidth);\r\n  }, []);\r\n\r\n  const handleViewportScroll = useCallback(() => {\r\n    updateScrollState();\r\n    if (syncingFromScrollbar.current) return;\r\n    const vp = viewportRef.current;\r\n    const sb = scrollbarRef.current;\r\n    if (!vp || !sb) return;\r\n    syncingFromViewport.current = true;\r\n    sb.scrollLeft = vp.scrollLeft;\r\n    syncingFromViewport.current = false;\r\n  }, [updateScrollState]);\r\n\r\n  const handleScrollbarScroll = useCallback(() => {\r\n    if (syncingFromViewport.current) return;\r\n    const vp = viewportRef.current;\r\n    const sb = scrollbarRef.current;\r\n    if (!vp || !sb) return;\r\n    syncingFromScrollbar.current = true;\r\n    vp.scrollLeft = sb.scrollLeft;\r\n    syncingFromScrollbar.current = false;\r\n  }, []);\r\n\r\n  useEffect(() => {\r\n    updateScrollState();\r\n    const el = viewportRef.current;\r\n    if (!el) return;\r\n    el.addEventListener('scroll', handleViewportScroll, { passive: true });\r\n    const ro = new ResizeObserver(updateScrollState);\r\n    ro.observe(el);\r\n    return () => {\r\n      el.removeEventListener('scroll', handleViewportScroll);\r\n      ro.disconnect();\r\n    };\r\n  }, [updateScrollState, handleViewportScroll]);\r\n\r\n  function handleActionInternal(payload: BTTableActionPayload) {\r\n    if (payload.action === 'check-all') {\r\n      const allIds = getAllRowIds(rows);\r\n      const allSelected = allIds.every((id) => selectedRows.includes(id));\r\n      onSelectedRowsChange?.(allSelected ? [] : allIds);\r\n    } else if (payload.action === 'check-row' && payload.rowId) {\r\n      const target = findRow(rows, payload.rowId);\r\n      // Collect this row + all its descendants\r\n      const affectedIds = target ? getAllRowIds([target]) : [payload.rowId];\r\n      const allAffectedSelected = affectedIds.every((id) => selectedRows.includes(id));\r\n      let next: string[];\r\n      if (allAffectedSelected) {\r\n        // All selected → uncheck all\r\n        next = selectedRows.filter((id) => !affectedIds.includes(id));\r\n      } else {\r\n        // Some or none selected → check all\r\n        const toAdd = affectedIds.filter((id) => !selectedRows.includes(id));\r\n        next = [...selectedRows, ...toAdd];\r\n      }\r\n      onSelectedRowsChange?.(next);\r\n    } else {\r\n      onAction?.(payload);\r\n    }\r\n  }\r\n\r\n  return (\r\n    <section\r\n      className={[\r\n        'bt-data-table',\r\n        `bt-data-table--${variant}`,\r\n        fixedHeader && 'bt-data-table--fixed-header',\r\n      ].filter(Boolean).join(' ')}\r\n      data-testid=\"bt-data-table\"\r\n    >\r\n      {/* Header layout (Figma 2373-11585): single row.\r\n          Title (left) · Search + Filter + toolbar actions (right). Filter tags\r\n          live in their own row directly below (.bt-data-table__filters). */}\r\n      <div className=\"bt-data-table__top\">\r\n        <h3 className=\"bt-data-table__title\">{title}</h3>\r\n        {showToolbar && (\r\n          <div className=\"bt-data-table__toolbar\">\r\n            {showSearch && (\r\n              <BTInputSearch\r\n                className=\"bt-data-table__search\"\r\n                value={searchValue}\r\n                placeholder=\"Search\"\r\n                onChange={onSearchValueChange}\r\n                onSearch={(v) => onSearchSubmit?.(v)}\r\n                onClear={() => { onSearchValueChange?.(''); onSearchSubmit?.(''); }}\r\n              />\r\n            )}\r\n            {showFilter && (\r\n              <BTButton\r\n                variant=\"secondary-light\"\r\n                iconOnly\r\n                aria-label=\"Filter\"\r\n                rightIcon={filters.length > 0 ? <BTHint count={filters.length} size=\"sm\" /> : undefined}\r\n                onClick={() => onAction?.({ action: 'filter' })}\r\n              >\r\n                <FilterIcon />\r\n              </BTButton>\r\n            )}\r\n            {toolbarActions}\r\n          </div>\r\n        )}\r\n      </div>\r\n\r\n      {showFilters && filters.length > 0 && (\r\n        <div className=\"bt-data-table__filters\">\r\n          {/* Horizontally scrollable strip of filter tags. No \"+N\" overflow —\r\n              native scroll handles long lists. \"Remove All\" sits outside the\r\n              scroll area so it's always reachable. */}\r\n          <div className=\"bt-data-table__filters-scroll\">\r\n            {filters.map((filter, index) => (\r\n              <BTTag key={`${filter}-${index}`} label={filter} onRemove={() => onRemoveFilter?.(filter)} />\r\n            ))}\r\n          </div>\r\n          <BTButtonLink label=\"Remove All\" onClick={onRemoveAllFilters} />\r\n        </div>\r\n      )}\r\n\r\n      <div\r\n        ref={viewportRef}\r\n        className={[\r\n          'bt-data-table__viewport',\r\n          scrolledLeft  && 'bt-data-table__viewport--scrolled-left',\r\n          scrolledRight && 'bt-data-table__viewport--scrolled-right',\r\n        ].filter(Boolean).join(' ')}\r\n        style={maxHeight ? { maxHeight, overflowY: 'auto' } : undefined}\r\n        onScroll={handleViewportScroll}\r\n      >\r\n        <table className=\"bt-data-table__table\">\r\n          <colgroup>\r\n            {hasCheckbox && <col style={{ width: 32 }} />}\r\n            {columns.map((column) => (\r\n              <col\r\n                key={column.key}\r\n                style={column.fill || !column.width ? undefined : { width: column.width }}\r\n              />\r\n            ))}\r\n          </colgroup>\r\n          <BTTableTitle\r\n            columns={columns}\r\n            variant={titleVariant(variant)}\r\n            hasCheckbox={hasCheckbox}\r\n            fixedLeft={fixedLeft}\r\n            fixedRight={fixedRight}\r\n            selectedRows={selectedRows}\r\n            totalRowCount={totalRowCount}\r\n            headerAllSelected={computeHeaderCheckState(rows, selectedRows) === 'checked'}\r\n            headerSomeSelected={computeHeaderCheckState(rows, selectedRows) === 'indeterminate'}\r\n            onAction={handleActionInternal}\r\n            onSort={onSort}\r\n          />\r\n          <tbody>\r\n            {rows.map((row) => (\r\n              <BTTableRow\r\n                key={row.id}\r\n                row={row}\r\n                columns={columns}\r\n                noFill={variant === 'no-fill'}\r\n                hasCheckbox={hasCheckbox}\r\n                fixedLeft={fixedLeft}\r\n                fixedRight={fixedRight}\r\n                selectedRows={selectedRows}\r\n                totalRowCount={totalRowCount}\r\n                depth={0}\r\n                expandedKeys={expandedKeys}\r\n                onAction={handleActionInternal}\r\n                onCellChange={onCellChange}\r\n                onExpandedKeysChange={onExpandedKeysChange}\r\n              />\r\n            ))}\r\n          </tbody>\r\n        </table>\r\n      </div>\r\n\r\n      {scrollWidth > clientWidth && (\r\n        <BTScrollbar\r\n          ref={scrollbarRef}\r\n          direction=\"x\"\r\n          className=\"bt-data-table__scrollbar-outer\"\r\n          onScroll={handleScrollbarScroll}\r\n        >\r\n          <div style={{ width: scrollWidth, height: 1 }} aria-hidden=\"true\" />\r\n        </BTScrollbar>\r\n      )}\r\n\r\n      {showPagination && (\r\n        <div className=\"bt-data-table__pagination\">\r\n          <BTPagination\r\n            variant=\"display\"\r\n            currentPage={currentPage}\r\n            totalPages={totalPages}\r\n            rowsPerPage={rowsPerPage}\r\n            rowsPerPageOptions={rowsPerPageOptions}\r\n            onPageChange={onPageChange}\r\n            onRowsChange={onRowsChange}\r\n          />\r\n        </div>\r\n      )}\r\n    </section>\r\n  );\r\n}\r\n\r\nBTDataTable.displayName = 'BTDataTable';\r\n"
    },
    {
      "path": "BTDataTable.types.ts",
      "target": "@ui/data-table/BTDataTable.types.ts",
      "type": "registry:ui",
      "content": "import type { CSSProperties, MouseEventHandler, ReactNode } from 'react';\r\nimport type { BTTreeNode } from '../tree/BTTree.types';\r\n\r\nexport type BTTableCellType =\r\n  | 'tree'\r\n  | 'text'\r\n  | 'button'\r\n  | 'double-button'\r\n  | 'checkbox'\r\n  | 'radio'\r\n  | 'option'\r\n  | 'field'\r\n  | 'dropdown'\r\n  | 'multiple-selection'\r\n  | 'badge'\r\n  | 'toggle'\r\n  | 'title'\r\n  | 'checkbox-title'\r\n  | 'title-dropdown'\r\n  | 'number';\r\n\r\nexport type BTDataTableVariant =\r\n  | 'default'\r\n  | 'secondary'\r\n  | 'no-fill';\r\n\r\nexport type BTDataTableSortDirection = 'asc' | 'desc' | 'none';\r\n\r\nexport interface BTDataTableOption {\r\n  label: string;\r\n  value: string;\r\n}\r\n\r\nexport interface BTDataTableTag {\r\n  id?: string;\r\n  label: string;\r\n}\r\n\r\nexport interface BTDataTableCell {\r\n  type?: BTTableCellType;\r\n  value?: string | number | boolean | null;\r\n  label?: string;\r\n  badgeLabel?: string;\r\n  options?: BTDataTableOption[];\r\n  tags?: BTDataTableTag[];\r\n  treeNodes?: BTTreeNode[];\r\n  checked?: boolean;\r\n  selected?: boolean;\r\n  disabled?: boolean;\r\n}\r\n\r\nexport interface BTDataTableColumn {\r\n  key: string;\r\n  label: string;\r\n  /**\r\n   * Fixed pixel/rem width for this column, e.g. `\"120px\"`.\r\n   * Omit (or use `fill: true`) to let the column fill remaining space.\r\n   */\r\n  width?: string;\r\n  /**\r\n   * When true the column fills available space proportionally with other\r\n   * fill columns — equivalent to leaving `width` unset.\r\n   * Useful for making the intent explicit in column definitions.\r\n   */\r\n  fill?: boolean;\r\n  sortable?: boolean;\r\n  sortDirection?: BTDataTableSortDirection;\r\n  headerType?: Extract<BTTableCellType, 'title' | 'checkbox-title' | 'title-dropdown'>;\r\n  /** Mark this column as the expander — renders indent + chevron for tree rows */\r\n  expander?: boolean;\r\n  /**\r\n   * Horizontal alignment of cell content and header label.\r\n   * @default 'left'\r\n   */\r\n  align?: 'left' | 'center' | 'right';\r\n}\r\n\r\nexport interface BTDataTableRow {\r\n  id: string;\r\n  cells: Record<string, BTDataTableCell | string | number | boolean | null | undefined>;\r\n  /** Nested child rows — presence triggers tree behaviour */\r\n  children?: BTDataTableRow[];\r\n}\r\n\r\nexport interface BTTableActionPayload {\r\n  action: string;\r\n  rowId?: string;\r\n  columnKey?: string;\r\n}\r\n\r\nexport interface BTDataTableCellChangePayload {\r\n  rowId: string;\r\n  columnKey: string;\r\n  cell: BTDataTableCell;\r\n}\r\n\r\nexport interface BTTableItemProps {\r\n  type?: BTTableCellType;\r\n  cell?: BTDataTableCell | string | number | boolean | null;\r\n  label?: string;\r\n  header?: boolean;\r\n  as?: 'div' | 'td' | 'th';\r\n  columnKey?: string;\r\n  rowId?: string;\r\n  noFill?: boolean;\r\n  className?: string;\r\n  style?: CSSProperties;\r\n  onClick?: (() => void) | MouseEventHandler<HTMLElement>;\r\n  onAction?: (payload: BTTableActionPayload) => void;\r\n  onCellChange?: (payload: BTDataTableCellChangePayload) => void;\r\n}\r\n\r\nexport interface BTTableRowProps {\r\n  row: BTDataTableRow;\r\n  columns: BTDataTableColumn[];\r\n  noFill?: boolean;\r\n  /** Nesting depth — 0 = root, 1 = first child level, etc. */\r\n  depth?: number;\r\n  expandedKeys?: Record<string, boolean>;\r\n  hasCheckbox?: boolean;\r\n  fixedLeft?: number;\r\n  fixedRight?: number;\r\n  selectedRows?: string[];\r\n  totalRowCount?: number;\r\n  onAction?: (payload: BTTableActionPayload) => void;\r\n  onCellChange?: (payload: BTDataTableCellChangePayload) => void;\r\n  onExpandedKeysChange?: (keys: Record<string, boolean>) => void;\r\n}\r\n\r\nexport interface BTSortPayload {\r\n  columnKey: string;\r\n  direction: BTDataTableSortDirection;\r\n}\r\n\r\nexport interface BTTableTitleProps {\r\n  columns: BTDataTableColumn[];\r\n  variant?: BTDataTableVariant;\r\n  hasCheckbox?: boolean;\r\n  fixedLeft?: number;\r\n  fixedRight?: number;\r\n  selectedRows?: string[];\r\n  totalRowCount?: number;\r\n  /** Pre-computed from recursive row check state — overrides length-based allSelected */\r\n  headerAllSelected?: boolean;\r\n  /** Pre-computed from recursive row check state — overrides length-based someSelected */\r\n  headerSomeSelected?: boolean;\r\n  onAction?: (payload: BTTableActionPayload) => void;\r\n  onSort?: (payload: BTSortPayload) => void;\r\n}\r\n\r\nexport interface BTDataTableProps {\r\n  title?: string;\r\n  columns?: BTDataTableColumn[];\r\n  rows?: BTDataTableRow[];\r\n  variant?: BTDataTableVariant;\r\n  showToolbar?: boolean;\r\n  showFilters?: boolean;\r\n  showPagination?: boolean;\r\n  showSearch?: boolean;\r\n  showFilter?: boolean;\r\n  searchValue?: string;\r\n  rowsPerPage?: number;\r\n  rowsPerPageOptions?: number[];\r\n  currentPage?: number;\r\n  totalPages?: number;\r\n  filters?: string[];\r\n  /**\r\n   * Max number of filter tags shown before collapsing into a \"+N\" badge.\r\n   * @default 4\r\n   */\r\n  maxVisibleFilters?: number;\r\n  /** Controlled expansion state — keys are row IDs, values are true=expanded */\r\n  expandedKeys?: Record<string, boolean>;\r\n  /**\r\n   * Max height of the table viewport. When set, rows scroll vertically inside\r\n   * the fixed-height container instead of growing the page.\r\n   * Accepts any valid CSS length, e.g. `\"480px\"` or `\"60vh\"`.\r\n   * Pass `null` or omit for auto height (no restriction).\r\n   */\r\n  maxHeight?: string | null;\r\n  /** Keeps the table header pinned while the viewport scrolls vertically. */\r\n  fixedHeader?: boolean;\r\n  /** Prepends a checkbox column (width 48px) as column 0 */\r\n  hasCheckbox?: boolean;\r\n  /** Number of columns to fix from left (checkbox counts if hasCheckbox) */\r\n  fixedLeft?: number;\r\n  /** Number of columns to fix from right */\r\n  fixedRight?: number;\r\n  /** Controlled array of selected row IDs */\r\n  selectedRows?: string[];\r\n  onSearchValueChange?: (value: string) => void;\r\n  /** Called when the user presses Enter in the search input — use for submit-based search */\r\n  onSearchSubmit?: (value: string) => void;\r\n  onPageChange?: (page: number) => void;\r\n  onRowsChange?: (rows: number) => void;\r\n  onAction?: (payload: BTTableActionPayload) => void;\r\n  onCellChange?: (payload: BTDataTableCellChangePayload) => void;\r\n  onRemoveFilter?: (filter: string) => void;\r\n  onRemoveAllFilters?: () => void;\r\n  onExpandedKeysChange?: (keys: Record<string, boolean>) => void;\r\n  onSort?: (payload: BTSortPayload) => void;\r\n  onSelectedRowsChange?: (ids: string[]) => void;\r\n  /** Custom actions rendered to the right of the filter button in the toolbar */\r\n  toolbarActions?: ReactNode;\r\n}\r\n"
    },
    {
      "path": "BTTableItem.tsx",
      "target": "@ui/data-table/BTTableItem.tsx",
      "type": "registry:ui",
      "content": "import './BTDataTable.css';\r\nimport { BTButton } from '../button/BTButton';\r\nimport { BTCheckbox } from '../checkbox/BTCheckbox';\r\nimport { BTRadioButton } from '../radio-button/BTRadioButton';\r\nimport { BTSwitchField } from '../switch-field/BTSwitchField';\r\nimport { BTInput } from '../input/BTInput';\r\nimport { BTInputDropdown } from '../input-dropdown/BTInputDropdown';\r\nimport { BTInputMultipleField } from '../input-multiple-field/BTInputMultipleField';\r\nimport { BTTree } from '../tree/BTTree';\r\nimport type { BTDataTableCell, BTDataTableOption, BTTableItemProps } from './BTDataTable.types';\r\n\r\nfunction cx(...parts: Array<string | false | null | undefined>): string {\r\n  return parts.filter(Boolean).join(' ');\r\n}\r\n\r\nfunction normalizeCell(cell: BTTableItemProps['cell'], type?: BTTableItemProps['type']): BTDataTableCell {\r\n  if (cell != null && typeof cell === 'object') return cell as BTDataTableCell;\r\n  return { type: type ?? 'text', value: cell as string | number | boolean | null };\r\n}\r\n\r\nfunction SortIcon() {\r\n  return (\r\n    <svg viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\">\r\n      <path d=\"M5 6L8 3L11 6H5ZM11 10L8 13L5 10H11Z\" fill=\"currentColor\" />\r\n    </svg>\r\n  );\r\n}\r\n\r\nexport function BTTableItem({\r\n  type,\r\n  cell = null,\r\n  label,\r\n  header = false,\r\n  as = 'div',\r\n  columnKey,\r\n  rowId,\r\n  noFill = false,\r\n  className,\r\n  style,\r\n  onClick,\r\n  onAction,\r\n  onCellChange,\r\n}: BTTableItemProps) {\r\n  const normalizedCell = normalizeCell(cell, type);\r\n  const cellType = type ?? normalizedCell.type ?? 'text';\r\n  const raw = label ?? normalizedCell.label ?? normalizedCell.value;\r\n  const textValue = raw == null ? '' : String(raw);\r\n  const Component = as;\r\n  const options: BTDataTableOption[] = normalizedCell.options ?? [\r\n    { label: 'Label', value: 'label-1' },\r\n    { label: 'Label', value: 'label-2' },\r\n  ];\r\n  const action = (name: string) => onAction?.({ action: name, rowId, columnKey });\r\n  const cellChange = (patch: Partial<BTDataTableCell>) => {\r\n    if (!rowId || !columnKey) return;\r\n    onCellChange?.({\r\n      rowId,\r\n      columnKey,\r\n      cell: {\r\n        ...normalizedCell,\r\n        type: cellType,\r\n        ...patch,\r\n      },\r\n    });\r\n  };\r\n\r\n  return (\r\n    <Component\r\n      className={cx(\r\n        'bt-table-item',\r\n        header && 'bt-table-item--header',\r\n        noFill && 'bt-table-item--no-fill',\r\n        `bt-table-item--${cellType}`,\r\n        className,\r\n      )}\r\n      style={style}\r\n      onClick={onClick}\r\n      {...(as === 'th' ? { scope: 'col' } : {})}\r\n    >\r\n      {cellType === 'tree' && (\r\n        <span className=\"bt-table-item__tree\">\r\n          <BTTree\r\n            nodes={normalizedCell.treeNodes ?? [\r\n              {\r\n                label: textValue || 'Subtext',\r\n                initiallyExpanded: true,\r\n                children: [{ label: 'Level 2' }, { label: 'Level 3' }],\r\n              },\r\n            ]}\r\n          />\r\n        </span>\r\n      )}\r\n\r\n      {(cellType === 'title' || cellType === 'checkbox-title') && (\r\n        <span className=\"bt-table-item__content\">\r\n          {cellType === 'checkbox-title' && (\r\n            <BTCheckbox checked={!!normalizedCell.checked} onChange={() => action('check-title')} />\r\n          )}\r\n          <span className=\"bt-table-item__header-label\">{textValue || 'LABEL'}</span>\r\n          <span className=\"bt-table-item__sort\"><SortIcon /></span>\r\n        </span>\r\n      )}\r\n\r\n      {cellType === 'button' && (\r\n        <span className=\"bt-table-item__actions\">\r\n          <BTButton size=\"small\" label={textValue || 'Label'} onClick={() => action('button')} />\r\n        </span>\r\n      )}\r\n\r\n      {cellType === 'double-button' && (\r\n        <span className=\"bt-table-item__actions\">\r\n          <BTButton size=\"small\" iconOnly aria-label=\"Primary action\" onClick={() => action('primary')}>\r\n            <svg viewBox=\"0 0 16 16\" fill=\"none\"><path d=\"M3 8.67V12.5H6.83L12.13 7.2L8.3 3.37L3 8.67ZM13.03 6.3L13.75 5.58C14.08 5.25 14.08 4.73 13.75 4.4L11.6 2.25C11.27 1.92 10.75 1.92 10.42 2.25L9.7 2.97L13.03 6.3Z\" fill=\"currentColor\" /></svg>\r\n          </BTButton>\r\n          <BTButton size=\"small\" variant=\"secondary-light\" iconOnly aria-label=\"Secondary action\" onClick={() => action('secondary')}>\r\n            <svg viewBox=\"0 0 16 16\" fill=\"none\"><path d=\"M3.5 4.5H12.5V5.83H3.5V4.5ZM3.5 7.33H12.5V8.67H3.5V7.33ZM3.5 10.17H12.5V11.5H3.5V10.17Z\" fill=\"currentColor\" /></svg>\r\n          </BTButton>\r\n        </span>\r\n      )}\r\n\r\n      {cellType === 'checkbox' && (\r\n        <span className=\"bt-table-item__control\">\r\n          <BTCheckbox checked={normalizedCell.checked ?? true} disabled={normalizedCell.disabled} onChange={(checked) => cellChange({ checked })} />\r\n        </span>\r\n      )}\r\n\r\n      {cellType === 'radio' && (\r\n        <span className=\"bt-table-item__control\">\r\n          <BTRadioButton modelValue={normalizedCell.selected ? 'on' : 'off'} value=\"on\" disabled={normalizedCell.disabled} onChange={(value) => cellChange({ selected: value === 'on' })} />\r\n        </span>\r\n      )}\r\n\r\n      {cellType === 'option' && (\r\n        <span className=\"bt-table-item__option-group\">\r\n          {options.map((option) => (\r\n            <span key={option.value} className=\"bt-table-item__option\">\r\n              <BTRadioButton modelValue={String(normalizedCell.value ?? '')} value={option.value} onChange={(value) => cellChange({ value, label: option.label })} />\r\n              <span className=\"bt-table-item__text\">{option.label}</span>\r\n            </span>\r\n          ))}\r\n        </span>\r\n      )}\r\n\r\n      {cellType === 'field' && (\r\n        <span className=\"bt-table-item__field\">\r\n          <BTInput value={textValue} label=\"Title\" onChange={(value) => cellChange({ value })} />\r\n        </span>\r\n      )}\r\n\r\n      {(cellType === 'dropdown' || cellType === 'title-dropdown') && (\r\n        <span className=\"bt-table-item__field\">\r\n          <BTInputDropdown\r\n            value={String(normalizedCell.value ?? options[0]?.value ?? '')}\r\n            label=\"Title\"\r\n            items={options}\r\n            onChange={(value) => cellChange({\r\n              value: value ?? '',\r\n              label: options.find((option) => option.value === value)?.label,\r\n            })}\r\n          />\r\n        </span>\r\n      )}\r\n\r\n      {cellType === 'multiple-selection' && (\r\n        <span className=\"bt-table-item__field\">\r\n          <BTInputMultipleField\r\n            value={normalizedCell.tags ?? [{ id: 'tag-1', label: 'Tag' }, { id: 'tag-2', label: 'Tag' }]}\r\n            label=\"Title\"\r\n            type=\"scroll\"\r\n            onChange={(tags) => cellChange({ tags })}\r\n          />\r\n        </span>\r\n      )}\r\n\r\n      {cellType === 'badge' && (\r\n        <span className=\"bt-table-item__badge\">{(normalizedCell.badgeLabel ?? textValue) || 'Badge'}</span>\r\n      )}\r\n\r\n      {cellType === 'toggle' && (\r\n        <span className=\"bt-table-item__control\">\r\n          <BTSwitchField checked={!!normalizedCell.checked} size=\"sm\" onChange={(checked) => cellChange({ checked })} />\r\n        </span>\r\n      )}\r\n\r\n      {(cellType === 'text' || cellType === 'number') && (\r\n        <span className=\"bt-table-item__content\">\r\n          <span className=\"bt-table-item__text\">{textValue || (cellType === 'number' ? '9999' : 'Subtext')}</span>\r\n        </span>\r\n      )}\r\n    </Component>\r\n  );\r\n}\r\n\r\nBTTableItem.displayName = 'BTTableItem';\r\n"
    },
    {
      "path": "BTTableRow.tsx",
      "target": "@ui/data-table/BTTableRow.tsx",
      "type": "registry:ui",
      "content": "import \"./BTDataTable.css\";\r\nimport { BTTableItem } from \"./BTTableItem\";\r\nimport { BTCheckbox } from \"../../atoms/Checkbox/BTCheckbox\";\r\nimport type {\r\n  BTDataTableCell,\r\n  BTDataTableColumn,\r\n  BTDataTableRow,\r\n  BTTableRowProps,\r\n} from \"./BTDataTable.types\";\r\n\r\nconst CHECKBOX_WIDTH = 32;\r\n\r\nfunction cx(...parts: Array<string | false | null | undefined>): string {\r\n  return parts.filter(Boolean).join(\" \");\r\n}\r\n\r\n/**\r\n * Recursive tri-state check for a row.\r\n * Leaf  → checked only if its own ID is in selectedRows.\r\n * Parent → derived from direct children's states (recursive), never from\r\n *          whether the parent's own ID is in selectedRows — this keeps\r\n *          check-by-children consistent with check-by-parent.\r\n */\r\nfunction rowCheckState(\r\n  row: BTDataTableRow,\r\n  selectedRows: string[],\r\n): \"checked\" | \"indeterminate\" | \"unchecked\" {\r\n  if (!row.children?.length) {\r\n    return selectedRows.includes(row.id) ? \"checked\" : \"unchecked\";\r\n  }\r\n  const childStates = row.children.map((c) => rowCheckState(c, selectedRows));\r\n  if (childStates.every((s) => s === \"checked\")) return \"checked\";\r\n  if (childStates.every((s) => s === \"unchecked\")) return \"unchecked\";\r\n  return \"indeterminate\";\r\n}\r\n\r\nfunction colBaseStyle(col: BTDataTableColumn): React.CSSProperties {\r\n  return col.fill || !col.width\r\n    ? { minWidth: \"148px\" }\r\n    : { width: col.width, minWidth: col.width, maxWidth: col.width };\r\n}\r\n\r\nconst HALIGN_MAP = {\r\n  left: \"flex-start\",\r\n  center: \"center\",\r\n  right: \"flex-end\",\r\n} as const;\r\n\r\nfunction colAlignStyle(col: BTDataTableColumn): React.CSSProperties {\r\n  if (!col.align || col.align === \"left\") return {};\r\n  return {\r\n    \"--col-halign\": HALIGN_MAP[col.align],\r\n    textAlign: col.align,\r\n  } as React.CSSProperties;\r\n}\r\n\r\nfunction getColWidth(col: BTDataTableColumn | undefined): number {\r\n  if (!col || !col.width) return 148;\r\n  return parseInt(col.width);\r\n}\r\n\r\nfunction stickyStyle(\r\n  effectiveIndex: number,\r\n  columns: BTDataTableColumn[],\r\n  hasCheckbox: boolean,\r\n  fixedLeft: number,\r\n  fixedRight: number,\r\n): React.CSSProperties | undefined {\r\n  const totalCols = (hasCheckbox ? 1 : 0) + columns.length;\r\n\r\n  if (fixedLeft > 0 && effectiveIndex < fixedLeft) {\r\n    let left = 0;\r\n    for (let i = 0; i < effectiveIndex; i++) {\r\n      if (i === 0 && hasCheckbox) left += CHECKBOX_WIDTH;\r\n      else {\r\n        const colIdx = hasCheckbox ? i - 1 : i;\r\n        left += getColWidth(columns[colIdx]);\r\n      }\r\n    }\r\n    const isLast = effectiveIndex === fixedLeft - 1;\r\n    return {\r\n      position: \"sticky\",\r\n      insetInlineStart: `${left}px`,\r\n      zIndex: 2,\r\n      background: \"var(--row-bg, var(--bg-primary))\",\r\n      boxShadow: isLast\r\n        ? \"var(--bt-dt-shadow-cast-inline-end, 4px 0) 6px -2px var(--fixed-left-shadow-color, transparent)\"\r\n        : undefined,\r\n      transition: isLast ? \"box-shadow 0.2s ease\" : undefined,\r\n    };\r\n  }\r\n\r\n  if (fixedRight > 0 && effectiveIndex >= totalCols - fixedRight) {\r\n    let right = 0;\r\n    for (let i = effectiveIndex + 1; i < totalCols; i++) {\r\n      if (i === 0 && hasCheckbox) right += CHECKBOX_WIDTH;\r\n      else {\r\n        const colIdx = hasCheckbox ? i - 1 : i;\r\n        right += getColWidth(columns[colIdx]);\r\n      }\r\n    }\r\n    const isFirst = effectiveIndex === totalCols - fixedRight;\r\n    return {\r\n      position: \"sticky\",\r\n      insetInlineEnd: `${right}px`,\r\n      zIndex: 2,\r\n      background: \"var(--row-bg, var(--bg-primary))\",\r\n      boxShadow: isFirst\r\n        ? \"var(--bt-dt-shadow-cast-inline-start, -4px 0) 6px -2px var(--fixed-right-shadow-color, transparent)\"\r\n        : undefined,\r\n      transition: isFirst ? \"box-shadow 0.2s ease\" : undefined,\r\n    };\r\n  }\r\n\r\n  return undefined;\r\n}\r\n\r\nconst CHEVRON = (\r\n  <svg\r\n    viewBox=\"0 0 16 16\"\r\n    fill=\"none\"\r\n    width=\"12\"\r\n    height=\"12\"\r\n    aria-hidden=\"true\"\r\n  >\r\n    <path\r\n      d=\"M6 4L10 8L6 12\"\r\n      stroke=\"currentColor\"\r\n      strokeWidth=\"1.5\"\r\n      strokeLinecap=\"round\"\r\n      strokeLinejoin=\"round\"\r\n    />\r\n  </svg>\r\n);\r\n\r\nexport function BTTableRow({\r\n  row,\r\n  columns,\r\n  noFill = false,\r\n  depth = 0,\r\n  expandedKeys = {},\r\n  hasCheckbox = false,\r\n  fixedLeft = 0,\r\n  fixedRight = 0,\r\n  selectedRows = [],\r\n  onAction,\r\n  onCellChange,\r\n  onExpandedKeysChange,\r\n}: BTTableRowProps) {\r\n  const hasChildren = (row.children?.length ?? 0) > 0;\r\n  const isExpanded = !!expandedKeys[row.id];\r\n  const clampedDepth = Math.min(5, depth);\r\n  const indentWidth = `${depth * 24}px`;\r\n  const levelClass = depth > 0 ? `bt-table-row--depth-${clampedDepth}` : null;\r\n  const checkState = rowCheckState(row, selectedRows);\r\n  // Parent row is \"selected\" (gets highlight bg) when all its children are checked.\r\n  // Leaf row is selected when its own ID is in selectedRows.\r\n  const isRowSelected = hasChildren\r\n    ? checkState === \"checked\"\r\n    : selectedRows.includes(row.id);\r\n  const totalCols = (hasCheckbox ? 1 : 0) + columns.length;\r\n\r\n  function toggleExpand(e: React.MouseEvent) {\r\n    e.stopPropagation();\r\n    onExpandedKeysChange?.({ ...expandedKeys, [row.id]: !isExpanded });\r\n  }\r\n\r\n  const checkboxIsLast = fixedLeft <= 1;\r\n  const checkboxStickyStyle: React.CSSProperties = {\r\n    position: \"sticky\",\r\n    insetInlineStart: 0,\r\n    zIndex: 2,\r\n    background: \"var(--row-bg, var(--bg-primary))\",\r\n    boxShadow: checkboxIsLast\r\n      ? \"var(--bt-dt-shadow-cast-inline-end, 4px 0) 6px -2px var(--fixed-left-shadow-color, transparent)\"\r\n      : undefined,\r\n    transition: checkboxIsLast ? \"box-shadow 0.2s ease\" : undefined,\r\n  };\r\n\r\n  return (\r\n    <>\r\n      <tr\r\n        className={cx(\r\n          \"bt-table-row\",\r\n          levelClass,\r\n          noFill && \"bt-table-row--no-fill\",\r\n          isRowSelected && \"bt-table-row--selected\",\r\n        )}\r\n      >\r\n        {/* Checkbox cell */}\r\n        {hasCheckbox && (\r\n          <td\r\n            className=\"bt-table-item bt-data-table__fixed-cell\"\r\n            style={{ ...checkboxStickyStyle }}\r\n          >\r\n            <span className=\"bt-table-item__control\">\r\n              <BTCheckbox\r\n                checked={checkState === \"checked\"}\r\n                indeterminate={checkState === \"indeterminate\"}\r\n                onChange={() =>\r\n                  onAction?.({ action: \"check-row\", rowId: row.id })\r\n                }\r\n              />\r\n            </span>\r\n          </td>\r\n        )}\r\n\r\n        {columns.map((column, index) => {\r\n          const effectiveIndex = hasCheckbox ? index + 1 : index;\r\n          const fixedStyle = stickyStyle(\r\n            effectiveIndex,\r\n            columns,\r\n            hasCheckbox,\r\n            fixedLeft,\r\n            fixedRight,\r\n          );\r\n          const isExpanderCol = !!column.expander;\r\n\r\n          if (isExpanderCol) {\r\n            const rawCell = row.cells[column.key];\r\n            const textValue =\r\n              rawCell != null &&\r\n              typeof rawCell === \"object\" &&\r\n              \"value\" in rawCell\r\n                ? String((rawCell as BTDataTableCell).value ?? \"\")\r\n                : String(rawCell ?? \"\");\r\n\r\n            const baseStyle = colBaseStyle(column);\r\n\r\n            return (\r\n              <td\r\n                key={column.key}\r\n                className=\"bt-table-item\"\r\n                style={{\r\n                  ...baseStyle,\r\n                  ...colAlignStyle(column),\r\n                  ...fixedStyle,\r\n                }}\r\n              >\r\n                <span className=\"bt-table-item__expander-cell\">\r\n                  <span\r\n                    className=\"bt-table-item__expander-indent\"\r\n                    style={{ width: indentWidth }}\r\n                  />\r\n                  {hasChildren && (\r\n                    <button\r\n                      type=\"button\"\r\n                      className={cx(\r\n                        \"bt-table-item__expander-btn\",\r\n                        isExpanded && \"bt-table-item__expander-btn--expanded\",\r\n                      )}\r\n                      aria-expanded={isExpanded}\r\n                      aria-label={isExpanded ? \"Collapse row\" : \"Expand row\"}\r\n                      onClick={toggleExpand}\r\n                    >\r\n                      {CHEVRON}\r\n                    </button>\r\n                  )}\r\n                  <span className=\"bt-table-item__text\">\r\n                    {textValue || \"Subtext\"}\r\n                  </span>\r\n                </span>\r\n              </td>\r\n            );\r\n          }\r\n\r\n          const baseStyle = colBaseStyle(column);\r\n\r\n          return (\r\n            <BTTableItem\r\n              key={column.key}\r\n              as=\"td\"\r\n              cell={row.cells[column.key]}\r\n              columnKey={column.key}\r\n              rowId={row.id}\r\n              noFill={noFill}\r\n              style={{ ...baseStyle, ...colAlignStyle(column), ...fixedStyle }}\r\n              onAction={onAction}\r\n              onCellChange={onCellChange}\r\n            />\r\n          );\r\n        })}\r\n      </tr>\r\n\r\n      {/* Children as flat <tr> siblings — same tbody, same colgroup → columns stay aligned */}\r\n      {hasChildren && isExpanded && row.children!.map((child) => (\r\n        <BTTableRow\r\n          key={child.id}\r\n          row={child}\r\n          columns={columns}\r\n          noFill={noFill}\r\n          hasCheckbox={hasCheckbox}\r\n          fixedLeft={fixedLeft}\r\n          fixedRight={fixedRight}\r\n          selectedRows={selectedRows}\r\n          depth={depth + 1}\r\n          expandedKeys={expandedKeys}\r\n          onAction={onAction}\r\n          onCellChange={onCellChange}\r\n          onExpandedKeysChange={onExpandedKeysChange}\r\n        />\r\n      ))}\r\n    </>\r\n  );\r\n}\r\n\r\nBTTableRow.displayName = \"BTTableRow\";\r\n"
    },
    {
      "path": "BTTableTitle.tsx",
      "target": "@ui/data-table/BTTableTitle.tsx",
      "type": "registry:ui",
      "content": "import \"./BTDataTable.css\";\r\nimport { BTTableItem } from \"./BTTableItem\";\r\nimport { BTCheckbox } from \"../../atoms/Checkbox/BTCheckbox\";\r\nimport type {\r\n  BTDataTableColumn,\r\n  BTDataTableSortDirection,\r\n  BTSortPayload,\r\n  BTTableTitleProps,\r\n} from \"./BTDataTable.types\";\r\n\r\nconst CHECKBOX_WIDTH = 32;\r\n\r\nfunction cx(...parts: Array<string | false | null | undefined>): string {\r\n  return parts.filter(Boolean).join(\" \");\r\n}\r\n\r\nfunction colBaseStyle(col: BTDataTableColumn): React.CSSProperties {\r\n  return col.fill || !col.width\r\n    ? { minWidth: \"148px\" }\r\n    : { width: col.width, minWidth: col.width, maxWidth: col.width };\r\n}\r\n\r\nconst HALIGN_MAP = {\r\n  left: \"flex-start\",\r\n  center: \"center\",\r\n  right: \"flex-end\",\r\n} as const;\r\n\r\nfunction colAlignStyle(col: BTDataTableColumn): React.CSSProperties {\r\n  if (!col.align || col.align === \"left\") return {};\r\n  return {\r\n    \"--col-halign\": HALIGN_MAP[col.align],\r\n    textAlign: col.align,\r\n  } as React.CSSProperties;\r\n}\r\n\r\nfunction nextDirection(\r\n  current: BTDataTableSortDirection | undefined,\r\n): BTDataTableSortDirection {\r\n  if (!current || current === \"none\") return \"asc\";\r\n  if (current === \"asc\") return \"desc\";\r\n  return \"none\";\r\n}\r\n\r\nfunction getColWidth(col: BTDataTableColumn | undefined): number {\r\n  if (!col || !col.width) return 148;\r\n  return parseInt(col.width);\r\n}\r\n\r\nfunction stickyStyle(\r\n  effectiveIndex: number,\r\n  columns: BTDataTableColumn[],\r\n  hasCheckbox: boolean,\r\n  fixedLeft: number,\r\n  fixedRight: number,\r\n): React.CSSProperties | undefined {\r\n  const totalCols = (hasCheckbox ? 1 : 0) + columns.length;\r\n\r\n  if (fixedLeft > 0 && effectiveIndex < fixedLeft) {\r\n    let left = 0;\r\n    for (let i = 0; i < effectiveIndex; i++) {\r\n      if (i === 0 && hasCheckbox) left += CHECKBOX_WIDTH;\r\n      else {\r\n        const colIdx = hasCheckbox ? i - 1 : i;\r\n        left += getColWidth(columns[colIdx]);\r\n      }\r\n    }\r\n    const isLast = effectiveIndex === fixedLeft - 1;\r\n    return {\r\n      position: \"sticky\",\r\n      insetInlineStart: `${left}px`,\r\n      zIndex: 3,\r\n      background: \"var(--row-bg, var(--bg-primary))\",\r\n      boxShadow: isLast\r\n        ? \"var(--bt-dt-shadow-cast-inline-end, 4px 0) 6px -2px var(--fixed-left-shadow-color, transparent)\"\r\n        : undefined,\r\n      transition: isLast ? \"box-shadow 0.2s ease\" : undefined,\r\n    };\r\n  }\r\n\r\n  if (fixedRight > 0 && effectiveIndex >= totalCols - fixedRight) {\r\n    let right = 0;\r\n    for (let i = effectiveIndex + 1; i < totalCols; i++) {\r\n      if (i === 0 && hasCheckbox) right += CHECKBOX_WIDTH;\r\n      else {\r\n        const colIdx = hasCheckbox ? i - 1 : i;\r\n        right += getColWidth(columns[colIdx]);\r\n      }\r\n    }\r\n    const isFirst = effectiveIndex === totalCols - fixedRight;\r\n    return {\r\n      position: \"sticky\",\r\n      insetInlineEnd: `${right}px`,\r\n      zIndex: 3,\r\n      background: \"var(--row-bg, var(--bg-primary))\",\r\n      boxShadow: isFirst\r\n        ? \"var(--bt-dt-shadow-cast-inline-start, -4px 0) 6px -2px var(--fixed-right-shadow-color, transparent)\"\r\n        : undefined,\r\n      transition: isFirst ? \"box-shadow 0.2s ease\" : undefined,\r\n    };\r\n  }\r\n\r\n  return undefined;\r\n}\r\n\r\nfunction SortIcon({ direction }: { direction?: BTDataTableSortDirection }) {\r\n  const upOpacity = direction === \"desc\" ? 0.3 : 1;\r\n  const downOpacity = direction === \"asc\" ? 0.3 : 1;\r\n  return (\r\n    <svg viewBox=\"0 0 16 16\" fill=\"none\" aria-hidden=\"true\">\r\n      <path d=\"M5 6L8 3L11 6H5Z\" fill=\"currentColor\" fillOpacity={upOpacity} />\r\n      <path\r\n        d=\"M11 10L8 13L5 10H11Z\"\r\n        fill=\"currentColor\"\r\n        fillOpacity={downOpacity}\r\n      />\r\n    </svg>\r\n  );\r\n}\r\n\r\nfunction TitleCell({\r\n  column,\r\n  fixedStyle,\r\n  onSort,\r\n}: {\r\n  column: BTDataTableColumn;\r\n  fixedStyle?: React.CSSProperties;\r\n  onSort?: (p: BTSortPayload) => void;\r\n}) {\r\n  function handleClick() {\r\n    if (!column.sortable || !onSort) return;\r\n    onSort({\r\n      columnKey: column.key,\r\n      direction: nextDirection(column.sortDirection),\r\n    });\r\n  }\r\n\r\n  const baseStyle = colBaseStyle(column);\r\n\r\n  return (\r\n    <th\r\n      className={cx(\r\n        \"bt-table-item\",\r\n        \"bt-table-item--header\",\r\n        \"bt-table-item--title\",\r\n        column.sortable && \"bt-table-title__sortable\",\r\n      )}\r\n      scope=\"col\"\r\n      style={{ ...baseStyle, ...colAlignStyle(column), ...fixedStyle }}\r\n      onClick={column.sortable ? handleClick : undefined}\r\n    >\r\n      <span className=\"bt-table-item__content\">\r\n        <span className=\"bt-table-item__header-label\">{column.label}</span>\r\n        <span className=\"bt-table-item__sort\">\r\n          <SortIcon direction={column.sortDirection} />\r\n        </span>\r\n      </span>\r\n    </th>\r\n  );\r\n}\r\n\r\nexport function BTTableTitle({\r\n  columns,\r\n  variant = \"default\",\r\n  hasCheckbox = false,\r\n  fixedLeft = 0,\r\n  fixedRight = 0,\r\n  selectedRows = [],\r\n  totalRowCount = 0,\r\n  headerAllSelected,\r\n  headerSomeSelected,\r\n  onAction,\r\n  onSort,\r\n}: BTTableTitleProps) {\r\n  const allSelected =\r\n    headerAllSelected ??\r\n    (totalRowCount > 0 && selectedRows.length >= totalRowCount);\r\n  const someSelected =\r\n    headerSomeSelected ?? (selectedRows.length > 0 && !allSelected);\r\n\r\n  const checkboxIsLast = fixedLeft <= 1;\r\n  const checkboxStickyStyle: React.CSSProperties = {\r\n    position: \"sticky\",\r\n    insetInlineStart: 0,\r\n    zIndex: 3,\r\n    background: \"var(--row-bg, var(--bg-primary))\",\r\n    boxShadow: checkboxIsLast\r\n      ? \"var(--bt-dt-shadow-cast-inline-end, 4px 0) 6px -2px var(--fixed-left-shadow-color, transparent)\"\r\n      : undefined,\r\n    transition: checkboxIsLast ? \"box-shadow 0.2s ease\" : undefined,\r\n  };\r\n\r\n  return (\r\n    <thead\r\n      className={cx(\r\n        \"bt-table-title\",\r\n        variant === \"secondary\" && \"bt-table-title--secondary\",\r\n        variant === \"no-fill\" && \"bt-table-title--no-fill\",\r\n      )}\r\n    >\r\n      <tr>\r\n        {hasCheckbox && (\r\n          <th\r\n            className=\"bt-table-item bt-table-item--header bt-data-table__fixed-cell\"\r\n            style={{ ...checkboxStickyStyle }}\r\n          >\r\n            <span className=\"bt-table-item__control\">\r\n              <BTCheckbox\r\n                checked={allSelected}\r\n                indeterminate={someSelected}\r\n                onChange={() => onAction?.({ action: \"check-all\" })}\r\n              />\r\n            </span>\r\n          </th>\r\n        )}\r\n        {columns.map((column, index) => {\r\n          const effectiveIndex = hasCheckbox ? index + 1 : index;\r\n          const fixedStyle = stickyStyle(\r\n            effectiveIndex,\r\n            columns,\r\n            hasCheckbox,\r\n            fixedLeft,\r\n            fixedRight,\r\n          );\r\n\r\n          // checkbox-title and title-dropdown go through BTTableItem\r\n          if (\r\n            column.headerType === \"checkbox-title\" ||\r\n            column.headerType === \"title-dropdown\"\r\n          ) {\r\n            const baseStyle = colBaseStyle(column);\r\n            return (\r\n              <BTTableItem\r\n                key={column.key}\r\n                as=\"th\"\r\n                header\r\n                type={column.headerType}\r\n                label={column.label}\r\n                cell={{ type: column.headerType, label: column.label }}\r\n                columnKey={column.key}\r\n                style={{\r\n                  ...baseStyle,\r\n                  ...colAlignStyle(column),\r\n                  ...fixedStyle,\r\n                }}\r\n                onAction={onAction}\r\n              />\r\n            );\r\n          }\r\n\r\n          // Plain title columns — render directly for sort icon control\r\n          return (\r\n            <TitleCell\r\n              key={column.key}\r\n              column={column}\r\n              fixedStyle={fixedStyle}\r\n              onSort={onSort}\r\n            />\r\n          );\r\n        })}\r\n      </tr>\r\n    </thead>\r\n  );\r\n}\r\n\r\nBTTableTitle.displayName = \"BTTableTitle\";\r\n"
    },
    {
      "path": "index.ts",
      "target": "@ui/data-table/index.ts",
      "type": "registry:ui",
      "content": "export { BTDataTable } from './BTDataTable';\r\nexport { BTTableItem } from './BTTableItem';\r\nexport { BTTableRow } from './BTTableRow';\r\nexport { BTTableTitle } from './BTTableTitle';\r\nexport type {\r\n  BTDataTableCell,\r\n  BTDataTableCellChangePayload,\r\n  BTDataTableColumn,\r\n  BTDataTableOption,\r\n  BTDataTableProps,\r\n  BTDataTableRow,\r\n  BTDataTableSortDirection,\r\n  BTDataTableTag,\r\n  BTDataTableVariant,\r\n  BTSortPayload,\r\n  BTTableActionPayload,\r\n  BTTableCellType,\r\n  BTTableItemProps,\r\n  BTTableRowProps,\r\n  BTTableTitleProps,\r\n} from './BTDataTable.types';\r\n"
    }
  ]
}