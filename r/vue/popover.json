{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "popover",
  "type": "registry:ui",
  "title": "BTPopover",
  "description": "Floating content panel triggered by any element. Composes three sub-components — BTPopover (root), BTPopoverTrigger (click target), BTPopoverContent (panel). Supports align start/center/end and side top/bottom/left/right. Portal-rendered via Vue Teleport.",
  "framework": "vue",
  "category": "molecules",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/Btech---Design-System?node-id=2057-3539",
  "figmaNodeId": "2057-3539",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "BTPopover.css",
      "target": "@ui/popover/BTPopover.css",
      "type": "registry:ui",
      "content": "/*\n * BTPopover — floating content panel.\n *\n * Used by Vue (BTPopoverContent.vue) and React (BTPopoverContent.tsx).\n * This single CSS file is shared between both framework packages.\n *\n * Figma node 2057-3539 · D - Popover\n *\n * Positioning is handled entirely in JS (position: fixed + inline top/left).\n * The panel starts at opacity: 0 so it is invisible while JS measures it;\n * adding .bt-popover-content--visible triggers the entrance animation.\n */\n\n/* ── Trigger wrapper ──────────────────────────────────────────────── */\n/*\n * display: contents makes the span layout-transparent — it does not\n * create its own box and does not affect the trigger element's sizing\n * or flow. Event bubbling still works through it normally.\n */\n.bt-popover-trigger {\n  display: contents;\n}\n\n/* ── Content panel ────────────────────────────────────────────────── */\n.bt-popover-content {\n  position: fixed;\n  z-index: 1000;\n  min-width: 200px;\n  background: var(--bg-primary);\n  border: 1px solid var(--border-primary);\n  border-radius: var(--radius-md);\n  padding: var(--space-lg);\n  /* Subtle elevation — raw rgba allowed for non-tokenised overlays */\n  box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08), 0 1px 4px rgba(0, 0, 0, 0.04);\n  box-sizing: border-box;\n  outline: none;\n\n  /* Hidden until JS positions and reveals the panel */\n  opacity: 0;\n}\n\n/* Bare mode — strips visual chrome; embedded content provides its own card.\n   Use with BTDropdownList, BTDropdownMenu, or any self-styled content. */\n.bt-popover-content--bare {\n  background: none;\n  border: none;\n  box-shadow: none;\n  padding: 0;\n  border-radius: 0;\n}\n\n.bt-popover-content--visible {\n  animation: bt-popover-in 150ms cubic-bezier(0.4, 0, 0.2, 1) forwards;\n}\n\n@keyframes bt-popover-in {\n  from {\n    opacity: 0;\n    transform: scale(0.96);\n  }\n  to {\n    opacity: 1;\n    transform: scale(1);\n  }\n}\n\n/* ── RTL: align content to the right ─────────────────────────────── */\n[dir=\"rtl\"].bt-popover-content,\n[dir=\"rtl\"] .bt-popover-content {\n  text-align: start;\n  direction: rtl;\n}\n"
    },
    {
      "path": "BTPopover.types.ts",
      "target": "@ui/popover/BTPopover.types.ts",
      "type": "registry:ui",
      "content": "/**\n * BTPopover — public prop types.\n * Shared semantics between Vue (v-model:open) and React (open + onOpenChange).\n *\n * Figma node 2057-3539 · D - Popover\n */\n\nexport type BTPopoverSide = 'top' | 'bottom' | 'left' | 'right';\nexport type BTPopoverAlign = 'start' | 'center' | 'end';\n\nexport interface BTPopoverProps {\n  /** Controlled open state. Vue: v-model:open. */\n  open?: boolean;\n  /** Emitted when open state changes. */\n  onOpenChange?: (open: boolean) => void;\n}\n\nexport interface BTPopoverTriggerProps {\n  // no public props for Vue — the trigger wraps its slot transparently\n}\n\nexport interface BTPopoverContentProps {\n  /** Horizontal alignment of the panel relative to the trigger. Default: 'start'. */\n  align?: BTPopoverAlign;\n  /** Which side of the trigger to render on. Default: 'bottom'. */\n  side?: BTPopoverSide;\n  /** Gap in px between the trigger edge and the panel. Default: 8. */\n  sideOffset?: number;\n  /**\n   * Strips the panel's visual chrome (background, border, shadow, padding,\n   * border-radius) so that the embedded content provides its own card styling.\n   *\n   * Use when composing with components that already have a card look,\n   * e.g. `BTDropdownList`, `BTDropdownMenu`.\n   *\n   * @default false\n   * @example\n   * ```vue\n   * <BTPopoverContent align=\"end\" side=\"bottom\" bare>\n   *   <BTDropdownList :items=\"items\" @select=\"onSelect\" />\n   * </BTPopoverContent>\n   * ```\n   */\n  bare?: boolean;\n}\n"
    },
    {
      "path": "BTPopover.vue",
      "target": "@ui/popover/BTPopover.vue",
      "type": "registry:ui",
      "content": "<!--\n  BTPopover — root component.\n\n  Manages open/close state and provides context to BTPopoverTrigger and\n  BTPopoverContent via Vue's provide/inject.\n\n  Supports both uncontrolled (internal state) and controlled (v-model:open) modes.\n\n  Usage:\n    <BTPopover v-model:open=\"isOpen\">\n      <BTPopoverTrigger>\n        <BTButton label=\"Open\" />\n      </BTPopoverTrigger>\n      <BTPopoverContent align=\"start\" side=\"bottom\">\n        <p>Popover content</p>\n      </BTPopoverContent>\n    </BTPopover>\n-->\n<script setup lang=\"ts\">\nimport { ref, provide, watch } from 'vue';\nimport type { Ref } from 'vue';\nimport type { BTPopoverProps } from './BTPopover.types';\n\n// ── Internal context type (not exported) ────────────────────────────────────\nexport interface BTPopoverContext {\n  isOpen:    Ref<boolean>;\n  triggerEl: Ref<HTMLElement | null>;\n  toggle:    () => void;\n  close:     () => void;\n}\n\nconst props = defineProps<BTPopoverProps>();\nconst emit  = defineEmits<{\n  'update:open': [value: boolean];\n}>();\n\n// ── State ────────────────────────────────────────────────────────────────────\nconst isOpen    = ref(props.open ?? false);\nconst triggerEl = ref<HTMLElement | null>(null);  // set by BTPopoverTrigger\n\n// Sync when controlled from outside\nwatch(\n  () => props.open,\n  (v) => { if (v !== undefined) isOpen.value = v; },\n);\n\n// ── Actions ──────────────────────────────────────────────────────────────────\nfunction toggle() {\n  const next = !isOpen.value;\n  isOpen.value = next;\n  emit('update:open', next);\n}\n\nfunction close() {\n  isOpen.value = false;\n  emit('update:open', false);\n}\n\n// ── Provide context ──────────────────────────────────────────────────────────\nprovide<BTPopoverContext>('bt-popover', { isOpen, triggerEl, toggle, close });\n</script>\n\n<template>\n  <slot />\n</template>\n"
    },
    {
      "path": "BTPopoverContent.vue",
      "target": "@ui/popover/BTPopoverContent.vue",
      "type": "registry:ui",
      "content": "<!--\n  BTPopoverContent — the floating panel.\n\n  Teleported to <body> so it escapes any overflow:hidden ancestor.\n  Positioned via JS (position: fixed + inline top/left) after mount so\n  the panel dimensions are known before computing coordinates.\n\n  Align options:\n    start  — panel left-edge aligns with trigger left-edge  (default)\n    center — panel is centred on the trigger\n    end    — panel right-edge aligns with trigger right-edge\n\n  Usage:\n    <BTPopoverContent align=\"start\" side=\"bottom\" :side-offset=\"8\">\n      <p>Content</p>\n    </BTPopoverContent>\n-->\n<script setup lang=\"ts\">\nimport './BTPopover.css';\nimport { inject, useTemplateRef, onMounted, onUnmounted, watch } from 'vue';\nimport type { BTPopoverContext } from './BTPopover.vue';\nimport type { BTPopoverContentProps } from './BTPopover.types';\nimport { positionPopover } from './internal/positionFloating';\n\nconst props = withDefaults(defineProps<BTPopoverContentProps>(), {\n  align:      'start',\n  side:       'bottom',\n  sideOffset: 8,\n  bare:       false,\n});\n\n// Non-null assertion: runtime guard still present in the template via v-if on ctx.isOpen.\n// vue-tsc cannot narrow closures after a throw, so we use ! here.\nconst ctx = inject<BTPopoverContext>('bt-popover')!;\n\nconst contentRef = useTemplateRef<HTMLDivElement>('content');\n\n// ── Positioning (smart flip) ─────────────────────────────────────────────────\nfunction updatePosition() {\n  const wrapper = ctx.triggerEl.value;\n  const content = contentRef.value;\n  if (!wrapper || !content) return;\n\n  // The actual trigger element is the first child of the display:contents span.\n  const trigger = (wrapper.firstElementChild as HTMLElement) ?? wrapper;\n\n  positionPopover(trigger, content, {\n    side:       props.side,\n    align:      props.align,\n    sideOffset: props.sideOffset,\n    margin:     8,\n  });\n\n  // Inherit reading direction from trigger context so portaled panel stays RTL-aware\n  const dir = getComputedStyle(trigger).direction;\n  content.setAttribute('dir', dir);\n\n  // Reveal panel with entrance animation (class adds keyframe animation)\n  content.classList.add('bt-popover-content--visible');\n}\n\n// ── Event handlers ───────────────────────────────────────────────────────────\nfunction onScroll() {\n  if (ctx.isOpen.value) updatePosition();\n}\n\nfunction onDocClick(e: MouseEvent) {\n  if (!ctx.isOpen.value) return;\n  const wrapper = ctx.triggerEl.value;\n  const content = contentRef.value;\n  // `display:contents` spans still appear in the DOM tree so `.contains()` works.\n  if (wrapper?.contains(e.target as Node) || content?.contains(e.target as Node)) return;\n  ctx.close();\n}\n\nfunction onKeyDown(e: KeyboardEvent) {\n  if (e.key === 'Escape') ctx.close();\n}\n\n// ── Positioning watch ─────────────────────────────────────────────────────────\n// `flush: 'post'` means the watcher runs AFTER Vue has patched the DOM,\n// so the v-if div is already in the DOM and contentRef is set when we measure.\nwatch(\n  () => ctx.isOpen.value,\n  (open) => { if (open) updatePosition(); },\n  { flush: 'post' },\n);\n\n// ── Lifecycle ─────────────────────────────────────────────────────────────────\nonMounted(() => {\n  document.addEventListener('scroll',  onScroll,   true);\n  window.addEventListener  ('resize',  onScroll);\n  document.addEventListener('click',   onDocClick, true);\n  document.addEventListener('keydown', onKeyDown);\n});\n\nonUnmounted(() => {\n  document.removeEventListener('scroll',  onScroll,   true);\n  window.removeEventListener  ('resize',  onScroll);\n  document.removeEventListener('click',   onDocClick, true);\n  document.removeEventListener('keydown', onKeyDown);\n});\n</script>\n\n<template>\n  <Teleport to=\"body\">\n    <div\n      v-if=\"ctx.isOpen.value\"\n      ref=\"content\"\n      class=\"bt-popover-content\"\n      :class=\"{ 'bt-popover-content--bare': props.bare }\"\n      role=\"dialog\"\n      tabindex=\"-1\"\n    >\n      <slot />\n    </div>\n  </Teleport>\n</template>\n"
    },
    {
      "path": "BTPopoverTrigger.vue",
      "target": "@ui/popover/BTPopoverTrigger.vue",
      "type": "registry:ui",
      "content": "<!--\n  BTPopoverTrigger — transparent click wrapper.\n\n  Wraps its slot in a `display: contents` span so it does not affect the\n  trigger element's layout.  The wrapper ref is stored in the shared\n  popover context so BTPopoverContent can measure the trigger position.\n\n  Usage:\n    <BTPopoverTrigger>\n      <BTButton label=\"Open\" />\n    </BTPopoverTrigger>\n-->\n<script setup lang=\"ts\">\nimport { inject, useTemplateRef, onMounted, onUnmounted } from 'vue';\nimport type { BTPopoverContext } from './BTPopover.vue';\n\nconst ctx = inject<BTPopoverContext>('bt-popover')!;\n\n// The wrapper span element — its first child is the actual trigger element.\n// We store the wrapper so `wrapper.contains(e.target)` works for click-outside\n// detection in BTPopoverContent (display:contents spans still participate\n// in the DOM tree for event containment checks).\nconst wrapperRef = useTemplateRef<HTMLElement>('wrapper');\n\nonMounted(() => {\n  ctx.triggerEl.value = wrapperRef.value;\n});\n\nonUnmounted(() => {\n  ctx.triggerEl.value = null;\n});\n</script>\n\n<template>\n  <span ref=\"wrapper\" class=\"bt-popover-trigger\" @click=\"ctx.toggle\">\n    <slot />\n  </span>\n</template>\n"
    },
    {
      "path": "index.ts",
      "target": "@ui/popover/index.ts",
      "type": "registry:ui",
      "content": "export { default as BTPopover }         from './BTPopover.vue';\nexport { default as BTPopoverTrigger }  from './BTPopoverTrigger.vue';\nexport { default as BTPopoverContent }  from './BTPopoverContent.vue';\nexport type {\n  BTPopoverProps,\n  BTPopoverTriggerProps,\n  BTPopoverContentProps,\n  BTPopoverSide,\n  BTPopoverAlign,\n} from './BTPopover.types';\n"
    },
    {
      "path": "internal/positionFloating.ts",
      "target": "@ui/popover/internal/positionFloating.ts",
      "type": "registry:ui",
      "content": "/**\n * positionFloating — shared smart-flip positioning utility.\n *\n * Measures available space around a trigger element and places a floating\n * panel (already rendered in the DOM at position:fixed) below or above\n * depending on which direction has more room. Clamps horizontally so the\n * panel never overflows the viewport edges.\n *\n * Usage:\n *   // After the panel is in the DOM (watch flush:'post' or nextTick):\n *   positionBelow(triggerEl, panelEl, { gap: 4, margin: 8 });\n *\n * Also exported: positionDropdown — same algorithm but centres the panel\n * horizontally over an anchor element (for header pill dropdowns).\n */\n\nexport interface PositionOptions {\n  /** Gap between trigger edge and panel edge. Default: 4. */\n  gap?: number;\n  /** Minimum distance from viewport edges. Default: 8. */\n  margin?: number;\n}\n\n/**\n * Position a panel below or above `trigger`, left-aligned (then clamped).\n * Mutates `panel.style.top / left` and returns the resolved direction.\n */\nexport function positionBelow(\n  trigger: HTMLElement,\n  panel: HTMLElement,\n  opts: PositionOptions = {},\n): 'below' | 'above' {\n  const { gap = 4, margin = 8 } = opts;\n  const tRect = trigger.getBoundingClientRect();\n  const pRect = panel.getBoundingClientRect();\n  const vw = window.innerWidth;\n  const vh = window.innerHeight;\n\n  // ── Vertical: prefer below, flip above when there's not enough space ────────\n  const spaceBelow = vh - tRect.bottom - gap;\n  const spaceAbove = tRect.top - gap;\n  const goAbove    = spaceBelow < pRect.height && spaceAbove >= pRect.height;\n\n  const top = goAbove\n    ? tRect.top  - pRect.height - gap\n    : tRect.bottom + gap;\n\n  // ── Horizontal: align to trigger start edge, clamp to viewport ─────────────\n  const isRtl = getComputedStyle(trigger).direction === 'rtl';\n  let left = isRtl ? tRect.right - pRect.width : tRect.left;\n  if (left + pRect.width > vw - margin) left = vw - margin - pRect.width;\n  if (left < margin)                    left = margin;\n\n  panel.style.top  = `${Math.round(top)}px`;\n  panel.style.left = `${Math.round(left)}px`;\n\n  return goAbove ? 'above' : 'below';\n}\n\nexport type PopoverSide   = 'top' | 'bottom' | 'left' | 'right';\nexport type PopoverAlign  = 'start' | 'center' | 'end';\n\n/**\n * Position a popover panel relative to a trigger with auto-flip on all axes.\n * Respects the requested `side` but flips to the opposite if there is no room.\n */\nexport function positionPopover(\n  trigger: HTMLElement,\n  panel: HTMLElement,\n  opts: { side?: PopoverSide; align?: PopoverAlign; sideOffset?: number; margin?: number } = {},\n): void {\n  const { side = 'bottom', align = 'start', sideOffset = 8, margin = 8 } = opts;\n  const tRect = trigger.getBoundingClientRect();\n  const pRect = panel.getBoundingClientRect();\n  const vw = window.innerWidth;\n  const vh = window.innerHeight;\n\n  // ── Resolve side with auto-flip ─────────────────────────────────────────────\n  let resolvedSide = side;\n  if (side === 'bottom') {\n    const fits = vh - tRect.bottom - sideOffset >= pRect.height;\n    if (!fits && tRect.top - sideOffset >= pRect.height) resolvedSide = 'top';\n  } else if (side === 'top') {\n    const fits = tRect.top - sideOffset >= pRect.height;\n    if (!fits && vh - tRect.bottom - sideOffset >= pRect.height) resolvedSide = 'bottom';\n  } else if (side === 'right') {\n    const fits = vw - tRect.right - sideOffset >= pRect.width;\n    if (!fits && tRect.left - sideOffset >= pRect.width) resolvedSide = 'left';\n  } else /* left */ {\n    const fits = tRect.left - sideOffset >= pRect.width;\n    if (!fits && vw - tRect.right - sideOffset >= pRect.width) resolvedSide = 'right';\n  }\n\n  // ── Main axis ───────────────────────────────────────────────────────────────\n  let top  = 0;\n  let left = 0;\n  if      (resolvedSide === 'bottom') top  = tRect.bottom + sideOffset;\n  else if (resolvedSide === 'top')    top  = tRect.top - pRect.height - sideOffset;\n  else if (resolvedSide === 'right')  left = tRect.right  + sideOffset;\n  else /* left */                     left = tRect.left  - pRect.width - sideOffset;\n\n  // ── Cross axis (alignment) — flip start/end in RTL ──────────────────────────\n  const isRtl = getComputedStyle(trigger).direction === 'rtl';\n  const resolvedAlign = isRtl\n    ? (align === 'start' ? 'end' : align === 'end' ? 'start' : align)\n    : align;\n\n  if (resolvedSide === 'top' || resolvedSide === 'bottom') {\n    if      (resolvedAlign === 'start')  left = tRect.left;\n    else if (resolvedAlign === 'center') left = tRect.left + tRect.width / 2 - pRect.width / 2;\n    else /* end */                       left = tRect.right - pRect.width;\n\n    // Clamp horizontally\n    left = Math.min(left, vw - margin - pRect.width);\n    left = Math.max(left, margin);\n  } else {\n    if      (align === 'start')  top = tRect.top;\n    else if (align === 'center') top = tRect.top + tRect.height / 2 - pRect.height / 2;\n    else /* end */               top = tRect.bottom - pRect.height;\n\n    // Clamp vertically\n    top = Math.min(top, vh - margin - pRect.height);\n    top = Math.max(top, margin);\n  }\n\n  panel.style.top  = `${Math.round(top)}px`;\n  panel.style.left = `${Math.round(left)}px`;\n}\n\n/**\n * Position a small dropdown centred horizontally over `anchor`, below or above.\n * Used by BTCalendarHeader month/year pills.\n */\nexport function positionCentered(\n  anchor: HTMLElement,\n  dropdown: HTMLElement,\n  opts: PositionOptions = {},\n): void {\n  const { gap = 4, margin = 8 } = opts;\n  const aRect = anchor.getBoundingClientRect();\n  const dRect = dropdown.getBoundingClientRect();\n  const vw = window.innerWidth;\n  const vh = window.innerHeight;\n\n  // Vertical: prefer below, flip above\n  const spaceBelow = vh - aRect.bottom - gap;\n  const spaceAbove = aRect.top - gap;\n  const goAbove    = spaceBelow < dRect.height && spaceAbove >= dRect.height;\n\n  const top = goAbove\n    ? aRect.top  - dRect.height - gap\n    : aRect.bottom + gap;\n\n  // Horizontal: centre over anchor, clamp\n  let left = aRect.left + aRect.width / 2 - dRect.width / 2;\n  if (left + dRect.width > vw - margin) left = vw - margin - dRect.width;\n  if (left < margin)                    left = margin;\n\n  dropdown.style.top  = `${Math.round(top)}px`;\n  dropdown.style.left = `${Math.round(left)}px`;\n}\n"
    }
  ]
}