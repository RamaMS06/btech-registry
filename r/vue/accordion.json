{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "accordion",
  "type": "registry:ui",
  "title": "BTAccordion",
  "description": "Three-component accordion system. BTAccordion is the group wrapper that manages open state (type=single/multiple). BTAccordionItem is the individual collapsible panel (separator-based, no card chrome). BTAccordionCard is a self-contained expandable card with its own header (title + subtext + chevron).",
  "framework": "vue",
  "category": "molecules",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/Btech---Design-System?node-id=2473-2493",
  "figmaNodeId": "2473:2493",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "BTAccordion.css",
      "target": "@ui/accordion/BTAccordion.css",
      "type": "registry:ui",
      "content": "/* BTAccordion styles — Figma 2473:2493\r\n *\r\n * BTAccordion is the group wrapper; BTAccordionItem is the individual panel.\r\n * Default visual only: separator-based, no card chrome.\r\n * For card-style panels, see BTAccordionCard.css.\r\n *\r\n * Architecture:\r\n *   .bt-accordion-group          — group wrapper (BTAccordion root)\r\n *   .bt-accordion                — single item root (BTAccordionItem)\r\n *   .bt-accordion--open          — currently expanded state\r\n *   .bt-accordion--disabled      — non-interactive\r\n *   .bt-accordion__header        — clickable trigger row\r\n *   .bt-accordion__header--static — non-interactive header (no child slot)\r\n *   .bt-accordion__title-wrap    — flex-col wrapper for title + subtext\r\n *   .bt-accordion__title         — primary label\r\n *   .bt-accordion__subtext       — secondary label\r\n *   .bt-accordion__chevron       — rotating arrow icon\r\n *   .bt-accordion__body          — CSS-grid height animating wrapper\r\n *   .bt-accordion__body-inner    — overflow:hidden inner\r\n *   .bt-accordion__body-content  — padding wrapper\r\n *   .bt-accordion__separator     — 1px divider\r\n */\r\n\r\n/* ── Group wrapper ───────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion-group {\r\n  display: flex;\r\n  flex-direction: column;\r\n  width: 100%;\r\n}\r\n\r\n/* ── Single item root ────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion {\r\n  display: flex;\r\n  flex-direction: column;\r\n  width: 100%;\r\n  box-sizing: border-box;\r\n}\r\n\r\n/* Disabled */\r\n.bt-accordion--disabled {\r\n  opacity: 0.5;\r\n  pointer-events: none;\r\n}\r\n\r\n/* ── Header button ───────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion__header {\r\n  display: flex;\r\n  align-items: flex-start;\r\n  justify-content: space-between;\r\n  gap: var(--space-sm);\r\n  width: 100%;\r\n  padding: 8px 0; /* default variant: 8px vertical, no horizontal */\r\n  background: none;\r\n  border: none;\r\n  cursor: pointer;\r\n  user-select: none;\r\n  text-align: start;\r\n  transition: background 0.15s ease;\r\n}\r\n\r\n.bt-accordion__header:focus-visible {\r\n  outline: 2px solid var(--color-brand-primary);\r\n  outline-offset: 2px;\r\n  border-radius: var(--radius-sm);\r\n}\r\n\r\n/* ── Static header — no chevron, no cursor: pointer ─────────────────────── */\r\n\r\n.bt-accordion__header--static {\r\n  display: flex;\r\n  align-items: flex-start;\r\n  gap: var(--space-sm);\r\n  width: 100%;\r\n  padding: 8px 0;\r\n  text-align: start;\r\n}\r\n\r\n/* ── Title + subtext column ──────────────────────────────────────────────── */\r\n\r\n.bt-accordion__title-wrap {\r\n  display: flex;\r\n  flex-direction: column;\r\n  gap: var(--space-xs);\r\n  flex: 1;\r\n  min-width: 0;\r\n}\r\n\r\n/* ── Title ───────────────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion__title {\r\n  font-family: var(--typography-font-family-sans);\r\n  font-size: var(--typography-font-size-sm); /* 14px */\r\n  font-weight: var(--typography-font-weight-medium); /* 500 */\r\n  line-height: var(--typography-line-height-xs);\r\n  color: var(--text-primary);\r\n  margin: 0;\r\n}\r\n\r\n/* ── Subtext ─────────────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion__subtext {\r\n  font-family: var(--typography-font-family-sans);\r\n  font-size: var(--typography-font-size-xs); /* 12px */\r\n  font-weight: var(--typography-font-weight-medium); /* 500 */\r\n  line-height: var(--typography-line-height-xs);\r\n  color: var(--text-secondary);\r\n  margin: 0;\r\n}\r\n\r\n/* ── Chevron ─────────────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion__chevron {\r\n  flex-shrink: 0;\r\n  width: 16px;\r\n  height: 16px;\r\n  margin-top: var(--space-2xs);\r\n  color: var(--text-secondary);\r\n  display: flex;\r\n  align-items: center;\r\n  justify-content: center;\r\n  transition: transform 0.25s cubic-bezier(0.4, 0, 0.2, 1);\r\n}\r\n\r\n.bt-accordion--open .bt-accordion__chevron {\r\n  transform: rotate(180deg);\r\n}\r\n\r\n/* ── Animated body — CSS grid height trick ───────────────────────────────── */\r\n\r\n.bt-accordion__body {\r\n  display: grid;\r\n  grid-template-rows: 0fr;\r\n  transition: grid-template-rows 0.28s cubic-bezier(0.4, 0, 0.2, 1);\r\n  overflow: hidden;\r\n}\r\n\r\n.bt-accordion--open .bt-accordion__body {\r\n  grid-template-rows: 1fr;\r\n}\r\n\r\n.bt-accordion__body-inner {\r\n  min-height: 0;\r\n}\r\n\r\n/* Small bottom padding before the separator */\r\n.bt-accordion__body-content {\r\n  padding-bottom: var(--space-sm);\r\n}\r\n\r\n/* ── Separator ───────────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion__separator {\r\n  height: 1px;\r\n  background: var(--border-primary);\r\n  width: 100%;\r\n  flex-shrink: 0;\r\n}\r\n\r\n/* ── Disabled text colors ────────────────────────────────────────────────── */\r\n\r\n.bt-accordion--disabled .bt-accordion__title {\r\n  color: var(--text-disabled);\r\n}\r\n\r\n.bt-accordion--disabled .bt-accordion__subtext {\r\n  color: var(--text-disabled);\r\n}\r\n"
    },
    {
      "path": "BTAccordion.types.ts",
      "target": "@ui/accordion/BTAccordion.types.ts",
      "type": "registry:ui",
      "content": "/**\r\n * BTAccordion types — Figma node 2473:2493\r\n * https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/?node-id=2473-2493\r\n */\r\n\r\nexport type BTAccordionType = 'single' | 'multiple';\r\n\r\n/** Props for the BTAccordion group wrapper. */\r\nexport interface BTAccordionProps {\r\n  /**\r\n   * Controls whether one or many items can be open simultaneously.\r\n   * @default 'single'\r\n   */\r\n  type?: BTAccordionType;\r\n  /**\r\n   * The value (key) of the currently open item (single) or open items (multiple).\r\n   * `null` means all items are collapsed (single mode).\r\n   * `[]` means all items are collapsed (multiple mode).\r\n   * Bind with v-model:value.\r\n   */\r\n  value?: string | null | string[];\r\n  /** Additional CSS class on the root element. */\r\n  class?: string;\r\n}\r\n\r\n/** Props for a single BTAccordionItem. subtext is a named slot. */\r\nexport interface BTAccordionItemProps {\r\n  /** Unique key identifying this item within the group. */\r\n  value: string;\r\n  /** Header title text. */\r\n  title: string;\r\n  /** Disable interaction for this item. */\r\n  disabled?: boolean;\r\n  /**\r\n   * Show the 1px separator line below this item.\r\n   * @default true\r\n   */\r\n  hasSeparator?: boolean;\r\n}\r\n\r\n/** Props for a self-contained BTAccordionCard. subtext is a named slot. */\r\nexport interface BTAccordionCardProps {\r\n  /** Card header title text. */\r\n  title: string;\r\n  /** Plain subtext below the title. Use the #subtext slot for rich content. */\r\n  subtext?: string;\r\n  /** When true, the card is non-interactive and dimmed. */\r\n  disabled?: boolean;\r\n  /** Controlled expanded state — bind with v-model. */\r\n  modelValue?: boolean;\r\n  /** Initial expanded state (uncontrolled). */\r\n  initiallyExpanded?: boolean;\r\n}\r\n\r\n/** Shape of the context provided by BTAccordion to BTAccordionItem children. */\r\nexport interface BTAccordionContext {\r\n  isItemOpen: (value: string) => boolean;\r\n  toggle: (value: string) => void;\r\n}\r\n"
    },
    {
      "path": "BTAccordion.vue",
      "target": "@ui/accordion/BTAccordion.vue",
      "type": "registry:ui",
      "content": "<script setup lang=\"ts\">\r\n/**\r\n * BTAccordion — accordion group wrapper (Figma 2473:2493).\r\n *\r\n * Manages which item(s) are open. Supports single (one at a time) and\r\n * multiple (any number) types. No visual chrome of its own.\r\n *\r\n * For a card-style expandable panel with its own header, use BTAccordionCard.\r\n *\r\n * @example\r\n * ```ts\r\n * // Single (default) — one open at a time\r\n * // <BTAccordion v-model:value=\"openKey\">\r\n * //   <BTAccordionItem value=\"a\" title=\"First item\">Content A</BTAccordionItem>\r\n * //   <BTAccordionItem value=\"b\" title=\"Second item\">Content B</BTAccordionItem>\r\n * // </BTAccordion>\r\n *\r\n * // Multiple — any number open simultaneously\r\n * // <BTAccordion type=\"multiple\" v-model:value=\"openKeys\">\r\n * //   <BTAccordionItem value=\"a\" title=\"First item\">Content A</BTAccordionItem>\r\n * //   <BTAccordionItem value=\"b\" title=\"Second item\">Content B</BTAccordionItem>\r\n * // </BTAccordion>\r\n * ```\r\n */\r\nimport './BTAccordion.css';\r\nimport { ref, computed, provide, watch } from 'vue';\r\nimport type { BTAccordionProps, BTAccordionContext } from './BTAccordion.types';\r\n\r\nconst props = withDefaults(defineProps<BTAccordionProps>(), {\r\n  type: 'single',\r\n});\r\n\r\nconst emit = defineEmits<{\r\n  'update:value': [value: string | null | string[]];\r\n}>();\r\n\r\nconst internalSingle = ref<string | null>(null);\r\nconst internalMultiple = ref<string[]>([]);\r\nconst isControlled = computed(() => props.value !== undefined);\r\n\r\nfunction isItemOpen(itemValue: string): boolean {\r\n  if (props.type === 'multiple') {\r\n    const arr = isControlled.value\r\n      ? (props.value as string[] | null | undefined ?? [])\r\n      : internalMultiple.value;\r\n    return Array.isArray(arr) ? arr.includes(itemValue) : false;\r\n  }\r\n  const single = isControlled.value\r\n    ? (props.value as string | null | undefined ?? null)\r\n    : internalSingle.value;\r\n  return single === itemValue;\r\n}\r\n\r\nwatch(\r\n  () => props.value,\r\n  (v) => {\r\n    if (v === undefined) return;\r\n    if (props.type === 'multiple') {\r\n      internalMultiple.value = Array.isArray(v) ? v : [];\r\n    } else {\r\n      internalSingle.value = typeof v === 'string' ? v : null;\r\n    }\r\n  },\r\n);\r\n\r\nfunction toggle(itemValue: string) {\r\n  if (props.type === 'multiple') {\r\n    const current = isControlled.value\r\n      ? (Array.isArray(props.value) ? props.value : [])\r\n      : internalMultiple.value;\r\n    const next = current.includes(itemValue)\r\n      ? current.filter((v) => v !== itemValue)\r\n      : [...current, itemValue];\r\n    if (!isControlled.value) internalMultiple.value = next;\r\n    emit('update:value', next);\r\n  } else {\r\n    const current = isControlled.value\r\n      ? (typeof props.value === 'string' ? props.value : null)\r\n      : internalSingle.value;\r\n    const next = current === itemValue ? null : itemValue;\r\n    if (!isControlled.value) internalSingle.value = next;\r\n    emit('update:value', next);\r\n  }\r\n}\r\n\r\nprovide<BTAccordionContext>('bt-accordion', {\r\n  get isItemOpen() { return isItemOpen; },\r\n  toggle,\r\n});\r\n</script>\r\n\r\n<template>\r\n  <div class=\"bt-accordion-group\" :class=\"props.class\">\r\n    <slot />\r\n  </div>\r\n</template>\r\n"
    },
    {
      "path": "BTAccordionCard.css",
      "target": "@ui/accordion/BTAccordionCard.css",
      "type": "registry:ui",
      "content": "/* BTAccordionCard styles — Figma 2481:6923\r\n *\r\n * Self-contained expandable card with its own header (title + chevron).\r\n * Body has padding: 0 16px 16px (no top, 16px horizontal + bottom) —\r\n * nested BTAccordion items provide their own top spacing via 8px header padding.\r\n * The 16px horizontal aligns item text with the card header title.\r\n * The 16px bottom gives breathing room before the card edge.\r\n *\r\n * Architecture:\r\n *   .bt-accordion-card               — card root (bg + border + radius-sm)\r\n *   .bt-accordion-card--open         — expanded state\r\n *   .bt-accordion-card--disabled     — non-interactive + dimmed\r\n *   .bt-accordion-card__header       — clickable trigger row (constant padding)\r\n *   .bt-accordion-card__header--static — non-interactive header (no body slot)\r\n *   .bt-accordion-card__title-wrap   — flex-col for title + subtext\r\n *   .bt-accordion-card__title        — 16px/bold primary label\r\n *   .bt-accordion-card__subtext      — 12px/medium secondary label\r\n *   .bt-accordion-card__chevron      — rotating arrow icon\r\n *   .bt-accordion-card__body         — CSS-grid height animating wrapper\r\n *   .bt-accordion-card__body-inner   — overflow:hidden inner\r\n *   .bt-accordion-card__body-content — always-on padding (0 16px 16px)\r\n */\r\n\r\n/* ── Card root ───────────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion-card {\r\n  background: var(--bg-primary);\r\n  border: 1px solid var(--border-primary);\r\n  border-radius: var(--radius-sm); /* 6px — Figma radius.sm */\r\n  display: flex;\r\n  flex-direction: column;\r\n  width: 100%;\r\n  box-sizing: border-box;\r\n}\r\n\r\n.bt-accordion-card--disabled {\r\n  opacity: 0.5;\r\n  pointer-events: none;\r\n}\r\n\r\n/* ── Header button ───────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion-card__header {\r\n  display: flex;\r\n  align-items: flex-start;\r\n  justify-content: space-between;\r\n  gap: var(--space-sm);\r\n  width: 100%;\r\n  padding: 12px 16px; /* always 12px top/bottom — does NOT change on expand */\r\n  background: none;\r\n  border: none;\r\n  cursor: pointer;\r\n  user-select: none;\r\n  text-align: start;\r\n}\r\n\r\n.bt-accordion-card__header:focus-visible {\r\n  outline: 2px solid var(--color-brand-primary);\r\n  outline-offset: -2px;\r\n}\r\n\r\n/* ── Static header — no chevron, no cursor: pointer ─────────────────────── */\r\n\r\n.bt-accordion-card__header--static {\r\n  display: flex;\r\n  align-items: flex-start;\r\n  padding: 12px 16px;\r\n  text-align: start;\r\n\r\n}\r\n\r\n/* ── Title + subtext column ──────────────────────────────────────────────── */\r\n\r\n.bt-accordion-card__title-wrap {\r\n  display: flex;\r\n  flex-direction: column;\r\n  gap: var(--space-xs);\r\n  flex: 1;\r\n  min-width: 0;\r\n}\r\n\r\n/* ── Title ───────────────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion-card__title {\r\n  font-family: var(--typography-font-family-sans);\r\n  font-size: var(--typography-font-size-md); /* 16px */\r\n  font-weight: var(--typography-font-weight-bold); /* 700 */\r\n  line-height: var(--typography-line-height-sm);\r\n  color: var(--text-primary);\r\n  margin: 0;\r\n}\r\n\r\n/* ── Subtext ─────────────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion-card__subtext {\r\n  font-family: var(--typography-font-family-sans);\r\n  font-size: var(--typography-font-size-xs); /* 12px */\r\n  font-weight: var(--typography-font-weight-medium); /* 500 */\r\n  line-height: var(--typography-line-height-xs);\r\n  color: var(--text-secondary);\r\n  margin: 0;\r\n}\r\n\r\n/* ── Chevron ─────────────────────────────────────────────────────────────── */\r\n\r\n.bt-accordion-card__chevron {\r\n  flex-shrink: 0;\r\n  width: 16px;\r\n  height: 16px;\r\n  margin-top: var(--space-2xs);\r\n  color: var(--text-secondary);\r\n  display: flex;\r\n  align-items: center;\r\n  justify-content: center;\r\n  transition: transform 0.25s cubic-bezier(0.4, 0, 0.2, 1);\r\n}\r\n\r\n.bt-accordion-card--open .bt-accordion-card__chevron {\r\n  transform: rotate(180deg);\r\n}\r\n\r\n/* ── Animated body — 3-layer CSS grid height trick ───────────────────────── */\r\n/*\r\n * Flutter SizeTransition equivalent:\r\n *   body         → grid 0fr↔1fr + overflow:hidden  (clips body-inner height)\r\n *   body-inner   → min-height:0 + overflow:hidden  (clips body-content)\r\n *   body-content → always-on padding               (no class toggle = smooth)\r\n *\r\n * Why 3 layers: overflow:hidden never clips an element's OWN padding box.\r\n * Padding must be on a CHILD so the parent's overflow:hidden can clip it.\r\n * Without body-content, body-inner's own padding bleeds through when 0fr.\r\n */\r\n\r\n.bt-accordion-card__body {\r\n  display: grid;\r\n  grid-template-rows: 0fr;\r\n  transition: grid-template-rows 0.28s cubic-bezier(0.4, 0, 0.2, 1);\r\n}\r\n\r\n.bt-accordion-card--open .bt-accordion-card__body {\r\n  grid-template-rows: 1fr;\r\n}\r\n\r\n.bt-accordion-card__body-inner {\r\n  min-height: 0;\r\n  overflow: hidden;\r\n}\r\n\r\n.bt-accordion-card__body-content {\r\n  /* 16px horizontal aligns nested BTAccordionItem text with the card header title.\r\n   * 16px bottom gives breathing room before the card edge.\r\n   * No top padding — accordion items provide their own top spacing\r\n   * via their 8px header padding. */\r\n  padding: 0 var(--space-lg) var(--space-lg);\r\n}\r\n\r\n/* ── Disabled text colors ────────────────────────────────────────────────── */\r\n\r\n.bt-accordion-card--disabled .bt-accordion-card__title {\r\n  color: var(--text-disabled);\r\n}\r\n\r\n.bt-accordion-card--disabled .bt-accordion-card__subtext {\r\n  color: var(--text-disabled);\r\n}\r\n"
    },
    {
      "path": "BTAccordionCard.vue",
      "target": "@ui/accordion/BTAccordionCard.vue",
      "type": "registry:ui",
      "content": "<script lang=\"ts\">\r\n/**\r\n * BTAccordionCard — self-contained expandable card with its own header\r\n * (Figma 2473:2493).\r\n *\r\n * Renders a card (white bg + 1px border + border-radius) with a clickable\r\n * header row (title + optional subtext + chevron). The body animates open/close.\r\n *\r\n * When no default slot content is provided, the header renders as a static\r\n * non-interactive div (no chevron, no cursor).\r\n *\r\n * @example\r\n * ```vue\r\n * // FAQ pattern\r\n * <BTAccordionCard title=\"Subscription & Billing\">\r\n *   <template #subtext>Common questions</template>\r\n *   <BTAccordion type=\"single\">\r\n *     <BTAccordionItem value=\"q1\" title=\"How do I cancel?\">...</BTAccordionItem>\r\n *   </BTAccordion>\r\n * </BTAccordionCard>\r\n *\r\n * // Static header — no default slot\r\n * <BTAccordionCard title=\"Header - No Content\" />\r\n * ```\r\n */\r\nimport './BTAccordionCard.css';\r\nimport { defineComponent, ref, computed, watch } from 'vue';\r\n\r\nexport default defineComponent({\r\n  name: 'BTAccordionCard',\r\n\r\n  props: {\r\n    title: { type: String, required: true },\r\n    subtext: { type: String, default: undefined },\r\n    disabled: { type: Boolean, default: false },\r\n    modelValue: { type: Boolean, default: undefined },\r\n    initiallyExpanded: { type: Boolean, default: false },\r\n  },\r\n\r\n  emits: ['update:modelValue'],\r\n\r\n  setup(props, { slots, emit }) {\r\n    const isControlled = computed(() => props.modelValue !== undefined);\r\n    const internalOpen = ref(props.initiallyExpanded ?? false);\r\n\r\n    const isOpen = computed(() =>\r\n      isControlled.value ? (props.modelValue ?? false) : internalOpen.value,\r\n    );\r\n\r\n    watch(\r\n      () => props.modelValue,\r\n      (v) => { if (v !== undefined) internalOpen.value = v; },\r\n    );\r\n\r\n    function toggle() {\r\n      if (props.disabled) return;\r\n      const next = !isOpen.value;\r\n      if (!isControlled.value) internalOpen.value = next;\r\n      emit('update:modelValue', next);\r\n    }\r\n\r\n    // hasContent is checked at render time via a computed that reads ctx.slots.\r\n    // Using ctx.slots (not useSlots()) from defineComponent setup avoids\r\n    // <script setup> compilation quirks and shallowReadonly caching issues.\r\n    const hasContent = computed(() => typeof slots.default === 'function');\r\n\r\n    return { isOpen, hasContent, toggle };\r\n  },\r\n});\r\n</script>\r\n\r\n<template>\r\n  <div\r\n    class=\"bt-accordion-card\"\r\n    :class=\"{\r\n      'bt-accordion-card--open': isOpen && hasContent,\r\n      'bt-accordion-card--disabled': disabled,\r\n    }\"\r\n  >\r\n    <!-- Static header: no default slot → non-interactive div, no chevron -->\r\n    <div v-if=\"!hasContent\" class=\"bt-accordion-card__header--static\">\r\n      <span class=\"bt-accordion-card__title-wrap\">\r\n        <span class=\"bt-accordion-card__title\">{{ title }}</span>\r\n        <span v-if=\"$slots.subtext\" class=\"bt-accordion-card__subtext\">\r\n          <slot name=\"subtext\" />\r\n        </span>\r\n        <span v-else-if=\"subtext\" class=\"bt-accordion-card__subtext\">{{ subtext }}</span>\r\n      </span>\r\n    </div>\r\n\r\n    <!-- Interactive header: default slot present → button with chevron -->\r\n    <button\r\n      v-if=\"hasContent\"\r\n      type=\"button\"\r\n      class=\"bt-accordion-card__header\"\r\n      :aria-expanded=\"isOpen\"\r\n      :disabled=\"disabled || undefined\"\r\n      @click=\"toggle\"\r\n    >\r\n      <span class=\"bt-accordion-card__title-wrap\">\r\n        <span class=\"bt-accordion-card__title\">{{ title }}</span>\r\n        <span v-if=\"$slots.subtext\" class=\"bt-accordion-card__subtext\">\r\n          <slot name=\"subtext\" />\r\n        </span>\r\n        <span v-else-if=\"subtext\" class=\"bt-accordion-card__subtext\">{{ subtext }}</span>\r\n      </span>\r\n      <span class=\"bt-accordion-card__chevron\" aria-hidden=\"true\">\r\n        <svg viewBox=\"0 0 24 24\" width=\"16\" height=\"16\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\">\r\n          <path d=\"M7.41 8.59 12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z\" fill=\"currentColor\" />\r\n        </svg>\r\n      </span>\r\n    </button>\r\n\r\n    <!-- Animated body — 3-layer CSS grid height trick -->\r\n    <!-- body → body-inner → body-content; see BTAccordionCard.css for why 3 layers -->\r\n    <div v-if=\"hasContent\" class=\"bt-accordion-card__body\" :aria-hidden=\"!isOpen\">\r\n      <div class=\"bt-accordion-card__body-inner\">\r\n        <div class=\"bt-accordion-card__body-content\">\r\n          <slot />\r\n        </div>\r\n      </div>\r\n    </div>\r\n  </div>\r\n</template>\r\n"
    },
    {
      "path": "BTAccordionItem.vue",
      "target": "@ui/accordion/BTAccordionItem.vue",
      "type": "registry:ui",
      "content": "<script setup lang=\"ts\">\r\n/**\r\n * BTAccordionItem — single collapsible panel inside a BTAccordion group.\r\n *\r\n * Default visual: separator line at bottom, 14px/medium title.\r\n * Use the #subtext named slot for optional rich content below the title.\r\n * When no default slot content is provided, the header renders as a static\r\n * non-interactive div (no chevron, no cursor: pointer).\r\n *\r\n * @example\r\n * ```ts\r\n * // <BTAccordionItem value=\"item-1\" title=\"My title\">\r\n * //   <template #subtext>\r\n * //     <span style=\"color: var(--color-brand-primary)\">Custom subtext</span>\r\n * //   </template>\r\n * //   <p>Content goes here</p>\r\n * // </BTAccordionItem>\r\n * ```\r\n */\r\nimport { computed, inject, ref, useSlots } from 'vue';\r\nimport type { BTAccordionContext, BTAccordionItemProps } from './BTAccordion.types';\r\n\r\nconst props = withDefaults(defineProps<BTAccordionItemProps>(), {\r\n  hasSeparator: true,\r\n});\r\n\r\nconst slots = useSlots();\r\nconst hasContent = computed(() => !!slots.default);\r\n\r\nconst group = inject<BTAccordionContext | null>('bt-accordion', null);\r\nconst standaloneOpen = ref(false);\r\n\r\nconst isOpen = computed(() => {\r\n  if (group) return group.isItemOpen(props.value);\r\n  return standaloneOpen.value;\r\n});\r\n\r\nfunction toggle() {\r\n  if (props.disabled) return;\r\n  if (group) {\r\n    group.toggle(props.value);\r\n  } else {\r\n    standaloneOpen.value = !standaloneOpen.value;\r\n  }\r\n}\r\n</script>\r\n\r\n<template>\r\n  <div\r\n    class=\"bt-accordion\"\r\n    :class=\"[\r\n      isOpen && 'bt-accordion--open',\r\n      disabled && 'bt-accordion--disabled',\r\n    ]\"\r\n  >\r\n    <!-- Static header when no default slot content -->\r\n    <div v-if=\"!hasContent\" class=\"bt-accordion__header--static\">\r\n      <span class=\"bt-accordion__title-wrap\">\r\n        <span class=\"bt-accordion__title\">{{ title }}</span>\r\n        <span v-if=\"$slots.subtext\" class=\"bt-accordion__subtext\">\r\n          <slot name=\"subtext\" />\r\n        </span>\r\n      </span>\r\n    </div>\r\n\r\n    <!-- Interactive header when child content exists -->\r\n    <button\r\n      v-else\r\n      type=\"button\"\r\n      class=\"bt-accordion__header\"\r\n      :aria-expanded=\"isOpen\"\r\n      :disabled=\"disabled\"\r\n      @click=\"toggle\"\r\n    >\r\n      <span class=\"bt-accordion__title-wrap\">\r\n        <span class=\"bt-accordion__title\">{{ title }}</span>\r\n        <span v-if=\"$slots.subtext\" class=\"bt-accordion__subtext\">\r\n          <slot name=\"subtext\" />\r\n        </span>\r\n      </span>\r\n      <span class=\"bt-accordion__chevron\" aria-hidden=\"true\">\r\n        <svg viewBox=\"0 0 24 24\" width=\"16\" height=\"16\" fill=\"none\" xmlns=\"http://www.w3.org/2000/svg\">\r\n          <path d=\"M7.41 8.59 12 13.17l4.59-4.58L18 10l-6 6-6-6 1.41-1.41z\" fill=\"currentColor\" />\r\n        </svg>\r\n      </span>\r\n    </button>\r\n\r\n    <!-- Animated content body — CSS grid height trick -->\r\n    <div v-if=\"hasContent\" class=\"bt-accordion__body\" :aria-hidden=\"!isOpen\">\r\n      <div class=\"bt-accordion__body-inner\">\r\n        <div class=\"bt-accordion__body-content\">\r\n          <slot />\r\n        </div>\r\n      </div>\r\n    </div>\r\n\r\n    <!-- Separator (always default variant; hasSeparator controls visibility) -->\r\n    <div\r\n      v-if=\"hasSeparator\"\r\n      class=\"bt-accordion__separator\"\r\n      aria-hidden=\"true\"\r\n    />\r\n  </div>\r\n</template>\r\n"
    },
    {
      "path": "index.ts",
      "target": "@ui/accordion/index.ts",
      "type": "registry:ui",
      "content": "export { default as BTAccordion } from './BTAccordion.vue';\r\nexport { default as BTAccordionCard } from './BTAccordionCard.vue';\r\nexport { default as BTAccordionItem } from './BTAccordionItem.vue';\r\nexport type {\r\n  BTAccordionProps,\r\n  BTAccordionCardProps,\r\n  BTAccordionItemProps,\r\n  BTAccordionType,\r\n} from './BTAccordion.types';\r\n"
    }
  ]
}