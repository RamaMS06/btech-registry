{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "tabs",
  "type": "registry:ui",
  "title": "BTTabs",
  "description": "Tab strip molecule with two variants: 'segmented' (pill tray with primary fill on active) and 'line' (flat row with primary underline on active). Controlled via v-model:activeIndex. Supports optional leading/trailing icon slots per tab index.",
  "framework": "vue",
  "category": "molecules",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/BTech-Design-System?node-id=1-53",
  "figmaNodeId": "1:53",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "BTTabs.css",
      "target": "@ui/BTTabs.css",
      "type": "registry:ui",
      "content": "/* BTTabs — tab molecule (Figma 1:53 / Base/TabItem 434:5262)\n *\n * Variants:  segmented · line\n * Animation: sliding indicator (not opacity/border toggling)\n * Scrollable: overflow-x auto + center-on-active\n *\n * Architecture:\n *   .bt-tabs                    root — position:relative for absolute indicator\n *   .bt-tabs--segmented         pill-style tray\n *   .bt-tabs--line              underline-style\n *   .bt-tabs--scrollable        horizontal scroll, fills parent width\n *   .bt-tabs--ready             enables CSS transition (added after first paint)\n *   .bt-tabs__indicator         sliding pill / underline — absolutely positioned\n *   .bt-tabs__item              individual tab button — z-index:1 (above indicator)\n *   .bt-tabs__item--active      active text color\n *   .bt-tabs__icon              icon wrapper slot\n */\n\n/* ── Container ───────────────────────────────────────────────────────────── */\n\n.bt-tabs {\n  display: inline-flex;\n  align-items: center;\n  position: relative;  /* anchor for absolute indicator */\n}\n\n/* Scrollable: fills parent, allows horizontal overflow.\n   Scrollbar styling delegated to BTScrollbar atom (bt-scrollbar--x class). */\n.bt-tabs--scrollable {\n  display: flex;\n  width: 100%;\n  overflow-x: auto;\n  -webkit-overflow-scrolling: touch;\n}\n\n/* Segmented: gray tray, 4px padding, radius.md, height 40px */\n.bt-tabs--segmented {\n  background: var(--bg-secondary);\n  border-radius: var(--radius-md);\n  padding: var(--space-xs);\n  gap: 0;\n  height: 40px;\n}\n\n/* Line: no tray, 4px gap between items */\n.bt-tabs--line {\n  gap: var(--space-xs);\n  background: none;\n}\n\n/* ── Sliding indicator ───────────────────────────────────────────────────── */\n\n.bt-tabs__indicator {\n  position: absolute;\n  pointer-events: none;\n  z-index: 0;\n  /* transition enabled only after first paint (.bt-tabs--ready) */\n}\n\n.bt-tabs--ready .bt-tabs__indicator {\n  transition:\n    left  0.22s cubic-bezier(0.4, 0, 0.2, 1),\n    width 0.22s cubic-bezier(0.4, 0, 0.2, 1);\n}\n\n/* Segmented: filled primary pill behind the label */\n.bt-tabs--segmented .bt-tabs__indicator {\n  top:    4px;   /* = container padding-top */\n  height: 32px;\n  background: var(--color-brand-primary);\n  border-radius: var(--radius-sm);\n}\n\n/* Line: 2px primary underline at the bottom */\n.bt-tabs--line .bt-tabs__indicator {\n  bottom: 0;\n  height: 2px;\n  background: var(--color-brand-primary);\n}\n\n/* ── Tab item ────────────────────────────────────────────────────────────── */\n\n.bt-tabs__item {\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  gap: var(--space-xs);\n  height: 32px;\n  padding: var(--space-sm) var(--space-md);\n  border-radius: var(--radius-sm);\n  border: none;\n  background: none;\n  cursor: pointer;\n  white-space: nowrap;\n  overflow: hidden;\n  font-family: var(--typography-font-family-sans, 'Geist', sans-serif);\n  font-size: var(--typography-font-size-sm);\n  font-weight: 500;\n  line-height: var(--typography-line-height-xs);\n  letter-spacing: 0;\n  color: var(--text-secondary);\n  transition: color 0.22s cubic-bezier(0.4, 0, 0.2, 1);\n  flex-shrink: 0;\n  /* above the absolute indicator */\n  position: relative;\n  z-index: 1;\n}\n\n.bt-tabs__item:focus-visible {\n  outline: 2px solid var(--color-brand-primary);\n  outline-offset: 2px;\n  border-radius: var(--radius-sm);\n  z-index: 2;\n}\n\n.bt-tabs__item:disabled,\n.bt-tabs__item[aria-disabled='true'] {\n  opacity: 0.4;\n  cursor: not-allowed;\n}\n\n/* ── Segmented active / hover ────────────────────────────────────────────── */\n\n/* Active text color — background is the sliding indicator */\n.bt-tabs--segmented .bt-tabs__item--active {\n  color: var(--text-inverse);\n}\n\n.bt-tabs--segmented .bt-tabs__item:not(.bt-tabs__item--active):not(:disabled):hover {\n  background: rgb(0 0 0 / 0.05);\n  border-radius: var(--radius-sm);\n}\n\n/* ── Line active / hover ─────────────────────────────────────────────────── */\n\n.bt-tabs--line .bt-tabs__item--active {\n  color: var(--color-brand-primary);\n}\n\n.bt-tabs--line .bt-tabs__item:not(.bt-tabs__item--active):not(:disabled):hover {\n  color: var(--text-secondary);\n  opacity: 0.7;\n}\n\n/* ── Icon slot ───────────────────────────────────────────────────────────── */\n\n.bt-tabs__icon {\n  display: flex;\n  align-items: center;\n  justify-content: center;\n  width: 16px;\n  height: 16px;\n  flex-shrink: 0;\n}\n\n/* Inline SVG (startIcon data prop) fills the 16×16 icon wrapper. */\n.bt-tabs__icon > svg {\n  width: 100%;\n  height: 100%;\n}\n"
    },
    {
      "path": "BTTabs.types.ts",
      "target": "@ui/BTTabs.types.ts",
      "type": "registry:ui",
      "content": "// BTTabs types — Figma source: node 1:53 · Base/TabItem 434:5262\nimport { reactive } from 'vue';\n\n/** Visual style of the tab strip. */\nexport type BTTabsVariant = 'segmented' | 'line';\n\n/** Data for a single tab. */\nexport interface BTTabItem {\n  /** Label text displayed in the tab. */\n  label: string;\n  /** Whether this tab is not interactive. @default false */\n  disabled?: boolean;\n  /**\n   * SVG path `d` attribute for an optional leading icon (24×24 viewBox).\n   * When provided, a 16×16 icon is rendered before the label.\n   * Slot `#leading-icon-{i}` takes precedence when both are supplied.\n   */\n  startIcon?: string;\n}\n\nexport interface BTTabsProps {\n  /**\n   * Visual variant.\n   * - `'segmented'` — pill-style tray, active tab has filled primary background.\n   * - `'line'`      — flat row, active tab has primary underline border.\n   * @default 'segmented'\n   */\n  variant?: BTTabsVariant;\n  /** Ordered list of tab descriptors. */\n  tabs: BTTabItem[];\n  /**\n   * Zero-based index of the currently active tab.\n   * Controlled via `v-model:activeIndex`.\n   * Ignored when `controller` is provided.\n   */\n  activeIndex?: number;\n  /**\n   * When `true`, the tab strip becomes horizontally scrollable and\n   * auto-scrolls to center the active tab on each selection.\n   * Useful when there are many tabs and the container is narrow.\n   * @default false\n   */\n  scrollable?: boolean;\n  /** Additional CSS class names on the root element. */\n  className?: string;\n  /**\n   * Optional programmatic controller. When provided, overrides `activeIndex`.\n   * Create with {@link createBTTabsController}.\n   */\n  controller?: BTTabsController;\n}\n\n/**\n * Programmatic controller for {@link BTTabs}.\n *\n * Create with {@link createBTTabsController} to get a reactive instance.\n *\n * @example\n * ```ts\n * const tabController = createBTTabsController(3);\n *\n * // In template:\n * // <BTTabs :controller=\"tabController\" :tabs=\"tabs\" />\n *\n * // Programmatic jump:\n * tabController.animateTo(2);\n * ```\n */\nexport interface BTTabsController {\n  /** Total number of tabs. */\n  readonly length: number;\n  /** Zero-based index of the currently selected tab. */\n  index: number;\n  /** Zero-based index of the previously selected tab. */\n  readonly previousIndex: number;\n  /**\n   * Selects the tab at [value]. No-op if already selected.\n   * Updates [index] and [previousIndex] reactively.\n   */\n  animateTo: (value: number) => void;\n}\n\n/**\n * Creates a reactive {@link BTTabsController} for programmatic tab control.\n *\n * The returned object is Vue-reactive — template expressions depending on\n * `controller.index` update automatically when `animateTo` is called.\n *\n * @param length - total number of tabs (must be > 0)\n * @param initialIndex - initially selected tab index (default `0`)\n */\nexport function createBTTabsController(\n  length: number,\n  initialIndex = 0,\n): BTTabsController {\n  const state = reactive<BTTabsController>({\n    length,\n    index: initialIndex,\n    previousIndex: initialIndex,\n    animateTo(value: number) {\n      if (value < 0 || value >= state.length) return;\n      if (state.index === value) return;\n      (state as { previousIndex: number }).previousIndex = state.index;\n      state.index = value;\n    },\n  });\n  return state;\n}\n"
    },
    {
      "path": "BTTabs.vue",
      "target": "@ui/BTTabs.vue",
      "type": "registry:ui",
      "content": "<script setup lang=\"ts\">\n/**\n * BTTabs — tab molecule (Figma 1:53 / Base/TabItem 434:5262).\n *\n * Two variants: 'segmented' (pill tray) · 'line' (underline).\n * Sliding indicator: absolutely-positioned div that animates left+width\n * instead of toggling classes — smooth slide on every tab change.\n * scrollable: overflow-x auto + centers the active tab on selection.\n *\n * @example\n * ```ts\n * // Segmented tabs\n * // <BTTabs variant=\"segmented\" :tabs=\"[{label:'Overview'},{label:'Details'}]\" v-model:activeIndex=\"active\" />\n * // Scrollable line tabs\n * // <BTTabs variant=\"line\" :tabs=\"manyTabs\" v-model:activeIndex=\"active\" :scrollable=\"true\" />\n * ```\n */\nimport './BTTabs.css';\nimport '../../atoms/Scrollbar/BTScrollbar.css';\nimport { computed, ref, watch, onMounted, nextTick } from 'vue';\nimport type { BTTabsProps } from './BTTabs.types';\n\nconst props = withDefaults(defineProps<BTTabsProps>(), {\n  variant: 'segmented',\n  activeIndex: 0,\n  scrollable: false,\n  controller: undefined,\n});\n\nconst emit = defineEmits<{\n  (e: 'update:activeIndex', index: number): void;\n}>();\n\nconst currentIndex = computed(() =>\n  props.controller ? props.controller.index : (props.activeIndex ?? 0),\n);\n\n// ── Refs ─────────────────────────────────────────────────────────────────────\n\nconst containerRef = ref<HTMLDivElement | null>(null);\nconst buttonRefs = ref<HTMLButtonElement[]>([]);\n\nfunction storeButtonRef(el: unknown, i: number) {\n  if (el) buttonRefs.value[i] = el as HTMLButtonElement;\n}\n\n// ── Sliding indicator state ───────────────────────────────────────────────────\n\nconst indicatorLeft = ref(0);\nconst indicatorWidth = ref(0);\n// `isReady` prevents the CSS transition from firing on first paint\n// (indicator jumps to correct position without sliding from 0).\nconst isReady = ref(false);\n\nfunction updateIndicator() {\n  const btn = buttonRefs.value[currentIndex.value];\n  const container = containerRef.value;\n  if (!btn || !container) return;\n\n  indicatorLeft.value = btn.offsetLeft;\n  indicatorWidth.value = btn.offsetWidth;\n\n  // Center active tab when scrollable\n  if (props.scrollable) {\n    const center = btn.offsetLeft + btn.offsetWidth / 2;\n    container.scrollTo({\n      left: center - container.offsetWidth / 2,\n      behavior: isReady.value ? 'smooth' : 'instant',\n    });\n  }\n}\n\nonMounted(async () => {\n  // Wait for layout so offsetLeft values are populated\n  await nextTick();\n  updateIndicator();\n  // Enable CSS transition only after the first position is painted\n  await nextTick();\n  isReady.value = true;\n});\n\n// Re-position indicator whenever active index changes\nwatch(currentIndex, updateIndicator);\n// Re-position when controller index changes externally\nwatch(() => props.controller?.index, updateIndicator);\n\n// ── Event handling ────────────────────────────────────────────────────────────\n\nfunction select(index: number, disabled?: boolean) {\n  if (disabled) return;\n  props.controller?.animateTo(index);\n  emit('update:activeIndex', index);\n}\n\n// ── Derived styles / classes ──────────────────────────────────────────────────\n\nconst rootClass = computed(() =>\n  [\n    'bt-tabs',\n    `bt-tabs--${props.variant}`,\n    props.scrollable ? 'bt-tabs--scrollable' : '',\n    // bt-scrollbar--x: btech-token scrollbar styling from BTScrollbar atom\n    props.scrollable ? 'bt-scrollbar--x' : '',\n    isReady.value ? 'bt-tabs--ready' : '',\n    props.className,\n  ].filter(Boolean),\n);\n\nconst indicatorStyle = computed(() => ({\n  left: `${indicatorLeft.value}px`,\n  width: `${indicatorWidth.value}px`,\n}));\n</script>\n\n<template>\n  <div :class=\"rootClass\" role=\"tablist\" ref=\"containerRef\">\n    <!-- Sliding indicator — behind tab labels (z-index 0) -->\n    <div\n      class=\"bt-tabs__indicator\"\n      :style=\"indicatorStyle\"\n      aria-hidden=\"true\"\n    />\n\n    <button\n      v-for=\"(tab, i) in tabs\"\n      :key=\"i\"\n      role=\"tab\"\n      :ref=\"(el) => storeButtonRef(el, i)\"\n      :class=\"[\n        'bt-tabs__item',\n        i === currentIndex && 'bt-tabs__item--active',\n      ]\"\n      :aria-selected=\"i === currentIndex\"\n      :aria-disabled=\"tab.disabled || undefined\"\n      :disabled=\"tab.disabled\"\n      :tabindex=\"i === currentIndex ? 0 : -1\"\n      @click=\"select(i, tab.disabled)\"\n    >\n      <!-- Leading icon: slot takes precedence over startIcon data prop -->\n      <span v-if=\"$slots[`leading-icon-${i}`]\" class=\"bt-tabs__icon\">\n        <slot :name=\"`leading-icon-${i}`\" />\n      </span>\n      <span v-else-if=\"tab.startIcon\" class=\"bt-tabs__icon\">\n        <svg viewBox=\"0 0 24 24\" fill=\"currentColor\" aria-hidden=\"true\">\n          <path :d=\"tab.startIcon\" />\n        </svg>\n      </span>\n\n      {{ tab.label }}\n\n      <!-- Optional trailing icon via scoped slot -->\n      <span v-if=\"$slots[`trailing-icon-${i}`]\" class=\"bt-tabs__icon\">\n        <slot :name=\"`trailing-icon-${i}`\" />\n      </span>\n    </button>\n  </div>\n</template>\n"
    }
  ]
}