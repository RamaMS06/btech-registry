{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "hover-card",
  "type": "registry:ui",
  "title": "BTHoverCard",
  "description": "Hover-triggered floating card that appears after a configurable delay when the user hovers over a trigger element. Supports four-direction positioning (top/bottom/left/right) with auto-flip and alignment control. Web only (Vue + React). Compose BTHoverCard > BTHoverCardTrigger + BTHoverCardContent > BTHoverCardItem.",
  "framework": "vue",
  "category": "molecules",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/Btech---Design-System?m=auto&node-id=2065-2405",
  "figmaNodeId": "2065-2405",
  "dependencies": [
    "@btech/tokens"
  ],
  "devDependencies": [],
  "registryDependencies": [],
  "files": [
    {
      "path": "BTHoverCard.css",
      "target": "@ui/hover-card/BTHoverCard.css",
      "type": "registry:ui",
      "content": "/* BTHoverCard — floating hover card.\r\n   Figma node 2065-2405 · D - HoverCard\r\n   Web only. Single CSS file — copied verbatim to both vue/ and react/ folders.\r\n   All colors via @btech/tokens CSS vars — no hex fallbacks. */\r\n\r\n/* ── Trigger wrapper ─────────────────────────────────────────── */\r\n/* display:contents makes the span layout-transparent — it does not\r\n   create its own box. Event bubbling still works normally. */\r\n.bt-hover-card-trigger {\r\n  display: contents;\r\n}\r\n\r\n/* ── Content panel ───────────────────────────────────────────── */\r\n.bt-hover-card-content {\r\n  position: fixed;\r\n  z-index: 50;\r\n  width: fit-content;\r\n  max-width: 380px;\r\n  box-sizing: border-box;\r\n  background: var(--bg-primary);\r\n  border: 1px solid var(--border-primary);\r\n  border-radius: var(--radius-md);\r\n  padding: var(--space-lg);\r\n  box-shadow: var(--shadow-elevation-xl);\r\n  outline: none;\r\n  /* Hidden until JS positions and reveals the panel */\r\n  opacity: 0;\r\n}\r\n\r\n.bt-hover-card-content--visible {\r\n  animation: bt-hover-card-in 120ms ease-out forwards;\r\n}\r\n\r\n@keyframes bt-hover-card-in {\r\n  from { opacity: 0; transform: scale(0.97); }\r\n  to   { opacity: 1; transform: scale(1); }\r\n}\r\n\r\n/* ── BTHoverCardItem ─────────────────────────────────────────── */\r\n.bt-hover-card-item {\r\n  display: flex;\r\n  flex-direction: column;\r\n  gap: var(--space-sm);\r\n}\r\n\r\n.bt-hover-card-item__title {\r\n  font-family: var(--typography-font-family-sans);\r\n  font-size: var(--typography-font-size-sm);\r\n  font-weight: 700;\r\n  line-height: var(--typography-line-height-xs);\r\n  letter-spacing: 0;\r\n  color: var(--text-primary);\r\n  margin: 0;\r\n}\r\n\r\n.bt-hover-card-item__description {\r\n  font-family: var(--typography-font-family-sans);\r\n  font-size: var(--typography-font-size-sm);\r\n  font-weight: 500;\r\n  line-height: var(--typography-line-height-xs);\r\n  letter-spacing: 0;\r\n  color: var(--text-primary);\r\n  margin: 0;\r\n}\r\n\r\n.bt-hover-card-item__caption {\r\n  font-family: var(--typography-font-family-sans);\r\n  font-size: var(--typography-font-size-2xs);\r\n  font-weight: 500;\r\n  line-height: var(--typography-line-height-xs);\r\n  letter-spacing: 0;\r\n  color: var(--text-secondary);\r\n  margin: 0;\r\n}\r\n"
    },
    {
      "path": "BTHoverCard.types.ts",
      "target": "@ui/hover-card/BTHoverCard.types.ts",
      "type": "registry:ui",
      "content": "/**\r\n * BTHoverCard — public prop types.\r\n * Figma node 2065-2405 · D - HoverCard\r\n * Web only (Vue 3 + React 18). No Flutter.\r\n */\r\n\r\nexport type BTHoverCardSide  = 'top' | 'bottom' | 'left' | 'right';\r\nexport type BTHoverCardAlign = 'start' | 'center' | 'end';\r\n\r\nexport interface BTHoverCardProps {\r\n  /** Delay in ms before the card opens after mouseenter. Default: 0. */\r\n  openDelay?:  number;\r\n  /** Delay in ms before the card closes after mouseleave. Default: 0. */\r\n  closeDelay?: number;\r\n}\r\n\r\nexport interface BTHoverCardTriggerProps {\r\n  // no public props — wraps slot/children transparently\r\n}\r\n\r\nexport interface BTHoverCardContentProps {\r\n  /** Which side of the trigger to render the panel on. Default: 'bottom'. */\r\n  side?:       BTHoverCardSide;\r\n  /** Alignment of the panel relative to the trigger. Default: 'center'. */\r\n  align?:      BTHoverCardAlign;\r\n  /** Gap in px between trigger edge and panel edge. Default: 8. */\r\n  sideOffset?: number;\r\n}\r\n\r\nexport interface BTHoverCardItemProps {\r\n  /** Bold title text — always shown. */\r\n  title:        string;\r\n  /** Optional secondary line — 14px, text-primary. */\r\n  description?: string;\r\n  /** Optional tertiary line — 10px, text-secondary. */\r\n  caption?:     string;\r\n}\r\n"
    },
    {
      "path": "BTHoverCard.vue",
      "target": "@ui/hover-card/BTHoverCard.vue",
      "type": "registry:ui",
      "content": "<!--\r\n  BTHoverCard — root component.\r\n\r\n  Manages open state and delay timers. Provides shared context to\r\n  BTHoverCardTrigger and BTHoverCardContent via Vue provide/inject.\r\n\r\n  Hover lifecycle:\r\n    mouseenter trigger → openDelay timer → isOpen = true\r\n    mouseleave trigger → closeDelay timer (cancelled if mouse enters content)\r\n    mouseleave content → closeDelay timer → isOpen = false\r\n    Escape key         → close immediately\r\n\r\n  Usage:\r\n    <BTHoverCard :open-delay=\"700\" :close-delay=\"300\">\r\n      <BTHoverCardTrigger>\r\n        <BTButton label=\"Hover me\" />\r\n      </BTHoverCardTrigger>\r\n      <BTHoverCardContent side=\"bottom\" align=\"center\">\r\n        <BTHoverCardItem title=\"Title\" description=\"...\" caption=\"...\" />\r\n      </BTHoverCardContent>\r\n    </BTHoverCard>\r\n-->\r\n<script setup lang=\"ts\">\r\nimport { ref, provide } from 'vue';\r\nimport type { Ref } from 'vue';\r\nimport type { BTHoverCardProps } from './BTHoverCard.types';\r\n\r\n// ── Context type (consumed by Trigger + Content) ─────────────────────────────\r\nexport interface BTHoverCardContext {\r\n  isOpen:        Ref<boolean>;\r\n  triggerEl:     Ref<HTMLElement | null>;\r\n  scheduleOpen:  () => void;\r\n  scheduleClose: () => void;\r\n  cancelClose:   () => void;\r\n  /** Close immediately — used by Escape key for accessibility. */\r\n  close:         () => void;\r\n}\r\n\r\nconst props = withDefaults(defineProps<BTHoverCardProps>(), {\r\n  openDelay:  0,\r\n  closeDelay: 0,\r\n});\r\n\r\n// ── State ────────────────────────────────────────────────────────────────────\r\nconst isOpen    = ref(false);\r\nconst triggerEl = ref<HTMLElement | null>(null);\r\n\r\nlet openTimer:  ReturnType<typeof setTimeout> | null = null;\r\nlet closeTimer: ReturnType<typeof setTimeout> | null = null;\r\n\r\n// ── Timer actions ────────────────────────────────────────────────────────────\r\nfunction scheduleOpen() {\r\n  if (closeTimer) { clearTimeout(closeTimer); closeTimer = null; }\r\n  if (isOpen.value) return;\r\n  openTimer = setTimeout(() => { isOpen.value = true; }, props.openDelay);\r\n}\r\n\r\nfunction scheduleClose() {\r\n  if (openTimer) { clearTimeout(openTimer); openTimer = null; }\r\n  closeTimer = setTimeout(() => { isOpen.value = false; }, props.closeDelay);\r\n}\r\n\r\nfunction cancelClose() {\r\n  if (closeTimer) { clearTimeout(closeTimer); closeTimer = null; }\r\n}\r\n\r\nfunction close() {\r\n  if (openTimer)  { clearTimeout(openTimer);  openTimer  = null; }\r\n  if (closeTimer) { clearTimeout(closeTimer); closeTimer = null; }\r\n  isOpen.value = false;\r\n}\r\n\r\n// ── Provide context ──────────────────────────────────────────────────────────\r\nprovide<BTHoverCardContext>('bt-hover-card', {\r\n  isOpen,\r\n  triggerEl,\r\n  scheduleOpen,\r\n  scheduleClose,\r\n  cancelClose,\r\n  close,\r\n});\r\n</script>\r\n\r\n<template>\r\n  <slot />\r\n</template>\r\n"
    },
    {
      "path": "BTHoverCardContent.vue",
      "target": "@ui/hover-card/BTHoverCardContent.vue",
      "type": "registry:ui",
      "content": "<!--\r\n  BTHoverCardContent — the floating card panel.\r\n\r\n  Teleported to <body> to escape overflow:hidden ancestors.\r\n  Positioned via positionPopover() from utils/positionFloating.ts.\r\n  Supports side (top/bottom/left/right) with auto-flip and\r\n  align (start/center/end) with viewport clamping.\r\n\r\n  mouseenter → cancel the close timer (user moved mouse from trigger to card).\r\n  mouseleave → restart the close timer.\r\n  Escape key → close immediately.\r\n-->\r\n<script setup lang=\"ts\">\r\nimport './BTHoverCard.css';\r\nimport { inject, useTemplateRef, watch, onMounted, onUnmounted } from 'vue';\r\nimport type { BTHoverCardContext } from './BTHoverCard.vue';\r\nimport type { BTHoverCardContentProps } from './BTHoverCard.types';\r\nimport { positionPopover } from './internal/positionFloating';\r\n\r\nconst props = withDefaults(defineProps<BTHoverCardContentProps>(), {\r\n  side:       'bottom',\r\n  align:      'center',\r\n  sideOffset: 8,\r\n});\r\n\r\nconst ctx = inject<BTHoverCardContext>('bt-hover-card')!;\r\nconst contentRef = useTemplateRef<HTMLDivElement>('content');\r\n\r\n// ── Positioning ──────────────────────────────────────────────────────────────\r\nfunction updatePosition() {\r\n  const wrapper = ctx.triggerEl.value;\r\n  const content = contentRef.value;\r\n  if (!wrapper || !content) return;\r\n\r\n  // display:contents span → measure its first child (the actual trigger element)\r\n  const trigger = (wrapper.firstElementChild as HTMLElement) ?? wrapper;\r\n\r\n  positionPopover(trigger, content, {\r\n    side:       props.side,\r\n    align:      props.align,\r\n    sideOffset: props.sideOffset,\r\n    margin:     8,\r\n  });\r\n\r\n  // Propagate direction to portal panel for RTL text/layout.\r\n  const dir = getComputedStyle(trigger).direction;\r\n  if (dir === 'rtl') content.setAttribute('dir', 'rtl');\r\n\r\n  content.classList.add('bt-hover-card-content--visible');\r\n}\r\n\r\n// ── Event handlers ───────────────────────────────────────────────────────────\r\nfunction onScroll() {\r\n  if (ctx.isOpen.value) updatePosition();\r\n}\r\n\r\nfunction onKeyDown(e: KeyboardEvent) {\r\n  // Escape closes immediately for accessibility (not delayed like mouseleave).\r\n  if (e.key === 'Escape') ctx.close();\r\n}\r\n\r\n// flush:'post' ensures the v-if panel is in the DOM before we measure it.\r\nwatch(\r\n  () => ctx.isOpen.value,\r\n  (open) => { if (open) updatePosition(); },\r\n  { flush: 'post' },\r\n);\r\n\r\nonMounted(() => {\r\n  document.addEventListener('scroll', onScroll, true);\r\n  window.addEventListener('resize', onScroll);\r\n  document.addEventListener('keydown', onKeyDown);\r\n});\r\n\r\nonUnmounted(() => {\r\n  document.removeEventListener('scroll', onScroll, true);\r\n  window.removeEventListener('resize', onScroll);\r\n  document.removeEventListener('keydown', onKeyDown);\r\n});\r\n</script>\r\n\r\n<template>\r\n  <Teleport to=\"body\">\r\n    <div\r\n      v-if=\"ctx.isOpen.value\"\r\n      ref=\"content\"\r\n      class=\"bt-hover-card-content\"\r\n      role=\"dialog\"\r\n      tabindex=\"-1\"\r\n      @mouseenter=\"ctx.cancelClose\"\r\n      @mouseleave=\"ctx.scheduleClose\"\r\n    >\r\n      <slot />\r\n    </div>\r\n  </Teleport>\r\n</template>\r\n"
    },
    {
      "path": "BTHoverCardItem.vue",
      "target": "@ui/hover-card/BTHoverCardItem.vue",
      "type": "registry:ui",
      "content": "<!--\r\n  BTHoverCardItem — canonical Figma content layout.\r\n  Purely presentational: title (bold 14px) + optional description (14px) +\r\n  optional caption (10px secondary). Callers may slot any content instead.\r\n\r\n  Usage:\r\n    <BTHoverCardItem\r\n      title=\"BTech Design System\"\r\n      description=\"Component library for Vue, React & Flutter.\"\r\n      caption=\"Joined May 2026\"\r\n    />\r\n-->\r\n<script setup lang=\"ts\">\r\nimport type { BTHoverCardItemProps } from './BTHoverCard.types';\r\n\r\ndefineProps<BTHoverCardItemProps>();\r\n</script>\r\n\r\n<template>\r\n  <div class=\"bt-hover-card-item\">\r\n    <p class=\"bt-hover-card-item__title\">{{ title }}</p>\r\n    <p v-if=\"description\" class=\"bt-hover-card-item__description\">{{ description }}</p>\r\n    <p v-if=\"caption\" class=\"bt-hover-card-item__caption\">{{ caption }}</p>\r\n  </div>\r\n</template>\r\n"
    },
    {
      "path": "BTHoverCardTrigger.vue",
      "target": "@ui/hover-card/BTHoverCardTrigger.vue",
      "type": "registry:ui",
      "content": "<!--\r\n  BTHoverCardTrigger — hover wrapper.\r\n\r\n  Wraps its slot in a display:contents span (layout-transparent).\r\n  Registers itself as the trigger element in the shared context so\r\n  BTHoverCardContent knows where to position the panel.\r\n\r\n  Listens to mouseenter/mouseleave and delegates to the context timers.\r\n-->\r\n<script setup lang=\"ts\">\r\nimport { inject, useTemplateRef, onMounted, onUnmounted } from 'vue';\r\nimport type { BTHoverCardContext } from './BTHoverCard.vue';\r\n\r\nconst ctx = inject<BTHoverCardContext>('bt-hover-card')!;\r\n\r\nconst wrapperRef = useTemplateRef<HTMLElement>('wrapper');\r\n\r\nonMounted(() => {\r\n  ctx.triggerEl.value = wrapperRef.value;\r\n});\r\n\r\nonUnmounted(() => {\r\n  ctx.triggerEl.value = null;\r\n});\r\n</script>\r\n\r\n<template>\r\n  <span\r\n    ref=\"wrapper\"\r\n    class=\"bt-hover-card-trigger\"\r\n    @mouseenter=\"ctx.scheduleOpen\"\r\n    @mouseleave=\"ctx.scheduleClose\"\r\n  >\r\n    <slot />\r\n  </span>\r\n</template>\r\n"
    },
    {
      "path": "index.ts",
      "target": "@ui/hover-card/index.ts",
      "type": "registry:ui",
      "content": "export { default as BTHoverCard }        from './BTHoverCard.vue';\r\nexport { default as BTHoverCardTrigger } from './BTHoverCardTrigger.vue';\r\nexport { default as BTHoverCardContent } from './BTHoverCardContent.vue';\r\nexport { default as BTHoverCardItem }    from './BTHoverCardItem.vue';\r\nexport type {\r\n  BTHoverCardProps,\r\n  BTHoverCardTriggerProps,\r\n  BTHoverCardContentProps,\r\n  BTHoverCardItemProps,\r\n  BTHoverCardSide,\r\n  BTHoverCardAlign,\r\n} from './BTHoverCard.types';\r\n"
    },
    {
      "path": "internal/positionFloating.ts",
      "target": "@ui/hover-card/internal/positionFloating.ts",
      "type": "registry:ui",
      "content": "/**\r\n * positionFloating — shared smart-flip positioning utility.\r\n *\r\n * Measures available space around a trigger element and places a floating\r\n * panel (already rendered in the DOM at position:fixed) below or above\r\n * depending on which direction has more room. Clamps horizontally so the\r\n * panel never overflows the viewport edges.\r\n *\r\n * Usage:\r\n *   // After the panel is in the DOM (watch flush:'post' or nextTick):\r\n *   positionBelow(triggerEl, panelEl, { gap: 4, margin: 8 });\r\n *\r\n * Also exported: positionDropdown — same algorithm but centres the panel\r\n * horizontally over an anchor element (for header pill dropdowns).\r\n */\r\n\r\nexport interface PositionOptions {\r\n  /** Gap between trigger edge and panel edge. Default: 4. */\r\n  gap?: number;\r\n  /** Minimum distance from viewport edges. Default: 8. */\r\n  margin?: number;\r\n}\r\n\r\n/**\r\n * Position a panel below or above `trigger`, left-aligned (then clamped).\r\n * Mutates `panel.style.top / left` and returns the resolved direction.\r\n */\r\nexport function positionBelow(\r\n  trigger: HTMLElement,\r\n  panel: HTMLElement,\r\n  opts: PositionOptions = {},\r\n): 'below' | 'above' {\r\n  const { gap = 4, margin = 8 } = opts;\r\n  const tRect = trigger.getBoundingClientRect();\r\n  const pRect = panel.getBoundingClientRect();\r\n  const vw = window.innerWidth;\r\n  const vh = window.innerHeight;\r\n\r\n  // ── Vertical: prefer below, flip above when there's not enough space ────────\r\n  const spaceBelow = vh - tRect.bottom - gap;\r\n  const spaceAbove = tRect.top - gap;\r\n  const goAbove    = spaceBelow < pRect.height && spaceAbove >= pRect.height;\r\n\r\n  const top = goAbove\r\n    ? tRect.top  - pRect.height - gap\r\n    : tRect.bottom + gap;\r\n\r\n  // ── Horizontal: align to trigger start edge, clamp to viewport ─────────────\r\n  const isRtl = getComputedStyle(trigger).direction === 'rtl';\r\n  let left = isRtl ? tRect.right - pRect.width : tRect.left;\r\n  if (left + pRect.width > vw - margin) left = vw - margin - pRect.width;\r\n  if (left < margin)                    left = margin;\r\n\r\n  panel.style.top  = `${Math.round(top)}px`;\r\n  panel.style.left = `${Math.round(left)}px`;\r\n\r\n  return goAbove ? 'above' : 'below';\r\n}\r\n\r\nexport type PopoverSide   = 'top' | 'bottom' | 'left' | 'right';\r\nexport type PopoverAlign  = 'start' | 'center' | 'end';\r\n\r\n/**\r\n * Position a popover panel relative to a trigger with auto-flip on all axes.\r\n * Respects the requested `side` but flips to the opposite if there is no room.\r\n */\r\nexport function positionPopover(\r\n  trigger: HTMLElement,\r\n  panel: HTMLElement,\r\n  opts: { side?: PopoverSide; align?: PopoverAlign; sideOffset?: number; margin?: number } = {},\r\n): void {\r\n  const { side = 'bottom', align = 'start', sideOffset = 8, margin = 8 } = opts;\r\n  const tRect = trigger.getBoundingClientRect();\r\n  const pRect = panel.getBoundingClientRect();\r\n  const vw = window.innerWidth;\r\n  const vh = window.innerHeight;\r\n\r\n  // ── Resolve side with auto-flip ─────────────────────────────────────────────\r\n  let resolvedSide = side;\r\n  if (side === 'bottom') {\r\n    const fits = vh - tRect.bottom - sideOffset >= pRect.height;\r\n    if (!fits && tRect.top - sideOffset >= pRect.height) resolvedSide = 'top';\r\n  } else if (side === 'top') {\r\n    const fits = tRect.top - sideOffset >= pRect.height;\r\n    if (!fits && vh - tRect.bottom - sideOffset >= pRect.height) resolvedSide = 'bottom';\r\n  } else if (side === 'right') {\r\n    const fits = vw - tRect.right - sideOffset >= pRect.width;\r\n    if (!fits && tRect.left - sideOffset >= pRect.width) resolvedSide = 'left';\r\n  } else /* left */ {\r\n    const fits = tRect.left - sideOffset >= pRect.width;\r\n    if (!fits && vw - tRect.right - sideOffset >= pRect.width) resolvedSide = 'right';\r\n  }\r\n\r\n  // ── Main axis ───────────────────────────────────────────────────────────────\r\n  let top  = 0;\r\n  let left = 0;\r\n  if      (resolvedSide === 'bottom') top  = tRect.bottom + sideOffset;\r\n  else if (resolvedSide === 'top')    top  = tRect.top - pRect.height - sideOffset;\r\n  else if (resolvedSide === 'right')  left = tRect.right  + sideOffset;\r\n  else /* left */                     left = tRect.left  - pRect.width - sideOffset;\r\n\r\n  // ── Cross axis (alignment) — flip start/end in RTL ──────────────────────────\r\n  const isRtl = getComputedStyle(trigger).direction === 'rtl';\r\n  const resolvedAlign = isRtl\r\n    ? (align === 'start' ? 'end' : align === 'end' ? 'start' : align)\r\n    : align;\r\n\r\n  if (resolvedSide === 'top' || resolvedSide === 'bottom') {\r\n    if      (resolvedAlign === 'start')  left = tRect.left;\r\n    else if (resolvedAlign === 'center') left = tRect.left + tRect.width / 2 - pRect.width / 2;\r\n    else /* end */                       left = tRect.right - pRect.width;\r\n\r\n    // Clamp horizontally\r\n    left = Math.min(left, vw - margin - pRect.width);\r\n    left = Math.max(left, margin);\r\n  } else {\r\n    if      (align === 'start')  top = tRect.top;\r\n    else if (align === 'center') top = tRect.top + tRect.height / 2 - pRect.height / 2;\r\n    else /* end */               top = tRect.bottom - pRect.height;\r\n\r\n    // Clamp vertically\r\n    top = Math.min(top, vh - margin - pRect.height);\r\n    top = Math.max(top, margin);\r\n  }\r\n\r\n  panel.style.top  = `${Math.round(top)}px`;\r\n  panel.style.left = `${Math.round(left)}px`;\r\n}\r\n\r\n/**\r\n * Position a small dropdown centred horizontally over `anchor`, below or above.\r\n * Used by BTCalendarHeader month/year pills.\r\n */\r\nexport function positionCentered(\r\n  anchor: HTMLElement,\r\n  dropdown: HTMLElement,\r\n  opts: PositionOptions = {},\r\n): void {\r\n  const { gap = 4, margin = 8 } = opts;\r\n  const aRect = anchor.getBoundingClientRect();\r\n  const dRect = dropdown.getBoundingClientRect();\r\n  const vw = window.innerWidth;\r\n  const vh = window.innerHeight;\r\n\r\n  // Vertical: prefer below, flip above\r\n  const spaceBelow = vh - aRect.bottom - gap;\r\n  const spaceAbove = aRect.top - gap;\r\n  const goAbove    = spaceBelow < dRect.height && spaceAbove >= dRect.height;\r\n\r\n  const top = goAbove\r\n    ? aRect.top  - dRect.height - gap\r\n    : aRect.bottom + gap;\r\n\r\n  // Horizontal: centre over anchor, clamp\r\n  let left = aRect.left + aRect.width / 2 - dRect.width / 2;\r\n  if (left + dRect.width > vw - margin) left = vw - margin - dRect.width;\r\n  if (left < margin)                    left = margin;\r\n\r\n  dropdown.style.top  = `${Math.round(top)}px`;\r\n  dropdown.style.left = `${Math.round(left)}px`;\r\n}\r\n"
    }
  ]
}