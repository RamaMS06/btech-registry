{
  "$schema": "https://ramams06.github.io/btech-registry/schema/registry-item.json",
  "name": "chart",
  "type": "registry:ui",
  "title": "BTChart",
  "description": "Chart molecule family for Flutter. Bundles three public widgets — BTBarChart (basic / grouped / stacked), BTLineChart (line / area), and BTCircleChart (donut) — plus the BTChartColors palette and shared data models. Painted natively with custom BT tooltip + legend styling.",
  "framework": "flutter",
  "category": "molecules",
  "figmaUrl": "https://www.figma.com/design/WANr9drWYNYbMPuT2sMeHi/Btech---Design-System?node-id=3398-521",
  "figmaNodeId": "3398-521",
  "dependencies": [
    "btech_tokens",
    "fl_chart:0.67.0"
  ],
  "devDependencies": [],
  "registryDependencies": [
    "tooltip"
  ],
  "files": [
    {
      "path": "bar_chart.widget.dart",
      "target": "@flutterBase/chart/bar_chart.widget.dart",
      "type": "registry:ui",
      "content": "// BTBarChart — bar chart molecule wrapping fl_chart BarChart.\r\n//\r\n// Example usage:\r\n// ```dart\r\n// // Basic — single-series bars\r\n// BTBarChart.basic(\r\n//   data: [BTBarData(value: 80, label: 'Jan'), BTBarData(value: 60)],\r\n// )\r\n//\r\n// // Grouped — 2 series side-by-side per x position\r\n// BTBarChart.grouped(\r\n//   groups: [\r\n//     BTBarGroupData(label: 'Jan', bars: [\r\n//       BTBarItem(value: 80, color: BTChartColors.infoBold, label: 'Revenue'),\r\n//       BTBarItem(value: 60, color: BTChartColors.info,     label: 'Cost'),\r\n//     ]),\r\n//   ],\r\n// )\r\n//\r\n// // Stacked — segments bottom→top\r\n// BTBarChart.stacked(\r\n//   stackedBars: [\r\n//     BTStackedBarData(label: 'Jan', segments: [\r\n//       BTBarItem(value: 40, color: BTChartColors.info,    label: 'A'),\r\n//       BTBarItem(value: 20, color: BTChartColors.success, label: 'B'),\r\n//     ]),\r\n//   ],\r\n// )\r\n// ```\r\n//\r\n// Figma node 3398:521 · M - BarChart\r\n\r\nimport 'package:btech_tokens/btech_tokens.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/chart.types.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/internal/chart_colors.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/internal/chart_tooltip.dart';\r\nimport 'package:fl_chart/fl_chart.dart';\r\nimport 'package:flutter/material.dart';\r\n\r\n/// Bar chart molecule — supports basic, grouped, and stacked variants.\r\n///\r\n/// All visual values come from Figma node 3398:521.\r\n/// Wrap in [BTChartCard] for header, summary row, and legend.\r\n///\r\n/// Bar spacing: 8px gap between bars/groups via [BarChartAlignment.center]\r\n/// + `groupsSpace: 8`. Bar widths: 24.5px (basic/stacked), 14px per rod\r\n/// (grouped).\r\n///\r\n/// Touch / drag: press or drag across bars to see a dark BTTooltip-style\r\n/// balloon with the x-axis label and per-series legend rows. The tooltip\r\n/// follows the finger and auto-selects arrow direction from 4 zones.\r\nclass BTBarChart extends StatefulWidget {\r\n  // ── named constructor: basic ─────────────────────────────────────────────\r\n\r\n  /// Single-series bar chart. Each [BTBarData] becomes one bar (24.5px wide).\r\n  ///\r\n  /// Bar color defaults to [BTChartColors.info] (#0ea5e9).\r\n  const BTBarChart.basic({\r\n    required this.data,\r\n    this.barColor,\r\n    this.maxY,\r\n    this.height = 160,\r\n    super.key,\r\n  })  : _variant = _BTBarVariant.basic,\r\n        groups = const [],\r\n        stackedBars = const [];\r\n\r\n  // ── named constructor: grouped ───────────────────────────────────────────\r\n\r\n  /// Multi-series grouped bar chart. Each [BTBarGroupData] contains 2+ bars\r\n  /// shown side-by-side (14px per rod, 4px barsSpace, 8px between groups).\r\n  const BTBarChart.grouped({\r\n    required this.groups,\r\n    this.maxY,\r\n    this.height = 160,\r\n    super.key,\r\n  })  : _variant = _BTBarVariant.grouped,\r\n        data = const [],\r\n        stackedBars = const [],\r\n        barColor = null;\r\n\r\n  // ── named constructor: stacked ───────────────────────────────────────────\r\n\r\n  /// Stacked bar chart. Each [BTStackedBarData] renders as one bar (24.5px)\r\n  /// with colored segments stacked bottom→top.\r\n  const BTBarChart.stacked({\r\n    required this.stackedBars,\r\n    this.maxY,\r\n    this.height = 160,\r\n    super.key,\r\n  })  : _variant = _BTBarVariant.stacked,\r\n        data = const [],\r\n        groups = const [],\r\n        barColor = null;\r\n\r\n  // ── fields ───────────────────────────────────────────────────────────────\r\n\r\n  final _BTBarVariant _variant;\r\n  final List<BTBarData> data;\r\n  final List<BTBarGroupData> groups;\r\n  final List<BTStackedBarData> stackedBars;\r\n  final Color? barColor;\r\n  final double? maxY;\r\n  final double height;\r\n\r\n  @override\r\n  State<BTBarChart> createState() => _BTBarChartState();\r\n}\r\n\r\nclass _BTBarChartState extends State<BTBarChart> {\r\n  /// Finger position in Stack-local coordinates (from [Listener]).\r\n  Offset? _touchPos;\r\n\r\n  /// X-axis label for the currently touched bar group (tooltip title).\r\n  String? _touchTitle;\r\n\r\n  /// Per-series items for the currently touched bar group (tooltip rows).\r\n  List<BTChartTooltipItem>? _touchItems;\r\n\r\n  /// Entrance flag — false on first frame (bars flat at y=0), flipped true\r\n  /// after mount so fl_chart tweens every bar up from the baseline.\r\n  bool _entered = false;\r\n\r\n  // ── lifecycle ───────────────────────────────────────────────────────────────\r\n\r\n  @override\r\n  void initState() {\r\n    super.initState();\r\n    WidgetsBinding.instance.addPostFrameCallback((_) {\r\n      if (mounted) setState(() => _entered = true);\r\n    });\r\n  }\r\n\r\n  // ── build ─────────────────────────────────────────────────────────────────\r\n\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    final colors = context.btColor;\r\n\r\n    final effectiveBarColor = widget.barColor ?? BTChartColors.info;\r\n    final effectiveMaxY = widget.maxY ?? _inferMaxY();\r\n\r\n    final barGroups = switch (widget._variant) {\r\n      _BTBarVariant.basic => _buildBasicGroups(effectiveBarColor),\r\n      _BTBarVariant.grouped => _buildGroupedGroups(),\r\n      _BTBarVariant.stacked => _buildStackedGroups(),\r\n    };\r\n\r\n    final xLabels = switch (widget._variant) {\r\n      _BTBarVariant.basic =>\r\n        widget.data.map((d) => d.label ?? 'Label').toList(),\r\n      _BTBarVariant.grouped =>\r\n        widget.groups.map((g) => g.label ?? 'Label').toList(),\r\n      _BTBarVariant.stacked =>\r\n        widget.stackedBars.map((b) => b.label ?? 'Label').toList(),\r\n    };\r\n\r\n    final axisTextStyle = TextStyle(\r\n      fontFamily: BTTypography.fontFamily,\r\n      fontSize: 10.0,\r\n      fontWeight: FontWeight.w500,\r\n      color: colors.text.secondary,\r\n    );\r\n\r\n    // Capture effectiveBarColor for closure — barColor is widget field.\r\n    final capturedBarColor = effectiveBarColor;\r\n\r\n    // fl_chart's built-in tooltip is disabled; we render our own overlay.\r\n    final chart = BarChart(\r\n      BarChartData(\r\n        maxY: effectiveMaxY,\r\n        alignment: BarChartAlignment.center,\r\n        groupsSpace: 8,\r\n        barGroups: barGroups,\r\n        gridData: FlGridData(\r\n          show: true,\r\n          drawVerticalLine: false,\r\n          horizontalInterval: effectiveMaxY / 5,\r\n          getDrawingHorizontalLine: (_) => FlLine(\r\n            color: colors.border.primary,\r\n            strokeWidth: 1,\r\n          ),\r\n        ),\r\n        borderData: FlBorderData(show: false),\r\n        titlesData: FlTitlesData(\r\n          leftTitles: AxisTitles(\r\n            sideTitles: SideTitles(\r\n              showTitles: true,\r\n              reservedSize: 28,\r\n              interval: effectiveMaxY / 5,\r\n              getTitlesWidget: (value, _) => Text(\r\n                value.toInt().toString(),\r\n                style: axisTextStyle,\r\n                textAlign: TextAlign.end,\r\n              ),\r\n            ),\r\n          ),\r\n          bottomTitles: AxisTitles(\r\n            sideTitles: SideTitles(\r\n              showTitles: true,\r\n              reservedSize: 20,\r\n              getTitlesWidget: (value, _) {\r\n                final idx = value.toInt();\r\n                final label = (idx >= 0 && idx < xLabels.length)\r\n                    ? xLabels[idx]\r\n                    : '';\r\n                return Padding(\r\n                  padding: EdgeInsetsDirectional.only(top: 4.0),\r\n                  child: Text(label, style: axisTextStyle),\r\n                );\r\n              },\r\n            ),\r\n          ),\r\n          rightTitles: const AxisTitles(\r\n            sideTitles: SideTitles(showTitles: false),\r\n          ),\r\n          topTitles: const AxisTitles(\r\n            sideTitles: SideTitles(showTitles: false),\r\n          ),\r\n        ),\r\n        barTouchData: BarTouchData(\r\n          handleBuiltInTouches: false,\r\n          touchCallback: (FlTouchEvent event, BarTouchResponse? response) {\r\n            if (!mounted) return;\r\n            if (!event.isInterestedForInteractions ||\r\n                response == null ||\r\n                response.spot == null) {\r\n              if (_touchItems != null) {\r\n                setState(() {\r\n                  _touchTitle = null;\r\n                  _touchItems = null;\r\n                });\r\n              }\r\n              return;\r\n            }\r\n\r\n            final spot = response.spot!;\r\n            final groupIdx = spot.touchedBarGroupIndex;\r\n\r\n            // ── Build tooltip title from x-axis label ──────────────────\r\n            final newTitle = switch (widget._variant) {\r\n              _BTBarVariant.basic =>\r\n                (groupIdx >= 0 && groupIdx < widget.data.length)\r\n                    ? widget.data[groupIdx].label ?? 'Label'\r\n                    : 'Label',\r\n              _BTBarVariant.grouped =>\r\n                (groupIdx >= 0 && groupIdx < widget.groups.length)\r\n                    ? widget.groups[groupIdx].label ?? 'Label'\r\n                    : 'Label',\r\n              _BTBarVariant.stacked =>\r\n                (groupIdx >= 0 && groupIdx < widget.stackedBars.length)\r\n                    ? widget.stackedBars[groupIdx].label ?? 'Label'\r\n                    : 'Label',\r\n            };\r\n\r\n            // ── Build tooltip item rows from touched group ─────────────\r\n            final newItems = switch (widget._variant) {\r\n              // Basic: single item, no series label\r\n              _BTBarVariant.basic => <BTChartTooltipItem>[\r\n                  BTChartTooltipItem(\r\n                    color: capturedBarColor,\r\n                    label: '',\r\n                    value: spot.touchedRodData.toY.toStringAsFixed(0),\r\n                  ),\r\n                ],\r\n\r\n              // Grouped: one item per rod in the touched group\r\n              _BTBarVariant.grouped =>\r\n                (groupIdx >= 0 && groupIdx < widget.groups.length)\r\n                    ? widget.groups[groupIdx].bars\r\n                        .asMap()\r\n                        .entries\r\n                        .map((e) {\r\n                          // Use all rods from the group for full picture\r\n                          final rodY =\r\n                              spot.touchedBarGroup.barRods.length > e.key\r\n                                  ? spot.touchedBarGroup.barRods[e.key].toY\r\n                                  : e.value.value;\r\n                          return BTChartTooltipItem(\r\n                            color: e.value.color,\r\n                            label: e.value.label ?? '',\r\n                            value: rodY.toStringAsFixed(0),\r\n                          );\r\n                        })\r\n                        .toList()\r\n                    : <BTChartTooltipItem>[],\r\n\r\n              // Stacked: one item per segment at touched x\r\n              _BTBarVariant.stacked =>\r\n                (groupIdx >= 0 && groupIdx < widget.stackedBars.length)\r\n                    ? widget.stackedBars[groupIdx].segments\r\n                        .map(\r\n                          (seg) => BTChartTooltipItem(\r\n                            color: seg.color,\r\n                            label: seg.label ?? '',\r\n                            value: seg.value.toStringAsFixed(0),\r\n                          ),\r\n                        )\r\n                        .toList()\r\n                    : <BTChartTooltipItem>[],\r\n            };\r\n\r\n            if (_touchTitle != newTitle || _touchItems != newItems) {\r\n              setState(() {\r\n                _touchTitle = newTitle;\r\n                _touchItems = newItems;\r\n              });\r\n            }\r\n          },\r\n        ),\r\n      ),\r\n      // Entrance tween — bars grow from the baseline to their real values on\r\n      // first mount (driven by the _entered flag flip in a post-frame callback).\r\n      swapAnimationDuration: const Duration(milliseconds: 700),\r\n      swapAnimationCurve: Curves.easeOutCubic,\r\n    );\r\n\r\n    return SizedBox(\r\n      height: widget.height,\r\n      child: LayoutBuilder(\r\n        builder: (context, constraints) {\r\n          final chartWidth = constraints.maxWidth;\r\n          return Stack(\r\n            clipBehavior: Clip.none,\r\n            children: [\r\n              Listener(\r\n                onPointerDown: (e) =>\r\n                    setState(() => _touchPos = e.localPosition),\r\n                onPointerMove: (e) {\r\n                  final pos = e.localPosition;\r\n                  if (pos.dx < 0 ||\r\n                      pos.dx > chartWidth ||\r\n                      pos.dy < 0 ||\r\n                      pos.dy > widget.height) {\r\n                    setState(() {\r\n                      _touchPos = null;\r\n                      _touchTitle = null;\r\n                      _touchItems = null;\r\n                    });\r\n                  } else {\r\n                    setState(() => _touchPos = pos);\r\n                  }\r\n                },\r\n                onPointerUp: (_) => setState(() {\r\n                  _touchPos = null;\r\n                  _touchTitle = null;\r\n                  _touchItems = null;\r\n                }),\r\n                onPointerCancel: (_) => setState(() {\r\n                  _touchPos = null;\r\n                  _touchTitle = null;\r\n                  _touchItems = null;\r\n                }),\r\n                child: chart,\r\n              ),\r\n              if (_touchPos != null && _touchItems != null)\r\n                BTChartTooltipOverlay(\r\n                  title: _touchTitle,\r\n                  items: _touchItems,\r\n                  touchDx: _touchPos!.dx,\r\n                  touchDy: _touchPos!.dy,\r\n                  chartWidth: chartWidth,\r\n                  chartHeight: widget.height,\r\n                ),\r\n            ],\r\n          );\r\n        },\r\n      ),\r\n    );\r\n  }\r\n\r\n  // ── group builders ────────────────────────────────────────────────────────\r\n\r\n  static const BorderRadius _topRadius = BorderRadius.only(\r\n    topLeft: Radius.circular(2),\r\n    topRight: Radius.circular(2),\r\n  );\r\n\r\n  List<BarChartGroupData> _buildBasicGroups(Color color) {\r\n    return widget.data.asMap().entries.map((e) {\r\n      return BarChartGroupData(\r\n        x: e.key,\r\n        barRods: [\r\n          BarChartRodData(\r\n            toY: _entered ? e.value.value : 0,\r\n            color: color,\r\n            width: 24.5,\r\n            borderRadius: _topRadius,\r\n          ),\r\n        ],\r\n      );\r\n    }).toList();\r\n  }\r\n\r\n  List<BarChartGroupData> _buildGroupedGroups() {\r\n    return widget.groups.asMap().entries.map((e) {\r\n      return BarChartGroupData(\r\n        x: e.key,\r\n        barsSpace: 4,\r\n        barRods: e.value.bars.map((b) {\r\n          return BarChartRodData(\r\n            toY: _entered ? b.value : 0,\r\n            color: b.color,\r\n            width: 14,\r\n            borderRadius: _topRadius,\r\n          );\r\n        }).toList(),\r\n      );\r\n    }).toList();\r\n  }\r\n\r\n  List<BarChartGroupData> _buildStackedGroups() {\r\n    return widget.stackedBars.asMap().entries.map((e) {\r\n      final bar = e.value;\r\n      final total =\r\n          bar.segments.fold<double>(0, (sum, s) => sum + s.value);\r\n      var runningY = 0.0;\r\n      final rodItems = bar.segments.map((s) {\r\n        // Collapse every segment to a zero-height slice pre-entrance so the\r\n        // tween grows each stacked segment up from the baseline.\r\n        final item = _entered\r\n            ? BarChartRodStackItem(runningY, runningY + s.value, s.color)\r\n            : BarChartRodStackItem(0, 0, s.color);\r\n        if (_entered) runningY += s.value;\r\n        return item;\r\n      }).toList();\r\n\r\n      return BarChartGroupData(\r\n        x: e.key,\r\n        barRods: [\r\n          BarChartRodData(\r\n            toY: _entered ? total : 0,\r\n            rodStackItems: rodItems,\r\n            width: 24.5,\r\n            borderRadius: _topRadius,\r\n            color: Colors.transparent,\r\n          ),\r\n        ],\r\n      );\r\n    }).toList();\r\n  }\r\n\r\n  // ── helpers ───────────────────────────────────────────────────────────────\r\n\r\n  double _inferMaxY() {\r\n    var max = 0.0;\r\n    switch (widget._variant) {\r\n      case _BTBarVariant.basic:\r\n        for (final d in widget.data) {\r\n          if (d.value > max) max = d.value;\r\n        }\r\n      case _BTBarVariant.grouped:\r\n        for (final g in widget.groups) {\r\n          for (final b in g.bars) {\r\n            if (b.value > max) max = b.value;\r\n          }\r\n        }\r\n      case _BTBarVariant.stacked:\r\n        for (final bar in widget.stackedBars) {\r\n          final total =\r\n              bar.segments.fold<double>(0, (s, seg) => s + seg.value);\r\n          if (total > max) max = total;\r\n        }\r\n    }\r\n    if (max == 0) return 100;\r\n    return ((max / 20).ceil() * 20).toDouble();\r\n  }\r\n}\r\n\r\nenum _BTBarVariant { basic, grouped, stacked }\r\n"
    },
    {
      "path": "chart.dart",
      "target": "@flutterBase/chart/chart.dart",
      "type": "registry:ui",
      "content": "// Chart molecule family barrel.\r\n//\r\n// Exports: BTBarChart, BTLineChart, BTCircleChart, all data models,\r\n// and BTChartColors palette for seeding chart data.\r\n// Internal layout helpers (BTChartLegend) are NOT re-exported.\r\nexport 'package:btech_ui/src/components/molecules/chart/bar_chart.widget.dart';\r\nexport 'package:btech_ui/src/components/molecules/chart/chart.types.dart';\r\nexport 'package:btech_ui/src/components/molecules/chart/circle_chart.widget.dart';\r\nexport 'package:btech_ui/src/components/molecules/chart/internal/chart_colors.dart';\r\nexport 'package:btech_ui/src/components/molecules/chart/line_chart.widget.dart';\r\n"
    },
    {
      "path": "chart.types.dart",
      "target": "@flutterBase/chart/chart.types.dart",
      "type": "registry:ui",
      "content": "// BTChart data models — shared across all chart widget types.\r\n//\r\n// Consumers build their data with these plain classes and pass them\r\n// to BTBarChart / BTLineChart / BTCircleChart / BTChartCard.\r\n// fl_chart types are never exposed publicly.\r\n\r\nimport 'package:flutter/painting.dart';\r\n\r\n// ── Bar Chart ──────────────────────────────────────────────────────────────\r\n\r\n/// A single data point for [BTBarChart.basic].\r\nclass BTBarData {\r\n  const BTBarData({\r\n    required this.value,\r\n    this.label,\r\n  });\r\n\r\n  /// X-axis label shown below the bar. Defaults to 'Label' if null.\r\n  final String? label;\r\n\r\n  /// Bar height value.\r\n  final double value;\r\n}\r\n\r\n/// One bar in a grouped [BTBarChart.grouped] group, or one segment in a\r\n/// stacked [BTBarChart.stacked] bar.\r\nclass BTBarItem {\r\n  const BTBarItem({\r\n    required this.value,\r\n    required this.color,\r\n    this.label,\r\n  });\r\n\r\n  final double value;\r\n  final Color color;\r\n\r\n  /// Optional series name shown in the tooltip legend row.\r\n  final String? label;\r\n}\r\n\r\n/// A group of bars in [BTBarChart.grouped] (shown side-by-side).\r\nclass BTBarGroupData {\r\n  const BTBarGroupData({\r\n    required this.bars,\r\n    this.label,\r\n  });\r\n\r\n  /// X-axis label for this group.\r\n  final String? label;\r\n\r\n  /// The individual bars in this group (≥1). Each has a value + color.\r\n  final List<BTBarItem> bars;\r\n}\r\n\r\n/// One position in [BTBarChart.stacked] (multiple stacked segments).\r\nclass BTStackedBarData {\r\n  const BTStackedBarData({\r\n    required this.segments,\r\n    this.label,\r\n  });\r\n\r\n  /// X-axis label.\r\n  final String? label;\r\n\r\n  /// Segments bottom→top. Each has a value + color.\r\n  final List<BTBarItem> segments;\r\n}\r\n\r\n// ── Line Chart ─────────────────────────────────────────────────────────────\r\n\r\n/// One (x, y) coordinate in a line chart series.\r\nclass BTLineSpot {\r\n  const BTLineSpot(this.x, this.y);\r\n\r\n  final double x;\r\n  final double y;\r\n}\r\n\r\n/// A single line series in [BTLineChart].\r\nclass BTLineSeries {\r\n  const BTLineSeries({\r\n    required this.spots,\r\n    required this.color,\r\n    this.label,\r\n  });\r\n\r\n  /// Legend label for this series.\r\n  final String? label;\r\n\r\n  /// Ordered list of (x, y) data points.\r\n  final List<BTLineSpot> spots;\r\n\r\n  /// Line and fill color.\r\n  final Color color;\r\n}\r\n\r\n// ── Circle Chart ───────────────────────────────────────────────────────────\r\n\r\n/// One slice in [BTCircleChart].\r\nclass BTCircleSection {\r\n  const BTCircleSection({\r\n    required this.value,\r\n    required this.color,\r\n    this.label,\r\n  });\r\n\r\n  /// Label shown outside the circle near this slice.\r\n  final String? label;\r\n\r\n  /// Proportional value (raw — the widget computes percentages internally).\r\n  final double value;\r\n\r\n  final Color color;\r\n}\r\n\r\n// ── Tooltip ────────────────────────────────────────────────────────────────\r\n\r\n/// One row in a [BTChartTooltipOverlay] balloon.\r\n///\r\n/// Each item renders as:\r\n///   [12×12 color square]  [label, flex-1]  [value, bold]\r\nclass BTChartTooltipItem {\r\n  const BTChartTooltipItem({\r\n    required this.color,\r\n    required this.label,\r\n    required this.value,\r\n  });\r\n\r\n  /// Legend color swatch (12×12, borderRadius 2).\r\n  final Color color;\r\n\r\n  /// Series name — pass empty string to hide label column.\r\n  final String label;\r\n\r\n  /// Formatted value string (e.g. '42', '38%').\r\n  final String value;\r\n}\r\n\r\n// ── Chart Card ─────────────────────────────────────────────────────────────\r\n\r\n/// One item in the summary row of [BTChartCard].\r\nclass BTChartSummaryItem {\r\n  const BTChartSummaryItem({\r\n    required this.label,\r\n    required this.value,\r\n    this.unit,\r\n  });\r\n\r\n  /// Small label above the value (e.g. 'Total Sales').\r\n  final String label;\r\n\r\n  /// Bold value (e.g. '1,200').\r\n  final String value;\r\n\r\n  /// Optional unit suffix in small text (e.g. '%', 'kg').\r\n  final String? unit;\r\n}\r\n\r\n/// One item in the legend row of [BTChartCard].\r\nclass BTChartLegendItem {\r\n  const BTChartLegendItem({\r\n    required this.label,\r\n    required this.color,\r\n  });\r\n\r\n  final String label;\r\n  final Color color;\r\n}\r\n"
    },
    {
      "path": "circle_chart.widget.dart",
      "target": "@flutterBase/chart/circle_chart.widget.dart",
      "type": "registry:ui",
      "content": "// BTCircleChart — pie / donut / donut-rounded chart molecule.\r\n//\r\n// Example usage:\r\n// ```dart\r\n// // Pie\r\n// BTCircleChart.pie(\r\n//   sections: [\r\n//     BTCircleSection(value: 40, color: BTChartColors.info, label: 'A'),\r\n//     BTCircleSection(value: 60, color: BTChartColors.success, label: 'B'),\r\n//   ],\r\n// )\r\n//\r\n// // Donut (large center hole)\r\n// BTCircleChart.donut(sections: [...])\r\n//\r\n// // Donut with gaps + rounded ends\r\n// BTCircleChart.donutRounded(sections: [...])\r\n// ```\r\n//\r\n// Figma node 3404:1813 · M - CircleChart\r\n\r\n// ignore_for_file: lines_longer_than_80_chars\r\n\r\nimport 'dart:math' as math;\r\n\r\nimport 'package:btech_tokens/btech_tokens.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/chart.types.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/internal/chart_tooltip.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/internal/circle_chart_labels.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/internal/circle_chart_painter.dart';\r\nimport 'package:fl_chart/fl_chart.dart';\r\nimport 'package:flutter/material.dart';\r\n\r\n/// Circle chart molecule — pie, donut, and gapped-donut variants.\r\n///\r\n/// All visual values come from Figma node 3404:1813.\r\n/// Every variant shows external badge labels (Label + %) connected to their\r\n/// segment by a short radial line. Tapping a segment shows a BTTooltip-style\r\n/// balloon overlay for all variants.\r\n///\r\n/// ## Radius layout (per variant, in a [size]×[size] widget)\r\n///\r\n/// All three variants share the same true outer radius of 70 px (default\r\n/// [size] 178) so connector lines and badge labels align identically.\r\n/// The difference is only in the center-hole size and ring thickness:\r\n///\r\n/// | Variant       | Center hole | Ring | True outer |\r\n/// |---------------|-------------|------|-----------|\r\n/// | pie           | 0 px        | 70   | 70        |\r\n/// | donut         | 36 px       | 34   | 70        |\r\n/// | donutRounded  | 40 px       | 30   | 70        |\r\n///\r\n/// fl_chart note: [PieChartSectionData.radius] is the **ring thickness**\r\n/// (distance from the inner hole edge to the outer edge), NOT the outer radius.\r\n/// The true outer radius = centerSpaceRadius + section.radius.\r\nclass BTCircleChart extends StatefulWidget {\r\n  // ── named constructor: pie ───────────────────────────────────────────────\r\n\r\n  /// Full pie chart (no center hole, no gaps, no rounded ends).\r\n  const BTCircleChart.pie({\r\n    required this.sections,\r\n    this.size = 178,\r\n    super.key,\r\n  })  : _innerRadius = 0,\r\n        _sectionsSpace = 0;\r\n\r\n  // ── named constructor: donut ─────────────────────────────────────────────\r\n\r\n  /// Donut chart — large center hole, no gaps, no rounded ends.\r\n  const BTCircleChart.donut({\r\n    required this.sections,\r\n    this.size = 178,\r\n    super.key,\r\n  })  : _innerRadius = 36,\r\n        _sectionsSpace = 0;\r\n\r\n  // ── named constructor: donutRounded ──────────────────────────────────────\r\n\r\n  /// Donut chart — 4 px gaps between segments, rounded ends.\r\n  ///\r\n  /// Visual rendering uses a [BTDonutRoundedPainter] ([StrokeCap.round]) for\r\n  /// the colored arcs. fl_chart is used (with transparent sections) only for\r\n  /// touch detection and badge-label positioning.\r\n  const BTCircleChart.donutRounded({\r\n    required this.sections,\r\n    this.size = 178,\r\n    super.key,\r\n  })  : _innerRadius = 40,\r\n        _sectionsSpace = 4;\r\n\r\n  // ── fields ────────────────────────────────────────────────────────────────\r\n\r\n  final List<BTCircleSection> sections;\r\n  final double size;\r\n  final double _innerRadius;\r\n  final double _sectionsSpace;\r\n\r\n  @override\r\n  State<BTCircleChart> createState() => _BTCircleChartState();\r\n}\r\n\r\nclass _BTCircleChartState extends State<BTCircleChart>\r\n    with SingleTickerProviderStateMixin {\r\n  /// Index of the currently tapped section, or -1 for none.\r\n  int _touchedIndex = -1;\r\n\r\n  /// Touch position in local Stack coordinates — used for tooltip placement.\r\n  Offset? _touchPos;\r\n\r\n  /// Entrance animation — a single clockwise \"hand\" sweeps from 12 o'clock so\r\n  /// the chart draws itself in on first mount (mirrors the web BTCircleChart\r\n  /// reveal and the Recharts pie). Drives a [_CircleSweepClipper].\r\n  late final AnimationController _enterController = AnimationController(\r\n    vsync: this,\r\n    duration: const Duration(milliseconds: 700),\r\n  );\r\n  late final Animation<double> _enter = CurvedAnimation(\r\n    parent: _enterController,\r\n    curve: Curves.easeOutCubic,\r\n  );\r\n\r\n  @override\r\n  void initState() {\r\n    super.initState();\r\n    _enterController.forward();\r\n  }\r\n\r\n  @override\r\n  void dispose() {\r\n    _enterController.dispose();\r\n    super.dispose();\r\n  }\r\n\r\n  // ── radius helpers ────────────────────────────────────────────────────────\r\n\r\n  /// Ring thickness (= [PieChartSectionData.radius] in fl_chart).\r\n  ///\r\n  /// fl_chart draws the ring from [_innerRadius] to\r\n  /// [_innerRadius] + [_ringThickness] — the outer edge is at\r\n  /// [_trueOuterRadius].\r\n  double get _ringThickness {\r\n    if (widget._innerRadius == 0) return 70; // pie: ring = outer = 70\r\n    if (widget._sectionsSpace > 0) return 30; // donutRounded: outer = 70\r\n    return 34; // donut: outer = 70\r\n  }\r\n\r\n  /// True outer radius of the visible section edge (center hole + ring).\r\n  /// All three variants equal 70 px at the default [size] of 178.\r\n  double get _trueOuterRadius => widget._innerRadius + _ringThickness;\r\n\r\n  /// Geometric anchor (mid-arc of section [index], at the ring outer edge) used\r\n  /// to place the tooltip when no live pointer position is available — e.g.\r\n  /// after a parent scrollable wins the drag and cancels pointer delivery while\r\n  /// fl_chart still reports the section as touched.\r\n  Offset _segmentAnchor(int index, double total) {\r\n    final center = Offset(widget.size / 2, widget.size / 2);\r\n    if (total <= 0) return center;\r\n    var angle = -90.0; // 12 o'clock, matches startDegreeOffset\r\n    for (var i = 0; i < index; i++) {\r\n      angle += widget.sections[i].value / total * 360;\r\n    }\r\n    angle += widget.sections[index].value / total * 360 / 2;\r\n    final rad = angle * math.pi / 180;\r\n    return center + Offset(math.cos(rad), math.sin(rad)) * _trueOuterRadius;\r\n  }\r\n\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    final c = context.btColor;\r\n    final total = widget.sections.fold<double>(0, (s, sec) => s + sec.value);\r\n\r\n    final ringThickness = _ringThickness;\r\n    final trueOuterRadius = _trueOuterRadius; // 70 px for all variants\r\n\r\n    // Badge offset: places label 30 px beyond the section outer edge.\r\n    // Formula derivation:\r\n    //   badge_center = centerSpace + ringThickness × offset\r\n    //   We want badge_center = trueOuter + 30 = centerSpace + ringThickness + 30\r\n    //   ⟹  offset = 1 + 30 / ringThickness\r\n    //\r\n    // The connector line ends at trueOuter + 13 (gap 3 + length 10).\r\n    // With two 10 px text lines (~13 px line-height each, 26 px total height),\r\n    // badge top = trueOuter + 30 − 13 = trueOuter + 17 → 4 px gap from line tip.\r\n    final badgeOffset = 1.0 + 30.0 / ringThickness;\r\n\r\n    // True for both Donut and DonutRounded — colored arcs drawn by\r\n    // BTDonutRoundedPainter; fl_chart sections become touch/badge targets only.\r\n    final isDonutVariant = widget._innerRadius > 0;\r\n    final isDonutRounded = widget._sectionsSpace > 0;\r\n\r\n    final pieSections = widget.sections.asMap().entries.map((entry) {\r\n      final isTouched = entry.key == _touchedIndex;\r\n      final sec = entry.value;\r\n      final pct = total > 0 ? sec.value / total * 100 : 0.0;\r\n\r\n      return PieChartSectionData(\r\n        value: sec.value,\r\n        // Donut / DonutRounded: transparent — colored arcs drawn by\r\n        // BTDonutRoundedPainter. Pie: use section color.\r\n        color: isDonutVariant ? Colors.transparent : sec.color,\r\n        // section.radius = ring thickness (NOT outer radius from center).\r\n        radius: isTouched ? ringThickness + 8 : ringThickness,\r\n        borderSide: const BorderSide(color: Colors.transparent, width: 0),\r\n        showTitle: false,\r\n        badgeWidget: BTCirclePieLabel(\r\n          label: sec.label ?? '',\r\n          value: '${pct.toStringAsFixed(0)}%',\r\n          textColor: c.text.secondary,\r\n          boldTextColor: c.text.primary,\r\n        ),\r\n        badgePositionPercentageOffset: badgeOffset,\r\n      );\r\n    }).toList();\r\n\r\n    final chart = PieChart(\r\n      PieChartData(\r\n        sections: pieSections,\r\n        centerSpaceRadius: widget._innerRadius,\r\n        // Always 0 — for donutRounded the visual gap is drawn by\r\n        // BTDonutRoundedPainter so fl_chart badge positions are unaffected.\r\n        sectionsSpace: 0,\r\n        startDegreeOffset: -90,\r\n        pieTouchData: PieTouchData(\r\n          touchCallback: (FlTouchEvent event, PieTouchResponse? response) {\r\n            setState(() {\r\n              if (!event.isInterestedForInteractions ||\r\n                  response == null ||\r\n                  response.touchedSection == null) {\r\n                _touchedIndex = -1;\r\n                _touchPos = null;\r\n                return;\r\n              }\r\n              _touchedIndex = response.touchedSection!.touchedSectionIndex;\r\n              // Source the tooltip position from fl_chart's OWN touch event so\r\n              // it stays in lock-step with [_touchedIndex]. A previous external\r\n              // Listener was cancelled (PointerCancel) when a parent scrollable\r\n              // won the drag arena — that nulled the position while fl_chart\r\n              // kept reporting the touched section, so the segment stayed\r\n              // popped-out but the tooltip vanished mid-drag.\r\n              _touchPos = event.localPosition;\r\n            });\r\n          },\r\n        ),\r\n      ),\r\n    );\r\n\r\n    // Tooltip value: \"Label XX%\" or \"XX%\" when label is absent.\r\n    final section = _touchedIndex >= 0 && _touchedIndex < widget.sections.length\r\n        ? widget.sections[_touchedIndex]\r\n        : null;\r\n    final pct =\r\n        section != null && total > 0 ? section.value / total * 100 : 0.0;\r\n    final tooltipValue = section != null\r\n        ? (section.label != null && section.label!.isNotEmpty\r\n            ? '${section.label} ${pct.toStringAsFixed(0)}%'\r\n            : '${pct.toStringAsFixed(0)}%')\r\n        : null;\r\n\r\n    return AnimatedBuilder(\r\n      animation: _enter,\r\n      builder: (context, child) {\r\n        // Once swept in, drop the clip entirely so external badge labels\r\n        // (which overflow the SizedBox via Clip.none) are never cut.\r\n        if (_enterController.isCompleted) return child!;\r\n        return ClipPath(\r\n          clipper: _CircleSweepClipper(_enter.value),\r\n          child: child,\r\n        );\r\n      },\r\n      child: SizedBox(\r\n        width: widget.size,\r\n        height: widget.size,\r\n        child: Stack(\r\n          clipBehavior: Clip.none,\r\n          alignment: Alignment.center,\r\n          children: [\r\n            // ① Radial connector lines — behind chart, non-interactive.\r\n            //   Uses trueOuterRadius (= centerSpaceRadius + ringThickness)\r\n            //   so lines start at the ACTUAL outer edge of the chart ring.\r\n            IgnorePointer(\r\n              child: CustomPaint(\r\n                size: Size(widget.size, widget.size),\r\n                painter: BTCircleConnectorPainter(\r\n                  sections: widget.sections,\r\n                  total: total,\r\n                  outerRadius: trueOuterRadius,\r\n                  color: c.text.secondary,\r\n                ),\r\n              ),\r\n            ),\r\n\r\n            // ② Donut + DonutRounded: colored arcs via BTDonutRoundedPainter.\r\n            //   • Donut:        StrokeCap.butt, no gap, no border — eliminates\r\n            //                   the sub-pixel anti-aliasing gaps fl_chart leaves\r\n            //                   at section boundaries.\r\n            //   • DonutRounded: StrokeCap.round, 4 px gap, 2 px white border.\r\n            //   Drawn above connector lines but below the fl_chart badge labels.\r\n            if (isDonutVariant)\r\n              IgnorePointer(\r\n                child: CustomPaint(\r\n                  size: Size(widget.size, widget.size),\r\n                  painter: BTDonutRoundedPainter(\r\n                    sections: widget.sections,\r\n                    total: total,\r\n                    innerRadius: widget._innerRadius,\r\n                    outerRadius: trueOuterRadius,\r\n                    touchedIndex: _touchedIndex,\r\n                    sectionsGap: widget._sectionsSpace,\r\n                    cap: isDonutRounded ? StrokeCap.round : StrokeCap.butt,\r\n                    borderWidth: isDonutRounded ? 2.0 : 0.0,\r\n                    borderColor: c.bg.primary,\r\n                  ),\r\n                ),\r\n              ),\r\n\r\n            // ③ Chart widget — fl_chart renders sections (transparent for\r\n            //   donutRounded) + badge labels, and drives touch events.\r\n            //   Touch position + index both come from PieTouchData.touchCallback\r\n            //   (see above) so they never diverge, even when a parent scrollable\r\n            //   wins the drag gesture.\r\n            chart,\r\n\r\n            // ④ Floating tooltip balloon — title-only (simple mode).\r\n            //   Shown whenever a section is touched ([tooltipValue] != null).\r\n            //   Anchored at the live finger position when available, otherwise at\r\n            //   the touched section's geometry — so the tooltip never disappears\r\n            //   while a segment stays highlighted (e.g. during a scroll-competing\r\n            //   drag where pointer delivery is cancelled).\r\n            if (tooltipValue != null)\r\n              Builder(\r\n                builder: (_) {\r\n                  final anchor =\r\n                      _touchPos ?? _segmentAnchor(_touchedIndex, total);\r\n                  return BTChartTooltipOverlay(\r\n                    title: tooltipValue,\r\n                    touchDx: anchor.dx,\r\n                    touchDy: anchor.dy,\r\n                    chartWidth: widget.size,\r\n                    chartHeight: widget.size,\r\n                  );\r\n                },\r\n              ),\r\n          ],\r\n        ),\r\n      ),\r\n    );\r\n  }\r\n}\r\n\r\n/// Clip that reveals its child with a clockwise \"hand\" sweeping from 12\r\n/// o'clock — drives the [BTCircleChart] entrance. [progress] runs 0→1.\r\nclass _CircleSweepClipper extends CustomClipper<Path> {\r\n  const _CircleSweepClipper(this.progress);\r\n\r\n  final double progress;\r\n\r\n  @override\r\n  Path getClip(Size size) {\r\n    final p = progress.clamp(0.0, 1.0);\r\n    final center = Offset(size.width / 2, size.height / 2);\r\n    if (p >= 1.0) {\r\n      // Generous rect (covers overflowing badges) — full reveal.\r\n      return Path()\r\n        ..addRect(Rect.fromCircle(center: center, radius: size.longestSide));\r\n    }\r\n    // Radius large enough to cover the chart ring AND its external badges.\r\n    final radius = size.longestSide;\r\n    return Path()\r\n      ..moveTo(center.dx, center.dy)\r\n      ..arcTo(\r\n        Rect.fromCircle(center: center, radius: radius),\r\n        -math.pi / 2, // 12 o'clock — matches startDegreeOffset: -90\r\n        2 * math.pi * p,\r\n        false,\r\n      )\r\n      ..close();\r\n  }\r\n\r\n  @override\r\n  bool shouldReclip(_CircleSweepClipper oldClipper) =>\r\n      oldClipper.progress != progress;\r\n}\r\n"
    },
    {
      "path": "internal/chart_colors.dart",
      "target": "@flutterBase/chart/internal/chart_colors.dart",
      "type": "registry:ui",
      "content": "// INTERNAL — do NOT re-export from chart.dart.\r\n// Hardcoded per CLAUDE.md §2: these colors appear only in chart components.\r\n\r\nimport 'package:flutter/painting.dart';\r\n\r\nabstract final class BTChartColors {\r\n  /// Default info blue (single-series bars, first line).\r\n  static const Color info = Color(0xFF0EA5E9);\r\n\r\n  /// Darker info (second bar in grouped variant).\r\n  static const Color infoBold = Color(0xFF0F81B4);\r\n\r\n  /// Success green — stacked segment 2, second line.\r\n  static const Color success = Color(0xFF08A94C);\r\n\r\n  /// Warning yellow — stacked segment 3, third line.\r\n  static const Color warning = Color(0xFFEEC513);\r\n\r\n  /// Amber orange — stacked segment 4, fourth line (brand.secondary tone).\r\n  static const Color amber = Color(0xFFF59E0B);\r\n\r\n  /// Error red — stacked segment 5, fifth line.\r\n  static const Color error = Color(0xFFEF4444);\r\n\r\n  /// Default 5-series palette in display order (Figma legend order).\r\n  static const List<Color> defaultPalette = [\r\n    info, success, warning, amber, error,\r\n  ];\r\n\r\n  /// Default grouped-bar palette (2 series: infoBold first, info second).\r\n  static const List<Color> groupedPalette = [infoBold, info];\r\n\r\n  /// Default stacked-bar palette bottom→top order.\r\n  static const List<Color> stackedPalette = [\r\n    info, success, warning, amber, error,\r\n  ];\r\n}\r\n"
    },
    {
      "path": "internal/chart_legend.widget.dart",
      "target": "@flutterBase/chart/internal/chart_legend.widget.dart",
      "type": "registry:ui",
      "content": "// BTChartLegend — internal legend row widget used by BTChartCard.\r\n// NOT exported from chart.dart.\r\n\r\nimport 'package:btech_tokens/btech_tokens.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/chart.types.dart';\r\nimport 'package:flutter/material.dart';\r\n\r\n/// Internal legend row — colored square dots + label text.\r\n///\r\n/// Figma: legend in BTChartCard node 3404:2299.\r\n/// 12×12px squares, borderRadius 2px, right-aligned Wrap.\r\nclass BTChartLegend extends StatelessWidget {\r\n  const BTChartLegend({required this.items, super.key});\r\n\r\n  final List<BTChartLegendItem> items;\r\n\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    final textColor = context.btColor.text.primary;\r\n\r\n    return Wrap(\r\n      spacing: 8.0,\r\n      runSpacing: 8.0,\r\n      alignment: WrapAlignment.end,\r\n      children: items.map((item) {\r\n        return Row(\r\n          mainAxisSize: MainAxisSize.min,\r\n          children: [\r\n            Container(\r\n              width: 12.0,\r\n              height: 12.0,\r\n              decoration: BoxDecoration(\r\n                color: item.color,\r\n                borderRadius: BorderRadius.circular(2.0),\r\n              ),\r\n            ),\r\n            SizedBox(width: 4.0),\r\n            Text(\r\n              item.label,\r\n              style: TextStyle(\r\n                fontFamily: BTTypography.fontFamily,\r\n                fontSize: 12.0,\r\n                fontWeight: FontWeight.w500,\r\n                color: textColor,\r\n              ),\r\n            ),\r\n          ],\r\n        );\r\n      }).toList(),\r\n    );\r\n  }\r\n}\r\n"
    },
    {
      "path": "internal/chart_tooltip.dart",
      "target": "@flutterBase/chart/internal/chart_tooltip.dart",
      "type": "registry:ui",
      "content": "// ignore_for_file: lines_longer_than_80_chars\r\n\r\nimport 'package:btech_tokens/btech_tokens.dart';\r\nimport '../../tooltip/internal/tooltip.arrow.dart';\r\nimport '../../tooltip/tooltip.types.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/chart.types.dart';\r\nimport 'package:flutter/material.dart';\r\n\r\n/// Internal chart tooltip overlay — dark balloon + directional arrow.\r\n///\r\n/// Supports two display modes:\r\n///\r\n/// **Simple** (circle chart): pass [title] only.\r\n/// ```dart\r\n/// BTChartTooltipOverlay(title: 'A 40%', touchDx: ..., ...)\r\n/// ```\r\n///\r\n/// **Rich** (bar / line chart): pass [title] + [items].\r\n/// ```dart\r\n/// BTChartTooltipOverlay(\r\n///   title: 'Jan',\r\n///   items: [\r\n///     BTChartTooltipItem(color: BTChartColors.info,    label: 'Revenue', value: '80'),\r\n///     BTChartTooltipItem(color: BTChartColors.success, label: 'Cost',    value: '60'),\r\n///   ],\r\n///   touchDx: ..., touchDy: ..., chartWidth: ..., chartHeight: ...,\r\n/// )\r\n/// ```\r\n///\r\n/// Direction is chosen automatically from the touch position:\r\n///   touchDy < 25% chart height → ABOVE (arrow at bottom, points down)\r\n///   touchDy > 75% chart height → BELOW (arrow at top, points up)\r\n///   middle + left half         → LEFT  (arrow at right, points right toward bar)\r\n///   middle + right half        → RIGHT (arrow at left, points left toward bar)\r\nclass BTChartTooltipOverlay extends StatelessWidget {\r\n  const BTChartTooltipOverlay({\r\n    required this.touchDx,\r\n    required this.touchDy,\r\n    required this.chartWidth,\r\n    required this.chartHeight,\r\n    this.title,\r\n    this.items,\r\n    super.key,\r\n  });\r\n\r\n  /// Optional headline shown at the top of the balloon (centered).\r\n  /// For simple tooltips (e.g. circle chart) this is the only content.\r\n  final String? title;\r\n\r\n  /// Optional list of legend rows shown below the separator.\r\n  /// When non-null and non-empty, a divider is drawn between title and rows.\r\n  final List<BTChartTooltipItem>? items;\r\n\r\n  /// Horizontal touch position in Stack-local coordinates.\r\n  final double touchDx;\r\n\r\n  /// Vertical touch position in Stack-local coordinates.\r\n  final double touchDy;\r\n\r\n  /// Full width of the chart widget (from LayoutBuilder).\r\n  final double chartWidth;\r\n\r\n  /// Full height of the chart widget.\r\n  final double chartHeight;\r\n\r\n  // Gap between the arrow tip and the touch point (matches BTTooltip._gap).\r\n  static const double _gap = 6;\r\n\r\n  // Arrow short dimension: 8px (matches BTTooltipArrow's isVertical sizing).\r\n  static const double _arrowShort = 8;\r\n\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    final dir = _resolveDirection(context);\r\n    final c = context.btColor;\r\n    final bg = c.bg.inverse;\r\n    final textColor = c.text.inverse;\r\n\r\n    final balloon = _BTChartBalloon(\r\n      title: title,\r\n      items: items,\r\n      bg: bg,\r\n      textColor: textColor,\r\n    );\r\n\r\n    // Reuse the same BTTooltipArrow used by BTTooltip — identical appearance.\r\n    final arrow = BTTooltipArrow(\r\n      direction: _btPos(dir),\r\n      color: bg,\r\n    );\r\n\r\n    Widget tooltip;\r\n    Offset translation;\r\n    double left;\r\n    double top;\r\n\r\n    switch (dir) {\r\n      // ── Balloon ABOVE touch, arrow points DOWN toward bar ─────────────────\r\n      case _Dir.above:\r\n        tooltip = Column(\r\n          mainAxisSize: MainAxisSize.min,\r\n          crossAxisAlignment: CrossAxisAlignment.center,\r\n          children: [balloon, arrow],\r\n        );\r\n        left = touchDx;\r\n        top = touchDy - _gap;\r\n        translation = const Offset(-0.5, -1);\r\n\r\n      // ── Balloon BELOW touch, arrow points UP toward touch ─────────────────\r\n      case _Dir.below:\r\n        tooltip = Column(\r\n          mainAxisSize: MainAxisSize.min,\r\n          crossAxisAlignment: CrossAxisAlignment.center,\r\n          children: [arrow, balloon],\r\n        );\r\n        left = touchDx;\r\n        top = touchDy + _gap;\r\n        translation = const Offset(-0.5, 0);\r\n\r\n      // ── Balloon to the RIGHT, arrow points LEFT toward bar ────────────────\r\n      case _Dir.right:\r\n        tooltip = Row(\r\n          // Force physical LTR order: arrow/balloon sides are computed from the\r\n          // physical touchDx, but a bare Row would re-flip them under RTL while\r\n          // the canvas-painted arrow keeps its direction — pointing the caret\r\n          // into the balloon. The balloon's own text stays ambient direction.\r\n          textDirection: TextDirection.ltr,\r\n          mainAxisSize: MainAxisSize.min,\r\n          crossAxisAlignment: CrossAxisAlignment.center,\r\n          children: [arrow, balloon],\r\n        );\r\n        left = touchDx + _gap;\r\n        top = touchDy;\r\n        translation = const Offset(0, -0.5);\r\n\r\n      // ── Balloon to the LEFT, arrow points RIGHT toward bar ────────────────\r\n      case _Dir.left:\r\n        tooltip = Row(\r\n          textDirection: TextDirection.ltr,\r\n          mainAxisSize: MainAxisSize.min,\r\n          crossAxisAlignment: CrossAxisAlignment.center,\r\n          children: [balloon, arrow],\r\n        );\r\n        left = touchDx - _gap - _arrowShort;\r\n        top = touchDy;\r\n        translation = const Offset(-1, -0.5);\r\n    }\r\n\r\n    return Positioned(\r\n      left: left,\r\n      top: top,\r\n      child: FractionalTranslation(\r\n        translation: translation,\r\n        child: tooltip,\r\n      ),\r\n    );\r\n  }\r\n\r\n  _Dir _resolveDirection(BuildContext context) {\r\n    final relY = touchDy / chartHeight;\r\n    if (relY < 0.25) return _Dir.above;\r\n    if (relY > 0.75) return _Dir.below;\r\n    final isRtl = Directionality.of(context) == TextDirection.rtl;\r\n    final onLeft = touchDx < chartWidth * 0.5;\r\n    // In RTL the chart is visually mirrored so flip left/right balloon side.\r\n    return (onLeft != isRtl) ? _Dir.left : _Dir.right;\r\n  }\r\n\r\n  static BTTooltipPosition _btPos(_Dir dir) => switch (dir) {\r\n        _Dir.above => BTTooltipPosition.top,\r\n        _Dir.below => BTTooltipPosition.bottom,\r\n        _Dir.right => BTTooltipPosition.right,\r\n        _Dir.left => BTTooltipPosition.left,\r\n      };\r\n}\r\n\r\nenum _Dir { above, below, left, right }\r\n\r\n// ── Balloon widget ────────────────────────────────────────────────────────────\r\n\r\n/// Dark rounded balloon showing an optional title, optional separator,\r\n/// and optional list of [BTChartTooltipItem] rows.\r\n///\r\n/// Figma node 3411:2482 — tooltip balloon inside BTChartCard.\r\nclass _BTChartBalloon extends StatelessWidget {\r\n  const _BTChartBalloon({\r\n    required this.bg,\r\n    required this.textColor,\r\n    this.title,\r\n    this.items,\r\n  });\r\n\r\n  final Color bg;\r\n  final Color textColor;\r\n  final String? title;\r\n  final List<BTChartTooltipItem>? items;\r\n\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    final hasItems = items != null && items!.isNotEmpty;\r\n\r\n    // IntrinsicWidth: sizes to the widest child so Expanded works in item Rows\r\n    // and the title can be centered within that natural width.\r\n    return IntrinsicWidth(\r\n      child: Container(\r\n        padding: EdgeInsets.all(16.0),\r\n        decoration: BoxDecoration(\r\n          color: bg,\r\n          borderRadius: BorderRadius.circular(8.0),\r\n        ),\r\n        child: Column(\r\n          mainAxisSize: MainAxisSize.min,\r\n          // stretch: all children receive the same tight width constraint so\r\n          // Expanded inside item Rows has a bounded parent.\r\n          crossAxisAlignment: CrossAxisAlignment.stretch,\r\n          children: [\r\n            // ── Title ───────────────────────────────────────────────────────\r\n            if (title != null)\r\n              Text(\r\n                title!,\r\n                textAlign: TextAlign.center,\r\n                style: TextStyle(\r\n                  fontFamily: BTTypography.fontFamily,\r\n                  fontSize: 14.0,\r\n                  fontWeight: FontWeight.w500,\r\n                  color: textColor,\r\n                ),\r\n              ),\r\n\r\n            // ── Separator ───────────────────────────────────────────────────\r\n            if (title != null && hasItems)\r\n              Padding(\r\n                padding: EdgeInsets.symmetric(vertical: 8.0),\r\n                child: Container(\r\n                  height: 1,\r\n                  color: Colors.white.withOpacity(0.2),\r\n                ),\r\n              ),\r\n\r\n            // ── Item rows ───────────────────────────────────────────────────\r\n            if (hasItems)\r\n              Column(\r\n                mainAxisSize: MainAxisSize.min,\r\n                children: items!.asMap().entries.map((entry) {\r\n                  final isLast = entry.key == items!.length - 1;\r\n                  final item = entry.value;\r\n                  return Padding(\r\n                    padding: isLast\r\n                        ? EdgeInsets.zero\r\n                        : EdgeInsets.only(bottom: 4.0),\r\n                    child: Row(\r\n                      children: [\r\n                        // Color swatch — 12×12, radius 2\r\n                        Container(\r\n                          width: 12.0,\r\n                          height: 12.0,\r\n                          decoration: BoxDecoration(\r\n                            color: item.color,\r\n                            borderRadius: BorderRadius.circular(2.0),\r\n                          ),\r\n                        ),\r\n                        SizedBox(width: 4.0),\r\n                        // Series label — takes all available space\r\n                        Expanded(\r\n                          child: Text(\r\n                            item.label,\r\n                            style: TextStyle(\r\n                              fontFamily: BTTypography.fontFamily,\r\n                              fontSize: 14.0,\r\n                              fontWeight: FontWeight.w500,\r\n                              color: textColor,\r\n                            ),\r\n                          ),\r\n                        ),\r\n                        if (item.label.isNotEmpty) SizedBox(width: 4.0),\r\n                        // Value — bold, right edge\r\n                        Text(\r\n                          item.value,\r\n                          style: TextStyle(\r\n                            fontFamily: BTTypography.fontFamily,\r\n                            fontSize: 14.0,\r\n                            fontWeight: FontWeight.w700,\r\n                            color: textColor,\r\n                          ),\r\n                        ),\r\n                      ],\r\n                    ),\r\n                  );\r\n                }).toList(),\r\n              ),\r\n          ],\r\n        ),\r\n      ),\r\n    );\r\n  }\r\n}\r\n"
    },
    {
      "path": "internal/circle_chart_labels.dart",
      "target": "@flutterBase/chart/internal/circle_chart_labels.dart",
      "type": "registry:ui",
      "content": "import 'package:btech_tokens/btech_tokens.dart';\r\nimport 'package:flutter/material.dart';\r\n\r\n/// External badge label rendered beside every pie/donut segment.\r\n///\r\n/// Shows [label] (medium, secondary color) above [value] (bold, primary color).\r\n/// Used as [PieChartSectionData.badgeWidget] for all [BTCircleChart] variants.\r\nclass BTCirclePieLabel extends StatelessWidget {\r\n  const BTCirclePieLabel({\r\n    required this.label,\r\n    required this.value,\r\n    required this.textColor,\r\n    required this.boldTextColor,\r\n    super.key,\r\n  });\r\n\r\n  final String label;\r\n  final String value;\r\n  final Color textColor;\r\n  final Color boldTextColor;\r\n\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    return Column(\r\n      mainAxisSize: MainAxisSize.min,\r\n      children: [\r\n        if (label.isNotEmpty)\r\n          Text(\r\n            label,\r\n            style: TextStyle(\r\n              fontFamily: BTTypography.fontFamily,\r\n              fontSize: 10.0,\r\n              fontWeight: FontWeight.w500,\r\n              color: textColor,\r\n            ),\r\n          ),\r\n        Text(\r\n          value,\r\n          style: TextStyle(\r\n            fontFamily: BTTypography.fontFamily,\r\n            fontSize: 10,\r\n            fontWeight: FontWeight.w700,\r\n            color: boldTextColor,\r\n          ),\r\n        ),\r\n      ],\r\n    );\r\n  }\r\n}\r\n"
    },
    {
      "path": "internal/circle_chart_painter.dart",
      "target": "@flutterBase/chart/internal/circle_chart_painter.dart",
      "type": "registry:ui",
      "content": "// ignore_for_file: lines_longer_than_80_chars\r\n\r\nimport 'dart:math';\r\n\r\nimport 'package:btech_ui/src/components/molecules/chart/chart.types.dart';\r\nimport 'package:flutter/material.dart';\r\n\r\n/// Draws short radial connector lines from each pie/donut segment's outer\r\n/// edge toward its external badge label (Figma node 3404:1813).\r\n///\r\n/// Place this as a [CustomPaint] in a [Stack] behind the [PieChart] widget so\r\n/// that the lines appear between the chart edge and the floating labels.\r\nclass BTCircleConnectorPainter extends CustomPainter {\r\n  const BTCircleConnectorPainter({\r\n    required this.sections,\r\n    required this.total,\r\n    required this.outerRadius,\r\n    required this.color,\r\n  });\r\n\r\n  final List<BTCircleSection> sections;\r\n  final double total;\r\n\r\n  /// TRUE outer radius of the chart ring — i.e. centerSpaceRadius + ringThickness.\r\n  /// For pie this equals [ringThickness]; for donut/donutRounded use\r\n  /// [centerSpaceRadius] + [ringThickness].\r\n  final double outerRadius;\r\n\r\n  final Color color;\r\n\r\n  // Gap between section outer edge and line start.\r\n  static const double _gap = 3;\r\n  // Length of each connector line in logical pixels.\r\n  static const double _length = 10;\r\n\r\n  @override\r\n  void paint(Canvas canvas, Size size) {\r\n    if (total <= 0) return;\r\n    final center = Offset(size.width / 2, size.height / 2);\r\n    final paint = Paint()\r\n      ..color = color\r\n      ..strokeWidth = 1.0\r\n      ..style = PaintingStyle.stroke\r\n      ..strokeCap = StrokeCap.round;\r\n\r\n    var angle = -90.0; // startDegreeOffset: -90 (12 o'clock)\r\n    for (final sec in sections) {\r\n      final sweep = sec.value / total * 360;\r\n      final mid = (angle + sweep / 2) * pi / 180;\r\n      final dir = Offset(cos(mid), sin(mid));\r\n      canvas.drawLine(\r\n        center + dir * (outerRadius + _gap),\r\n        center + dir * (outerRadius + _gap + _length),\r\n        paint,\r\n      );\r\n      angle += sweep;\r\n    }\r\n  }\r\n\r\n  @override\r\n  bool shouldRepaint(BTCircleConnectorPainter oldDelegate) =>\r\n      oldDelegate.sections != sections ||\r\n      oldDelegate.total != total ||\r\n      oldDelegate.outerRadius != outerRadius ||\r\n      oldDelegate.color != color;\r\n}\r\n\r\n/// Draws donut/donutRounded arcs using [Paint.strokeCap] so section edges\r\n/// are rendered cleanly (no sub-pixel anti-aliasing gaps at boundaries).\r\n///\r\n/// Used for **both** the plain Donut variant (flat ends, no gap, no border)\r\n/// and the DonutRounded variant (rounded ends, 4 px gap, optional border).\r\n///\r\n/// fl_chart is still used (with transparent sections) for badge-label\r\n/// positioning and touch detection; this painter sits below it in the [Stack].\r\nclass BTDonutRoundedPainter extends CustomPainter {\r\n  const BTDonutRoundedPainter({\r\n    required this.sections,\r\n    required this.total,\r\n    required this.innerRadius,\r\n    required this.outerRadius,\r\n    required this.touchedIndex,\r\n    this.sectionsGap = 4.0,\r\n    this.cap = StrokeCap.round,\r\n    this.borderWidth = 0.0,\r\n    this.borderColor = Colors.white,\r\n  });\r\n\r\n  final List<BTCircleSection> sections;\r\n  final double total;\r\n\r\n  /// Radius of the center hole.\r\n  final double innerRadius;\r\n\r\n  /// True outer radius of the ring (innerRadius + ringThickness).\r\n  final double outerRadius;\r\n\r\n  /// Index of the currently touched segment, or -1 for none.\r\n  final int touchedIndex;\r\n\r\n  /// Pixel gap between segments (measured at the ring mid-radius).\r\n  /// Use 0 for the plain Donut variant.\r\n  final double sectionsGap;\r\n\r\n  /// End-cap style: [StrokeCap.round] for donutRounded,\r\n  /// [StrokeCap.butt] for plain Donut.\r\n  final StrokeCap cap;\r\n\r\n  /// Width of the border drawn on the inner and outer edges of each arc.\r\n  /// 0 means no border (plain Donut). Typically 2 for donutRounded.\r\n  final double borderWidth;\r\n\r\n  /// Color of the border — should match the background so it creates a\r\n  /// \"knockout\" outline effect.\r\n  final Color borderColor;\r\n\r\n  @override\r\n  void paint(Canvas canvas, Size size) {\r\n    if (total <= 0) return;\r\n    final center = Offset(size.width / 2, size.height / 2);\r\n    final baseStrokeWidth = outerRadius - innerRadius;\r\n    final baseMidRadius = (innerRadius + outerRadius) / 2;\r\n\r\n    // Convert pixel gap to radians at the mid-radius.\r\n    // When sectionsGap == 0 (plain Donut), gapRadians == 0 and arcs are flush.\r\n    final gapRadians =\r\n        sectionsGap > 0 ? sectionsGap / baseMidRadius : 0.0;\r\n\r\n    var startAngle = -pi / 2; // 12 o'clock (matches fl_chart startDegreeOffset)\r\n\r\n    for (var i = 0; i < sections.length; i++) {\r\n      final sec = sections[i];\r\n      final sectionSweep = sec.value / total * 2 * pi;\r\n\r\n      // Shrink sweep by one full gap (half on each side).\r\n      final adjustedSweep = sectionSweep - gapRadians;\r\n      if (adjustedSweep <= 0) {\r\n        startAngle += sectionSweep;\r\n        continue;\r\n      }\r\n\r\n      final isTouched = i == touchedIndex;\r\n      // When touched, expand ring outward by 8 px (mirrors fl_chart behavior).\r\n      final strokeWidth =\r\n          isTouched ? baseStrokeWidth + 8 : baseStrokeWidth;\r\n      final midRadius = isTouched ? baseMidRadius + 4 : baseMidRadius;\r\n\r\n      final arcStart = startAngle + gapRadians / 2;\r\n      final arcRect = Rect.fromCircle(center: center, radius: midRadius);\r\n\r\n      // ── 1. Border pass (drawn wider, with background color) ─────────────\r\n      // Appears as a 2 px outline on both inner and outer edges of the arc.\r\n      if (borderWidth > 0) {\r\n        canvas.drawArc(\r\n          arcRect,\r\n          arcStart,\r\n          adjustedSweep,\r\n          false,\r\n          Paint()\r\n            ..color = borderColor\r\n            ..style = PaintingStyle.stroke\r\n            ..strokeWidth = strokeWidth + borderWidth * 2\r\n            ..strokeCap = cap,\r\n        );\r\n      }\r\n\r\n      // ── 2. Fill pass (section color) ─────────────────────────────────────\r\n      canvas.drawArc(\r\n        arcRect,\r\n        arcStart,\r\n        adjustedSweep,\r\n        false,\r\n        Paint()\r\n          ..color = sec.color\r\n          ..style = PaintingStyle.stroke\r\n          ..strokeWidth = strokeWidth\r\n          ..strokeCap = cap,\r\n      );\r\n\r\n      startAngle += sectionSweep;\r\n    }\r\n  }\r\n\r\n  @override\r\n  bool shouldRepaint(BTDonutRoundedPainter oldDelegate) =>\r\n      oldDelegate.sections != sections ||\r\n      oldDelegate.total != total ||\r\n      oldDelegate.innerRadius != innerRadius ||\r\n      oldDelegate.outerRadius != outerRadius ||\r\n      oldDelegate.touchedIndex != touchedIndex ||\r\n      oldDelegate.sectionsGap != sectionsGap ||\r\n      oldDelegate.cap != cap ||\r\n      oldDelegate.borderWidth != borderWidth ||\r\n      oldDelegate.borderColor != borderColor;\r\n}\r\n"
    },
    {
      "path": "line_chart.widget.dart",
      "target": "@flutterBase/chart/line_chart.widget.dart",
      "type": "registry:ui",
      "content": "// BTLineChart — line/area chart molecule wrapping fl_chart LineChart.\r\n//\r\n// Example usage:\r\n// ```dart\r\n// // Line (no fill)\r\n// BTLineChart.line(\r\n//   series: [\r\n//     BTLineSeries(\r\n//       color: BTChartColors.info,\r\n//       label: 'Revenue',\r\n//       spots: [BTLineSpot(0, 40), BTLineSpot(1, 60)],\r\n//     ),\r\n//   ],\r\n// )\r\n//\r\n// // Area (translucent fill under each line)\r\n// BTLineChart.area(series: [...])\r\n// ```\r\n//\r\n// Figma node 3398:1486 · M - LineChart\r\n\r\nimport 'package:btech_tokens/btech_tokens.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/chart.types.dart';\r\nimport 'package:btech_ui/src/components/molecules/chart/internal/chart_tooltip.dart';\r\nimport 'package:fl_chart/fl_chart.dart';\r\nimport 'package:flutter/material.dart';\r\n\r\n/// Line chart molecule — supports line and area-fill variants.\r\n///\r\n/// All visual values come from Figma node 3398:1486.\r\n/// Wrap in [BTChartCard] for header, summary row, and legend.\r\n///\r\n/// Touch / drag: press or drag along the chart to see a dark BTTooltip-style\r\n/// balloon showing the x-axis label + per-series [BTChartTooltipItem] rows.\r\n/// A dashed snap line + dots are drawn at the nearest data point.\r\nclass BTLineChart extends StatefulWidget {\r\n  // ── named constructor: line ──────────────────────────────────────────────\r\n\r\n  /// Smooth multi-series line chart without area fill.\r\n  const BTLineChart.line({\r\n    required this.series,\r\n    this.maxY,\r\n    this.height = 160,\r\n    super.key,\r\n  }) : _showArea = false;\r\n\r\n  // ── named constructor: area ──────────────────────────────────────────────\r\n\r\n  /// Smooth multi-series line chart with translucent area fill below each line.\r\n  const BTLineChart.area({\r\n    required this.series,\r\n    this.maxY,\r\n    this.height = 160,\r\n    super.key,\r\n  }) : _showArea = true;\r\n\r\n  // ── fields ────────────────────────────────────────────────────────────────\r\n\r\n  final List<BTLineSeries> series;\r\n  final bool _showArea;\r\n  final double? maxY;\r\n  final double height;\r\n\r\n  @override\r\n  State<BTLineChart> createState() => _BTLineChartState();\r\n}\r\n\r\nclass _BTLineChartState extends State<BTLineChart> {\r\n  /// Finger position in Stack-local coordinates (from [Listener]).\r\n  Offset? _touchPos;\r\n\r\n  /// X-axis label for the nearest data point (tooltip title).\r\n  String? _touchTitle;\r\n\r\n  /// Per-series values at the nearest x position (tooltip rows).\r\n  List<BTChartTooltipItem>? _touchItems;\r\n\r\n  /// Entrance flag — false on first frame (lines flat at y=0), flipped true\r\n  /// after mount so fl_chart tweens every line up to its real values.\r\n  bool _entered = false;\r\n\r\n  @override\r\n  void initState() {\r\n    super.initState();\r\n    WidgetsBinding.instance.addPostFrameCallback((_) {\r\n      if (mounted) setState(() => _entered = true);\r\n    });\r\n  }\r\n\r\n  @override\r\n  Widget build(BuildContext context) {\r\n    final colors = context.btColor;\r\n\r\n    final effectiveMaxY = widget.maxY ?? _inferMaxY();\r\n\r\n    final axisTextStyle = TextStyle(\r\n      fontFamily: BTTypography.fontFamily,\r\n      fontSize: 10.0,\r\n      fontWeight: FontWeight.w500,\r\n      color: colors.text.secondary,\r\n    );\r\n\r\n    // Count x ticks from first series spots.\r\n    final xCount = widget.series.isEmpty\r\n        ? 0\r\n        : widget.series.first.spots\r\n                .map((s) => s.x)\r\n                .fold<double>(0, (prev, x) => x > prev ? x : prev)\r\n                .toInt() +\r\n            1;\r\n\r\n    final lineBarsData = widget.series.map((s) {\r\n      return LineChartBarData(\r\n        spots: s.spots\r\n            .map((p) => FlSpot(p.x, _entered ? p.y : 0))\r\n            .toList(),\r\n        isCurved: true,\r\n        color: s.color,\r\n        barWidth: 2,\r\n        isStrokeCapRound: true,\r\n        dotData: const FlDotData(show: false),\r\n        belowBarData: BarAreaData(\r\n          show: widget._showArea,\r\n          color: s.color.withOpacity(0.12),\r\n        ),\r\n      );\r\n    }).toList();\r\n\r\n    final indicatorLineColor = colors.bg.inverse.withOpacity(0.5);\r\n\r\n    final chart = LineChart(\r\n      LineChartData(\r\n        maxY: effectiveMaxY,\r\n        minY: 0,\r\n        lineBarsData: lineBarsData,\r\n        gridData: FlGridData(\r\n          show: true,\r\n          drawVerticalLine: false,\r\n          horizontalInterval: effectiveMaxY / 5,\r\n          getDrawingHorizontalLine: (_) => FlLine(\r\n            color: colors.border.primary,\r\n            strokeWidth: 1,\r\n          ),\r\n        ),\r\n        borderData: FlBorderData(show: false),\r\n        titlesData: FlTitlesData(\r\n          leftTitles: AxisTitles(\r\n            sideTitles: SideTitles(\r\n              showTitles: true,\r\n              reservedSize: 28,\r\n              interval: effectiveMaxY / 5,\r\n              getTitlesWidget: (value, _) => Text(\r\n                value.toInt().toString(),\r\n                style: axisTextStyle,\r\n                textAlign: TextAlign.end,\r\n              ),\r\n            ),\r\n          ),\r\n          bottomTitles: AxisTitles(\r\n            sideTitles: SideTitles(\r\n              showTitles: true,\r\n              reservedSize: 20,\r\n              interval: 1,\r\n              getTitlesWidget: (value, _) {\r\n                if (value != value.truncateToDouble()) {\r\n                  return const SizedBox.shrink();\r\n                }\r\n                final idx = value.toInt();\r\n                if (idx < 0 || idx >= xCount) {\r\n                  return const SizedBox.shrink();\r\n                }\r\n                return Padding(\r\n                  padding: EdgeInsetsDirectional.only(top: 4.0),\r\n                  child: Text('Label', style: axisTextStyle),\r\n                );\r\n              },\r\n            ),\r\n          ),\r\n          rightTitles: const AxisTitles(\r\n            sideTitles: SideTitles(showTitles: false),\r\n          ),\r\n          topTitles: const AxisTitles(\r\n            sideTitles: SideTitles(showTitles: false),\r\n          ),\r\n        ),\r\n        lineTouchData: LineTouchData(\r\n          // true → fl_chart renders dots via getTouchedSpotIndicator.\r\n          handleBuiltInTouches: true,\r\n          touchSpotThreshold: 200,\r\n          // Suppress fl_chart's built-in tooltip balloon — we draw our own.\r\n          touchTooltipData: LineTouchTooltipData(\r\n            getTooltipColor: (_) => Colors.transparent,\r\n            getTooltipItems: (spots) => List.filled(spots.length, null),\r\n          ),\r\n          getTouchedSpotIndicator:\r\n              (LineChartBarData barData, List<int> spotIndexes) {\r\n            return spotIndexes.map((index) {\r\n              return TouchedSpotIndicatorData(\r\n                // Suppress fl_chart's built-in crosshair — we draw our own.\r\n                const FlLine(color: Colors.transparent),\r\n                FlDotData(\r\n                  show: true,\r\n                  getDotPainter: (spot, percent, bar, idx) {\r\n                    return FlDotCirclePainter(\r\n                      radius: 4,\r\n                      color: bar.color ?? Colors.white,\r\n                      strokeWidth: 2,\r\n                      strokeColor: Colors.white,\r\n                    );\r\n                  },\r\n                ),\r\n              );\r\n            }).toList();\r\n          },\r\n          touchCallback: (FlTouchEvent event, LineTouchResponse? response) {\r\n            if (!mounted) return;\r\n            if (!event.isInterestedForInteractions ||\r\n                response == null ||\r\n                response.lineBarSpots == null ||\r\n                response.lineBarSpots!.isEmpty) {\r\n              if (_touchItems != null) {\r\n                setState(() {\r\n                  _touchTitle = null;\r\n                  _touchItems = null;\r\n                });\r\n              }\r\n              return;\r\n            }\r\n\r\n            final spots = response.lineBarSpots!;\r\n            final xIdx = spots.first.x.toInt();\r\n\r\n            // Title: use spot x index as label\r\n            final newTitle = 'L${xIdx + 1}';\r\n\r\n            // Items: one row per series at this x position\r\n            final newItems = spots\r\n                .map((spot) {\r\n                  final seriesIdx = spot.barIndex;\r\n                  final s = (seriesIdx >= 0 &&\r\n                          seriesIdx < widget.series.length)\r\n                      ? widget.series[seriesIdx]\r\n                      : null;\r\n                  return BTChartTooltipItem(\r\n                    color: s?.color ?? Colors.grey,\r\n                    label: s?.label ?? '',\r\n                    value: spot.y.toStringAsFixed(0),\r\n                  );\r\n                })\r\n                .toList();\r\n\r\n            if (_touchTitle != newTitle) {\r\n              setState(() {\r\n                _touchTitle = newTitle;\r\n                _touchItems = newItems;\r\n              });\r\n            }\r\n          },\r\n        ),\r\n      ),\r\n      // Entrance tween — lines rise from the baseline to their real values on\r\n      // first mount (driven by the _entered flag flip in a post-frame callback).\r\n      duration: const Duration(milliseconds: 700),\r\n      curve: Curves.easeOutCubic,\r\n    );\r\n\r\n    return SizedBox(\r\n      height: widget.height,\r\n      child: LayoutBuilder(\r\n        builder: (context, constraints) {\r\n          final chartWidth = constraints.maxWidth;\r\n          return Stack(\r\n            clipBehavior: Clip.none,\r\n            children: [\r\n              Listener(\r\n                onPointerDown: (e) =>\r\n                    setState(() => _touchPos = e.localPosition),\r\n                onPointerMove: (e) {\r\n                  final pos = e.localPosition;\r\n                  if (pos.dx < 0 ||\r\n                      pos.dx > chartWidth ||\r\n                      pos.dy < 0 ||\r\n                      pos.dy > widget.height) {\r\n                    setState(() {\r\n                      _touchPos = null;\r\n                      _touchTitle = null;\r\n                      _touchItems = null;\r\n                    });\r\n                  } else {\r\n                    setState(() => _touchPos = pos);\r\n                  }\r\n                },\r\n                onPointerUp: (_) => setState(() {\r\n                  _touchPos = null;\r\n                  _touchTitle = null;\r\n                  _touchItems = null;\r\n                }),\r\n                onPointerCancel: (_) => setState(() {\r\n                  _touchPos = null;\r\n                  _touchTitle = null;\r\n                  _touchItems = null;\r\n                }),\r\n                child: chart,\r\n              ),\r\n              // Full-height vertical indicator line.\r\n              if (_touchPos != null)\r\n                Positioned(\r\n                  left: _touchPos!.dx,\r\n                  top: 0,\r\n                  bottom: 20,\r\n                  child: IgnorePointer(\r\n                    child: Container(\r\n                      width: 1,\r\n                      color: indicatorLineColor,\r\n                    ),\r\n                  ),\r\n                ),\r\n              if (_touchPos != null && _touchItems != null)\r\n                BTChartTooltipOverlay(\r\n                  title: _touchTitle,\r\n                  items: _touchItems,\r\n                  touchDx: _touchPos!.dx,\r\n                  touchDy: _touchPos!.dy,\r\n                  chartWidth: chartWidth,\r\n                  chartHeight: widget.height,\r\n                ),\r\n            ],\r\n          );\r\n        },\r\n      ),\r\n    );\r\n  }\r\n\r\n  double _inferMaxY() {\r\n    var max = 0.0;\r\n    for (final s in widget.series) {\r\n      for (final p in s.spots) {\r\n        if (p.y > max) max = p.y;\r\n      }\r\n    }\r\n    if (max == 0) return 100;\r\n    return ((max / 20).ceil() * 20).toDouble();\r\n  }\r\n}\r\n"
    }
  ]
}